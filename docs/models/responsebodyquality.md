# ResponseBodyQuality

The quality of the image that will be generated. `auto` will automatically select the best quality for the given model.

## Example Usage

```python
from orq_ai_sdk.models import ResponseBodyQuality
value: ResponseBodyQuality = "auto"
```


## Values

- `"auto"`
- `"high"`
- `"medium"`
- `"low"`
- `"hd"`
- `"standard"`
