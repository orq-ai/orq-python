# Shorthand

Shorthand: "auto" lets the model decide, "none" disables tools, "required" forces tool use.

## Example Usage

```python
from orq_ai_sdk.models import Shorthand
value: Shorthand = "auto"
```


## Values

- `"auto"`
- `"none"`
- `"required"`
