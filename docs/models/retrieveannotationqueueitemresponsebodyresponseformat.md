# RetrieveAnnotationQueueItemResponseBodyResponseFormat

The format in which the generated images are returned. Some of the models only return the image in base64 format.

## Example Usage

```python
from orq_ai_sdk.models import RetrieveAnnotationQueueItemResponseBodyResponseFormat
value: RetrieveAnnotationQueueItemResponseBodyResponseFormat = "url"
```


## Values

- `"url"`
- `"b64_json"`
