# SortBy

Field to sort by. Defaults to created_at (newest first).

## Example Usage

```python
from orq_ai_sdk.models import SortBy
value: SortBy = "created_at"
```


## Values

- `"created_at"`
- `"updated_at"`
- `"display_name"`
