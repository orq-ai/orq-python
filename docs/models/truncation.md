# Truncation

Controls how the model handles inputs longer than the maximum token length

## Example Usage

```python
from orq_ai_sdk.models import Truncation
value: Truncation = "auto"
```


## Values

- `"auto"`
- `"disabled"`
