# QueryReportRequestSort

Value ordering for `scalar` rows. Defaults to `desc`. Ignored for `timeseries`.

## Example Usage

```python
from orq_ai_sdk.models import QueryReportRequestSort
value: QueryReportRequestSort = "desc"
```


## Values

- `"desc"`
- `"asc"`
