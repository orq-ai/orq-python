# ResponseMetaCurrency

ISO 4217 currency code for cost fields. Always `USD` today.

## Example Usage

```python
from orq_ai_sdk.models import ResponseMetaCurrency
value: ResponseMetaCurrency = "USD"
```


## Values

- `"USD"`
