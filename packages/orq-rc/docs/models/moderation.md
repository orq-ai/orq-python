# Moderation

Control the content-moderation level for images generated by `gpt-image-1`. Must be either `low` or `auto`.

## Example Usage

```python
from orq_ai_sdk.models import Moderation
value: Moderation = "low"
```


## Values

- `"low"`
- `"auto"`
