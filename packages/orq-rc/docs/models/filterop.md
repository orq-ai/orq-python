# FilterOp

Predicate operator. `eq` and `neq` accept exactly one value; `in` and `not_in` accept 1-100 values.

## Example Usage

```python
from orq_ai_sdk.models import FilterOp
value: FilterOp = "eq"
```


## Values

- `"eq"`
- `"neq"`
- `"in"`
- `"not_in"`
