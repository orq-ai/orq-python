# AccessMode

Whether the agent may only read this file system, or also write to it.

## Example Usage

```python
from orq_ai_sdk.models import AccessMode
value: AccessMode = "read_only"
```


## Values

- `"read_only"`
- `"read_write"`
