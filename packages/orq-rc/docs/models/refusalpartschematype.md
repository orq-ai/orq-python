# RefusalPartSchemaType

The type of the content part. Always `refusal`.

## Example Usage

```python
from orq_ai_sdk.models import RefusalPartSchemaType
value: RefusalPartSchemaType = "refusal"
```


## Values

- `"refusal"`
