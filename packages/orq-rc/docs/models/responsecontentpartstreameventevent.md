# ResponseContentPartStreamEventEvent

The SSE event name, equal to the payload's `type`.

## Example Usage

```python
from orq_ai_sdk.models import ResponseContentPartStreamEventEvent
value: ResponseContentPartStreamEventEvent = "response.content_part.added"
```


## Values

- `"response.content_part.added"`
- `"response.content_part.done"`
