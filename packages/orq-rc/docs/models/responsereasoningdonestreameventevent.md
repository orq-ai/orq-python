# ResponseReasoningDoneStreamEventEvent

The SSE event name, equal to the payload's `type`.

## Example Usage

```python
from orq_ai_sdk.models import ResponseReasoningDoneStreamEventEvent
value: ResponseReasoningDoneStreamEventEvent = "response.reasoning.done"
```


## Values

- `"response.reasoning.done"`
- `"response.reasoning_text.done"`
