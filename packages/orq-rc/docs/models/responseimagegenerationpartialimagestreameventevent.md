# ResponseImageGenerationPartialImageStreamEventEvent

The SSE event name, equal to the payload's `type`.

## Example Usage

```python
from orq_ai_sdk.models import ResponseImageGenerationPartialImageStreamEventEvent
value: ResponseImageGenerationPartialImageStreamEventEvent = "response.image_generation_call.partial_image"
```


## Values

- `"response.image_generation_call.partial_image"`
