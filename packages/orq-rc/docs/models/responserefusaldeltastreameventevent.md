# ResponseRefusalDeltaStreamEventEvent

The SSE event name, equal to the payload's `type`.

## Example Usage

```python
from orq_ai_sdk.models import ResponseRefusalDeltaStreamEventEvent
value: ResponseRefusalDeltaStreamEventEvent = "response.refusal.delta"
```


## Values

- `"response.refusal.delta"`
