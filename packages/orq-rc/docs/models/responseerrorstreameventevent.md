# ResponseErrorStreamEventEvent

The SSE event name, equal to the payload's `type`.

## Example Usage

```python
from orq_ai_sdk.models import ResponseErrorStreamEventEvent
value: ResponseErrorStreamEventEvent = "error"
```


## Values

- `"error"`
