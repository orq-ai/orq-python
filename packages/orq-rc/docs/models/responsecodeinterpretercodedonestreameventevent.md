# ResponseCodeInterpreterCodeDoneStreamEventEvent

The SSE event name, equal to the payload's `type`.

## Example Usage

```python
from orq_ai_sdk.models import ResponseCodeInterpreterCodeDoneStreamEventEvent
value: ResponseCodeInterpreterCodeDoneStreamEventEvent = "response.code_interpreter_call_code.done"
```


## Values

- `"response.code_interpreter_call_code.done"`
