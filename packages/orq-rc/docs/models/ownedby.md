# OwnedBy

Describe the provider of the model. Every model with `vendor` is publicly available, while models with `workspace` are private.

## Example Usage

```python
from orq_ai_sdk.models import OwnedBy
value: OwnedBy = "vendor"
```


## Values

- `"vendor"`
- `"workspace"`
