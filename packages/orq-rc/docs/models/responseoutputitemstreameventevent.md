# ResponseOutputItemStreamEventEvent

The SSE event name, equal to the payload's `type`.

## Example Usage

```python
from orq_ai_sdk.models import ResponseOutputItemStreamEventEvent
value: ResponseOutputItemStreamEventEvent = "response.output_item.added"
```


## Values

- `"response.output_item.added"`
- `"response.output_item.done"`
