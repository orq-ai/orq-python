# Format

The format of the encoded audio data. Currently supports `wav` and `mp3`.

## Example Usage

```python
from orq_ai_sdk.models import Format
value: Format = "mp3"
```


## Values

- `"mp3"`
- `"wav"`
