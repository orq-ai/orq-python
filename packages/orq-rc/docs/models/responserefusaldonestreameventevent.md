# ResponseRefusalDoneStreamEventEvent

The SSE event name, equal to the payload's `type`.

## Example Usage

```python
from orq_ai_sdk.models import ResponseRefusalDoneStreamEventEvent
value: ResponseRefusalDoneStreamEventEvent = "response.refusal.done"
```


## Values

- `"response.refusal.done"`
