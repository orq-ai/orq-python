# CreateImageEditResponseFormat

The format in which the generated images are returned. Some of the models only return the image in base64 format.

## Example Usage

```python
from orq_ai_sdk.models import CreateImageEditResponseFormat
value: CreateImageEditResponseFormat = "url"
```


## Values

| Name       | Value      |
| ---------- | ---------- |
| `URL`      | url        |
| `B64_JSON` | b64_json   |