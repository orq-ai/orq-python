# FactoryBudgetScope

Per-factory cap. Matches requests whose api key carries the label
 `factory=<factory_id>` (keys minted for agent sessions of that factory).


## Fields

| Field              | Type               | Required           | Description        |
| ------------------ | ------------------ | ------------------ | ------------------ |
| `factory_id`       | *str*              | :heavy_check_mark: | N/A                |