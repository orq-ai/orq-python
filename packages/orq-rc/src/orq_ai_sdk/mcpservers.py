"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from .basesdk import BaseSDK
from orq_ai_sdk import models, utils
from orq_ai_sdk._hooks import HookContext
from orq_ai_sdk.types import OptionalNullable, UNSET
from orq_ai_sdk.utils import get_security_from_env
from orq_ai_sdk.utils.unmarshal_json_response import unmarshal_json_response
from typing import Dict, Mapping, Optional, Union


class McpServers(BaseSDK):
    def list(
        self,
        *,
        limit: Optional[int] = None,
        starting_after: Optional[str] = None,
        ending_before: Optional[str] = None,
        search: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.ListMcpServersResponse:
        r"""List MCP servers

        Returns a paginated list of MCP servers in the workspace.

        :param limit: Page size between 1 and 200. Defaults to 25.
        :param starting_after: Cursor for the page after the given item id. Mutually exclusive with `ending_before`.
        :param ending_before: Cursor for the page before the given item id. Mutually exclusive with `starting_after`.
        :param search: Case-insensitive match against the server key and display name.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.McpServerListRequest(
            limit=limit,
            starting_after=starting_after,
            ending_before=ending_before,
            search=search,
        )

        req = self._build_request(
            method="GET",
            path="/v2/mcp-servers",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpServerList",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpServers",
                    "x-cli-name": "list",
                    "x-code-samples": [
                        {
                            "label": "Core - List MCP servers",
                            "lang": "curl",
                            "source": "curl --get 'https://api.orq.ai/v2/mcp-servers' \\\n  --header 'Authorization: Bearer $ORQ_API_KEY' \\\n  --data-urlencode 'limit=25' \\\n  --data-urlencode 'search=github'\n",
                        },
                        {
                            "label": "Python - List MCP servers",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\npage = client.mcp_servers.list(\n    limit=25,\n    search="github",\n)\n\nfor server in page.data:\n    print(server.key, server.sync_state.tools_total)\n',
                        },
                        {
                            "label": "Node.js - List MCP servers",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\nconst page = await client.mcpServers.list({\n  limit: 25,\n  search: 'github',\n});\n\nfor (const server of page.data) {\n  console.log(server.key, server.syncState.toolsTotal);\n}\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.ListMcpServersResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    async def list_async(
        self,
        *,
        limit: Optional[int] = None,
        starting_after: Optional[str] = None,
        ending_before: Optional[str] = None,
        search: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.ListMcpServersResponse:
        r"""List MCP servers

        Returns a paginated list of MCP servers in the workspace.

        :param limit: Page size between 1 and 200. Defaults to 25.
        :param starting_after: Cursor for the page after the given item id. Mutually exclusive with `ending_before`.
        :param ending_before: Cursor for the page before the given item id. Mutually exclusive with `starting_after`.
        :param search: Case-insensitive match against the server key and display name.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.McpServerListRequest(
            limit=limit,
            starting_after=starting_after,
            ending_before=ending_before,
            search=search,
        )

        req = self._build_request_async(
            method="GET",
            path="/v2/mcp-servers",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpServerList",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpServers",
                    "x-cli-name": "list",
                    "x-code-samples": [
                        {
                            "label": "Core - List MCP servers",
                            "lang": "curl",
                            "source": "curl --get 'https://api.orq.ai/v2/mcp-servers' \\\n  --header 'Authorization: Bearer $ORQ_API_KEY' \\\n  --data-urlencode 'limit=25' \\\n  --data-urlencode 'search=github'\n",
                        },
                        {
                            "label": "Python - List MCP servers",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\npage = client.mcp_servers.list(\n    limit=25,\n    search="github",\n)\n\nfor server in page.data:\n    print(server.key, server.sync_state.tools_total)\n',
                        },
                        {
                            "label": "Node.js - List MCP servers",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\nconst page = await client.mcpServers.list({\n  limit: 25,\n  search: 'github',\n});\n\nfor (const server of page.data) {\n  console.log(server.key, server.syncState.toolsTotal);\n}\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.ListMcpServersResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    def create(
        self,
        *,
        key: str,
        display_name: str,
        connection: Union[models.McpConnection, models.McpConnectionTypedDict],
        auth: Union[models.McpAuthConfig, models.McpAuthConfigTypedDict],
        default_tool_exposure: Union[
            models.McpToolExposure, models.McpToolExposureTypedDict
        ],
        description: Optional[str] = None,
        sharing: Optional[Union[models.Sharing, models.SharingTypedDict]] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.CreateMcpServerResponse:
        r"""Create an MCP server

        Creates a new upstream MCP server connection in the workspace.

        :param key: Lowercase slug of letters, digits, hyphens and underscores, max 64 characters, unique per workspace; prefixes this server's tool names in gateways.
        :param display_name: Human readable name shown in the workspace.
        :param connection: How the gateway dials the upstream server.
        :param auth: Credentials the gateway sends upstream; send `type: NONE` explicitly for public servers.
        :param default_tool_exposure: Fallback exposure used by any gateway link that does not set its own.
        :param description: Free-form note about what this server is for.
        :param sharing: Which projects in the workspace may use this server. Defaults to every project.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.CreateMcpServerRequest(
            key=key,
            display_name=display_name,
            description=description,
            connection=utils.get_pydantic_model(connection, models.McpConnection),
            auth=utils.get_pydantic_model(auth, models.McpAuthConfig),
            default_tool_exposure=utils.get_pydantic_model(
                default_tool_exposure, models.McpToolExposure
            ),
            sharing=utils.get_pydantic_model(sharing, Optional[models.Sharing]),
        )

        req = self._build_request(
            method="POST",
            path="/v2/mcp-servers",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.CreateMcpServerRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpServerCreate",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpServers",
                    "x-cli-name": "create",
                    "x-code-samples": [
                        {
                            "label": "Core - Create MCP server",
                            "lang": "curl",
                            "source": 'curl --request POST \\\n  --url \'https://api.orq.ai/v2/mcp-servers\' \\\n  --header \'Authorization: Bearer $ORQ_API_KEY\' \\\n  --header \'Content-Type: application/json\' \\\n  --data \'{\n    "key": "github",\n    "display_name": "GitHub",\n    "description": "Repository and issue tools for the platform team.",\n    "connection": {\n      "type": "MCP_CONNECTION_TYPE_HTTP",\n      "url": "https://api.githubcopilot.com/mcp/"\n    },\n    "auth": {\n      "type": "MCP_AUTH_TYPE_STATIC_HEADERS",\n      "static_headers": [\n        { "key": "Authorization", "value": "Bearer ghp_example_token" }\n      ]\n    },\n    "default_tool_exposure": { "mode": "MCP_TOOL_EXPOSURE_MODE_ALL" }\n  }\'\n',
                        },
                        {
                            "label": "Python - Create MCP server",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\nresult = client.mcp_servers.create(\n    key="github",\n    display_name="GitHub",\n    description="Repository and issue tools for the platform team.",\n    connection={\n        "type": "MCP_CONNECTION_TYPE_HTTP",\n        "url": "https://api.githubcopilot.com/mcp/",\n    },\n    auth={\n        "type": "MCP_AUTH_TYPE_STATIC_HEADERS",\n        "static_headers": [\n            {"key": "Authorization", "value": "Bearer ghp_example_token"},\n        ],\n    },\n    default_tool_exposure={"mode": "MCP_TOOL_EXPOSURE_MODE_ALL"},\n)\n\n# A new server has no tools until it is synced.\nprint(result.mcp_server.key)\n',
                        },
                        {
                            "label": "Node.js - Create MCP server",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\nconst result = await client.mcpServers.create({\n  key: 'github',\n  displayName: 'GitHub',\n  description: 'Repository and issue tools for the platform team.',\n  connection: {\n    type: 'MCP_CONNECTION_TYPE_HTTP',\n    url: 'https://api.githubcopilot.com/mcp/',\n  },\n  auth: {\n    type: 'MCP_AUTH_TYPE_STATIC_HEADERS',\n    staticHeaders: [\n      { key: 'Authorization', value: 'Bearer ghp_example_token' },\n    ],\n  },\n  defaultToolExposure: { mode: 'MCP_TOOL_EXPOSURE_MODE_ALL' },\n});\n\n// A new server has no tools until it is synced.\nconsole.log(result.mcpServer.key);\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.CreateMcpServerResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    async def create_async(
        self,
        *,
        key: str,
        display_name: str,
        connection: Union[models.McpConnection, models.McpConnectionTypedDict],
        auth: Union[models.McpAuthConfig, models.McpAuthConfigTypedDict],
        default_tool_exposure: Union[
            models.McpToolExposure, models.McpToolExposureTypedDict
        ],
        description: Optional[str] = None,
        sharing: Optional[Union[models.Sharing, models.SharingTypedDict]] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.CreateMcpServerResponse:
        r"""Create an MCP server

        Creates a new upstream MCP server connection in the workspace.

        :param key: Lowercase slug of letters, digits, hyphens and underscores, max 64 characters, unique per workspace; prefixes this server's tool names in gateways.
        :param display_name: Human readable name shown in the workspace.
        :param connection: How the gateway dials the upstream server.
        :param auth: Credentials the gateway sends upstream; send `type: NONE` explicitly for public servers.
        :param default_tool_exposure: Fallback exposure used by any gateway link that does not set its own.
        :param description: Free-form note about what this server is for.
        :param sharing: Which projects in the workspace may use this server. Defaults to every project.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.CreateMcpServerRequest(
            key=key,
            display_name=display_name,
            description=description,
            connection=utils.get_pydantic_model(connection, models.McpConnection),
            auth=utils.get_pydantic_model(auth, models.McpAuthConfig),
            default_tool_exposure=utils.get_pydantic_model(
                default_tool_exposure, models.McpToolExposure
            ),
            sharing=utils.get_pydantic_model(sharing, Optional[models.Sharing]),
        )

        req = self._build_request_async(
            method="POST",
            path="/v2/mcp-servers",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.CreateMcpServerRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpServerCreate",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpServers",
                    "x-cli-name": "create",
                    "x-code-samples": [
                        {
                            "label": "Core - Create MCP server",
                            "lang": "curl",
                            "source": 'curl --request POST \\\n  --url \'https://api.orq.ai/v2/mcp-servers\' \\\n  --header \'Authorization: Bearer $ORQ_API_KEY\' \\\n  --header \'Content-Type: application/json\' \\\n  --data \'{\n    "key": "github",\n    "display_name": "GitHub",\n    "description": "Repository and issue tools for the platform team.",\n    "connection": {\n      "type": "MCP_CONNECTION_TYPE_HTTP",\n      "url": "https://api.githubcopilot.com/mcp/"\n    },\n    "auth": {\n      "type": "MCP_AUTH_TYPE_STATIC_HEADERS",\n      "static_headers": [\n        { "key": "Authorization", "value": "Bearer ghp_example_token" }\n      ]\n    },\n    "default_tool_exposure": { "mode": "MCP_TOOL_EXPOSURE_MODE_ALL" }\n  }\'\n',
                        },
                        {
                            "label": "Python - Create MCP server",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\nresult = client.mcp_servers.create(\n    key="github",\n    display_name="GitHub",\n    description="Repository and issue tools for the platform team.",\n    connection={\n        "type": "MCP_CONNECTION_TYPE_HTTP",\n        "url": "https://api.githubcopilot.com/mcp/",\n    },\n    auth={\n        "type": "MCP_AUTH_TYPE_STATIC_HEADERS",\n        "static_headers": [\n            {"key": "Authorization", "value": "Bearer ghp_example_token"},\n        ],\n    },\n    default_tool_exposure={"mode": "MCP_TOOL_EXPOSURE_MODE_ALL"},\n)\n\n# A new server has no tools until it is synced.\nprint(result.mcp_server.key)\n',
                        },
                        {
                            "label": "Node.js - Create MCP server",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\nconst result = await client.mcpServers.create({\n  key: 'github',\n  displayName: 'GitHub',\n  description: 'Repository and issue tools for the platform team.',\n  connection: {\n    type: 'MCP_CONNECTION_TYPE_HTTP',\n    url: 'https://api.githubcopilot.com/mcp/',\n  },\n  auth: {\n    type: 'MCP_AUTH_TYPE_STATIC_HEADERS',\n    staticHeaders: [\n      { key: 'Authorization', value: 'Bearer ghp_example_token' },\n    ],\n  },\n  defaultToolExposure: { mode: 'MCP_TOOL_EXPOSURE_MODE_ALL' },\n});\n\n// A new server has no tools until it is synced.\nconsole.log(result.mcpServer.key);\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.CreateMcpServerResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    def retrieve(
        self,
        *,
        id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.GetMcpServerResponse:
        r"""Retrieve an MCP server

        Retrieves the details of an existing MCP server by its unique ID.

        :param id: Unique identifier of the MCP server.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.McpServerGetRequest(
            id=id,
        )

        req = self._build_request(
            method="GET",
            path="/v2/mcp-servers/{id}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpServerGet",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpServers",
                    "x-cli-name": "retrieve",
                    "x-code-samples": [
                        {
                            "label": "Core - Retrieve an MCP server",
                            "lang": "curl",
                            "source": "curl --get 'https://api.orq.ai/v2/mcp-servers/01JQ0K5R8N2ZC7X4M9T3V6HWBD' \\\n  --header 'Authorization: Bearer $ORQ_API_KEY'\n",
                        },
                        {
                            "label": "Python - Retrieve an MCP server",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\nresult = client.mcp_servers.retrieve(id="01JQ0K5R8N2ZC7X4M9T3V6HWBD")\n\nfor tool in result.mcp_server.tools:\n    print(tool.name, tool.status)\n',
                        },
                        {
                            "label": "Node.js - Retrieve an MCP server",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\nconst result = await client.mcpServers.retrieve({\n  id: '01JQ0K5R8N2ZC7X4M9T3V6HWBD',\n});\n\nfor (const tool of result.mcpServer.tools) {\n  console.log(tool.name, tool.status);\n}\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.GetMcpServerResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    async def retrieve_async(
        self,
        *,
        id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.GetMcpServerResponse:
        r"""Retrieve an MCP server

        Retrieves the details of an existing MCP server by its unique ID.

        :param id: Unique identifier of the MCP server.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.McpServerGetRequest(
            id=id,
        )

        req = self._build_request_async(
            method="GET",
            path="/v2/mcp-servers/{id}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpServerGet",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpServers",
                    "x-cli-name": "retrieve",
                    "x-code-samples": [
                        {
                            "label": "Core - Retrieve an MCP server",
                            "lang": "curl",
                            "source": "curl --get 'https://api.orq.ai/v2/mcp-servers/01JQ0K5R8N2ZC7X4M9T3V6HWBD' \\\n  --header 'Authorization: Bearer $ORQ_API_KEY'\n",
                        },
                        {
                            "label": "Python - Retrieve an MCP server",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\nresult = client.mcp_servers.retrieve(id="01JQ0K5R8N2ZC7X4M9T3V6HWBD")\n\nfor tool in result.mcp_server.tools:\n    print(tool.name, tool.status)\n',
                        },
                        {
                            "label": "Node.js - Retrieve an MCP server",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\nconst result = await client.mcpServers.retrieve({\n  id: '01JQ0K5R8N2ZC7X4M9T3V6HWBD',\n});\n\nfor (const tool of result.mcpServer.tools) {\n  console.log(tool.name, tool.status);\n}\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.GetMcpServerResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    def delete(
        self,
        *,
        id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.DeleteMcpServerResponse:
        r"""Delete an MCP server

        Deletes an MCP server from the workspace. The response body is empty when the delete succeeds.

        :param id: Unique identifier of the MCP server.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.McpServerDeleteRequest(
            id=id,
        )

        req = self._build_request(
            method="DELETE",
            path="/v2/mcp-servers/{id}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpServerDelete",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpServers",
                    "x-cli-name": "delete",
                    "x-code-samples": [
                        {
                            "label": "Core - Delete an MCP server",
                            "lang": "curl",
                            "source": "curl --request DELETE \\\n  --url 'https://api.orq.ai/v2/mcp-servers/01JQ0K5R8N2ZC7X4M9T3V6HWBD' \\\n  --header 'Authorization: Bearer $ORQ_API_KEY'\n",
                        },
                        {
                            "label": "Python - Delete an MCP server",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\n# Fails while any gateway still links this server.\nclient.mcp_servers.delete(id="01JQ0K5R8N2ZC7X4M9T3V6HWBD")\n',
                        },
                        {
                            "label": "Node.js - Delete an MCP server",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\n// Fails while any gateway still links this server.\nawait client.mcpServers.delete({ id: '01JQ0K5R8N2ZC7X4M9T3V6HWBD' });\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.DeleteMcpServerResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    async def delete_async(
        self,
        *,
        id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.DeleteMcpServerResponse:
        r"""Delete an MCP server

        Deletes an MCP server from the workspace. The response body is empty when the delete succeeds.

        :param id: Unique identifier of the MCP server.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.McpServerDeleteRequest(
            id=id,
        )

        req = self._build_request_async(
            method="DELETE",
            path="/v2/mcp-servers/{id}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpServerDelete",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpServers",
                    "x-cli-name": "delete",
                    "x-code-samples": [
                        {
                            "label": "Core - Delete an MCP server",
                            "lang": "curl",
                            "source": "curl --request DELETE \\\n  --url 'https://api.orq.ai/v2/mcp-servers/01JQ0K5R8N2ZC7X4M9T3V6HWBD' \\\n  --header 'Authorization: Bearer $ORQ_API_KEY'\n",
                        },
                        {
                            "label": "Python - Delete an MCP server",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\n# Fails while any gateway still links this server.\nclient.mcp_servers.delete(id="01JQ0K5R8N2ZC7X4M9T3V6HWBD")\n',
                        },
                        {
                            "label": "Node.js - Delete an MCP server",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\n// Fails while any gateway still links this server.\nawait client.mcpServers.delete({ id: '01JQ0K5R8N2ZC7X4M9T3V6HWBD' });\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.DeleteMcpServerResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    def update(
        self,
        *,
        id: str,
        key: Optional[str] = None,
        display_name: Optional[str] = None,
        description: Optional[str] = None,
        connection: Optional[
            Union[models.McpConnection, models.McpConnectionTypedDict]
        ] = None,
        auth: Optional[
            Union[models.McpAuthConfig, models.McpAuthConfigTypedDict]
        ] = None,
        default_tool_exposure: Optional[
            Union[models.McpToolExposure, models.McpToolExposureTypedDict]
        ] = None,
        sharing: Optional[Union[models.Sharing, models.SharingTypedDict]] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.UpdateMcpServerResponse:
        r"""Update an MCP server

        Updates mutable fields of an existing MCP server. Omitted optional fields keep their current values.

        :param id: Unique identifier of the MCP server.
        :param key: Rejected with INVALID_ARGUMENT: the key prefixes this server's tools in
            every gateway that exposes it, so it is immutable after creation. Retained
            so callers get an error rather than a silently ignored field.
        :param display_name: Human readable name shown in the workspace.
        :param description: Free-form note about what this server is for.
        :param connection: How the gateway dials the upstream server.
        :param auth: Credentials the gateway sends upstream; send `type: NONE` explicitly for public servers.
        :param default_tool_exposure: Fallback exposure used by any gateway link that does not set its own.
        :param sharing: Which projects in the workspace may use this server. Defaults to every project.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.McpServerUpdateRequest(
            id=id,
            update_mcp_server_request=models.UpdateMcpServerRequest(
                key=key,
                display_name=display_name,
                description=description,
                connection=utils.get_pydantic_model(
                    connection, Optional[models.McpConnection]
                ),
                auth=utils.get_pydantic_model(auth, Optional[models.McpAuthConfig]),
                default_tool_exposure=utils.get_pydantic_model(
                    default_tool_exposure, Optional[models.McpToolExposure]
                ),
                sharing=utils.get_pydantic_model(sharing, Optional[models.Sharing]),
            ),
        )

        req = self._build_request(
            method="PATCH",
            path="/v2/mcp-servers/{id}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.update_mcp_server_request,
                False,
                False,
                "json",
                models.UpdateMcpServerRequest,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpServerUpdate",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpServers",
                    "x-cli-name": "update",
                    "x-code-samples": [
                        {
                            "label": "Core - Update an MCP server",
                            "lang": "curl",
                            "source": 'curl --request PATCH \\\n  --url \'https://api.orq.ai/v2/mcp-servers/01JQ0K5R8N2ZC7X4M9T3V6HWBD\' \\\n  --header \'Authorization: Bearer $ORQ_API_KEY\' \\\n  --header \'Content-Type: application/json\' \\\n  --data \'{\n    "display_name": "GitHub (read only)",\n    "default_tool_exposure": {\n      "mode": "MCP_TOOL_EXPOSURE_MODE_ALL",\n      "read_only": true\n    }\n  }\'\n',
                        },
                        {
                            "label": "Python - Update an MCP server",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\n# The key is immutable; sending a different one is rejected.\nresult = client.mcp_servers.update(\n    id="01JQ0K5R8N2ZC7X4M9T3V6HWBD",\n    display_name="GitHub (read only)",\n    default_tool_exposure={\n        "mode": "MCP_TOOL_EXPOSURE_MODE_ALL",\n        "read_only": True,\n    },\n)\n\nprint(result.mcp_server.display_name)\n',
                        },
                        {
                            "label": "Node.js - Update an MCP server",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\n// The key is immutable; sending a different one is rejected.\nconst result = await client.mcpServers.update({\n  id: '01JQ0K5R8N2ZC7X4M9T3V6HWBD',\n  displayName: 'GitHub (read only)',\n  defaultToolExposure: {\n    mode: 'MCP_TOOL_EXPOSURE_MODE_ALL',\n    readOnly: true,\n  },\n});\n\nconsole.log(result.mcpServer.displayName);\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.UpdateMcpServerResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    async def update_async(
        self,
        *,
        id: str,
        key: Optional[str] = None,
        display_name: Optional[str] = None,
        description: Optional[str] = None,
        connection: Optional[
            Union[models.McpConnection, models.McpConnectionTypedDict]
        ] = None,
        auth: Optional[
            Union[models.McpAuthConfig, models.McpAuthConfigTypedDict]
        ] = None,
        default_tool_exposure: Optional[
            Union[models.McpToolExposure, models.McpToolExposureTypedDict]
        ] = None,
        sharing: Optional[Union[models.Sharing, models.SharingTypedDict]] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.UpdateMcpServerResponse:
        r"""Update an MCP server

        Updates mutable fields of an existing MCP server. Omitted optional fields keep their current values.

        :param id: Unique identifier of the MCP server.
        :param key: Rejected with INVALID_ARGUMENT: the key prefixes this server's tools in
            every gateway that exposes it, so it is immutable after creation. Retained
            so callers get an error rather than a silently ignored field.
        :param display_name: Human readable name shown in the workspace.
        :param description: Free-form note about what this server is for.
        :param connection: How the gateway dials the upstream server.
        :param auth: Credentials the gateway sends upstream; send `type: NONE` explicitly for public servers.
        :param default_tool_exposure: Fallback exposure used by any gateway link that does not set its own.
        :param sharing: Which projects in the workspace may use this server. Defaults to every project.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.McpServerUpdateRequest(
            id=id,
            update_mcp_server_request=models.UpdateMcpServerRequest(
                key=key,
                display_name=display_name,
                description=description,
                connection=utils.get_pydantic_model(
                    connection, Optional[models.McpConnection]
                ),
                auth=utils.get_pydantic_model(auth, Optional[models.McpAuthConfig]),
                default_tool_exposure=utils.get_pydantic_model(
                    default_tool_exposure, Optional[models.McpToolExposure]
                ),
                sharing=utils.get_pydantic_model(sharing, Optional[models.Sharing]),
            ),
        )

        req = self._build_request_async(
            method="PATCH",
            path="/v2/mcp-servers/{id}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.update_mcp_server_request,
                False,
                False,
                "json",
                models.UpdateMcpServerRequest,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpServerUpdate",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpServers",
                    "x-cli-name": "update",
                    "x-code-samples": [
                        {
                            "label": "Core - Update an MCP server",
                            "lang": "curl",
                            "source": 'curl --request PATCH \\\n  --url \'https://api.orq.ai/v2/mcp-servers/01JQ0K5R8N2ZC7X4M9T3V6HWBD\' \\\n  --header \'Authorization: Bearer $ORQ_API_KEY\' \\\n  --header \'Content-Type: application/json\' \\\n  --data \'{\n    "display_name": "GitHub (read only)",\n    "default_tool_exposure": {\n      "mode": "MCP_TOOL_EXPOSURE_MODE_ALL",\n      "read_only": true\n    }\n  }\'\n',
                        },
                        {
                            "label": "Python - Update an MCP server",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\n# The key is immutable; sending a different one is rejected.\nresult = client.mcp_servers.update(\n    id="01JQ0K5R8N2ZC7X4M9T3V6HWBD",\n    display_name="GitHub (read only)",\n    default_tool_exposure={\n        "mode": "MCP_TOOL_EXPOSURE_MODE_ALL",\n        "read_only": True,\n    },\n)\n\nprint(result.mcp_server.display_name)\n',
                        },
                        {
                            "label": "Node.js - Update an MCP server",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\n// The key is immutable; sending a different one is rejected.\nconst result = await client.mcpServers.update({\n  id: '01JQ0K5R8N2ZC7X4M9T3V6HWBD',\n  displayName: 'GitHub (read only)',\n  defaultToolExposure: {\n    mode: 'MCP_TOOL_EXPOSURE_MODE_ALL',\n    readOnly: true,\n  },\n});\n\nconsole.log(result.mcpServer.displayName);\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.UpdateMcpServerResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    def test_tool(
        self,
        *,
        id: str,
        tool_name: str,
        arguments: Optional[Union[models.Arguments, models.ArgumentsTypedDict]] = None,
        discovery_variables: Optional[Mapping[str, str]] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.TestMcpServerToolResponse:
        r"""Test an MCP server tool

        Executes a single tool on an upstream MCP server for testing. Connects to the server, invokes the tool with the provided arguments, and returns the result.

        :param id: Unique identifier of the MCP server.
        :param tool_name: Bare upstream tool name, not a gateway's namespaced `exposed_name`.
        :param arguments: Arguments passed to the tool, matching its `input_schema`.
        :param discovery_variables: Values for the server's `template_variables`; treated as sensitive and not persisted.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.McpServerTestToolRequest(
            id=id,
            test_mcp_server_tool_request=models.TestMcpServerToolRequest(
                tool_name=tool_name,
                arguments=utils.get_pydantic_model(
                    arguments, Optional[models.Arguments]
                ),
                discovery_variables=utils.unmarshal(
                    discovery_variables, Optional[Dict[str, str]]
                ),
            ),
        )

        req = self._build_request(
            method="POST",
            path="/v2/mcp-servers/{id}/tools:test",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.test_mcp_server_tool_request,
                False,
                False,
                "json",
                models.TestMcpServerToolRequest,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpServerTestTool",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpServers",
                    "x-cli-name": "testTool",
                    "x-code-samples": [
                        {
                            "label": "Core - Test a single tool",
                            "lang": "curl",
                            "source": 'curl --request POST \\\n  --url \'https://api.orq.ai/v2/mcp-servers/01JQ0K5R8N2ZC7X4M9T3V6HWBD/tools:test\' \\\n  --header \'Authorization: Bearer $ORQ_API_KEY\' \\\n  --header \'Content-Type: application/json\' \\\n  --data \'{\n    "tool_name": "search_issues",\n    "arguments": { "query": "repo:orq-ai/orquesta-web is:open label:bug" }\n  }\'\n',
                        },
                        {
                            "label": "Python - Test a single tool",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\n# tool_name is the upstream name, not a gateway\'s namespaced exposed_name.\nresult = client.mcp_servers.test_tool(\n    id="01JQ0K5R8N2ZC7X4M9T3V6HWBD",\n    tool_name="search_issues",\n    arguments={"query": "repo:orq-ai/orquesta-web is:open label:bug"},\n)\n\nprint(result.result.ok, result.result.latency_ms)\n',
                        },
                        {
                            "label": "Node.js - Test a single tool",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\n// toolName is the upstream name, not a gateway's namespaced exposedName.\nconst { result } = await client.mcpServers.testTool({\n  id: '01JQ0K5R8N2ZC7X4M9T3V6HWBD',\n  toolName: 'search_issues',\n  arguments: { query: 'repo:orq-ai/orquesta-web is:open label:bug' },\n});\n\nconsole.log(result.ok, result.latencyMs);\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.TestMcpServerToolResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    async def test_tool_async(
        self,
        *,
        id: str,
        tool_name: str,
        arguments: Optional[Union[models.Arguments, models.ArgumentsTypedDict]] = None,
        discovery_variables: Optional[Mapping[str, str]] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.TestMcpServerToolResponse:
        r"""Test an MCP server tool

        Executes a single tool on an upstream MCP server for testing. Connects to the server, invokes the tool with the provided arguments, and returns the result.

        :param id: Unique identifier of the MCP server.
        :param tool_name: Bare upstream tool name, not a gateway's namespaced `exposed_name`.
        :param arguments: Arguments passed to the tool, matching its `input_schema`.
        :param discovery_variables: Values for the server's `template_variables`; treated as sensitive and not persisted.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.McpServerTestToolRequest(
            id=id,
            test_mcp_server_tool_request=models.TestMcpServerToolRequest(
                tool_name=tool_name,
                arguments=utils.get_pydantic_model(
                    arguments, Optional[models.Arguments]
                ),
                discovery_variables=utils.unmarshal(
                    discovery_variables, Optional[Dict[str, str]]
                ),
            ),
        )

        req = self._build_request_async(
            method="POST",
            path="/v2/mcp-servers/{id}/tools:test",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.test_mcp_server_tool_request,
                False,
                False,
                "json",
                models.TestMcpServerToolRequest,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpServerTestTool",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpServers",
                    "x-cli-name": "testTool",
                    "x-code-samples": [
                        {
                            "label": "Core - Test a single tool",
                            "lang": "curl",
                            "source": 'curl --request POST \\\n  --url \'https://api.orq.ai/v2/mcp-servers/01JQ0K5R8N2ZC7X4M9T3V6HWBD/tools:test\' \\\n  --header \'Authorization: Bearer $ORQ_API_KEY\' \\\n  --header \'Content-Type: application/json\' \\\n  --data \'{\n    "tool_name": "search_issues",\n    "arguments": { "query": "repo:orq-ai/orquesta-web is:open label:bug" }\n  }\'\n',
                        },
                        {
                            "label": "Python - Test a single tool",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\n# tool_name is the upstream name, not a gateway\'s namespaced exposed_name.\nresult = client.mcp_servers.test_tool(\n    id="01JQ0K5R8N2ZC7X4M9T3V6HWBD",\n    tool_name="search_issues",\n    arguments={"query": "repo:orq-ai/orquesta-web is:open label:bug"},\n)\n\nprint(result.result.ok, result.result.latency_ms)\n',
                        },
                        {
                            "label": "Node.js - Test a single tool",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\n// toolName is the upstream name, not a gateway's namespaced exposedName.\nconst { result } = await client.mcpServers.testTool({\n  id: '01JQ0K5R8N2ZC7X4M9T3V6HWBD',\n  toolName: 'search_issues',\n  arguments: { query: 'repo:orq-ai/orquesta-web is:open label:bug' },\n});\n\nconsole.log(result.ok, result.latencyMs);\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.TestMcpServerToolResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    def sync(
        self,
        *,
        id: str,
        discovery_variables: Optional[Mapping[str, str]] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.SyncMcpServerResponse:
        r"""Sync an MCP server

        Connects to an upstream MCP server, discovers tools, and persists the tool list and sync state on the server record.

        :param id: Unique identifier of the MCP server.
        :param discovery_variables: Values for the server's `template_variables`; treated as sensitive and not persisted.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.McpServerSyncRequest(
            id=id,
            sync_mcp_server_request=models.SyncMcpServerRequest(
                discovery_variables=utils.unmarshal(
                    discovery_variables, Optional[Dict[str, str]]
                ),
            ),
        )

        req = self._build_request(
            method="POST",
            path="/v2/mcp-servers/{id}:sync",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.sync_mcp_server_request,
                False,
                False,
                "json",
                models.SyncMcpServerRequest,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpServerSync",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpServers",
                    "x-cli-name": "sync",
                    "x-code-samples": [
                        {
                            "label": "Core - Sync the tool catalog",
                            "lang": "curl",
                            "source": "curl --request POST \\\n  --url 'https://api.orq.ai/v2/mcp-servers/01JQ0K5R8N2ZC7X4M9T3V6HWBD:sync' \\\n  --header 'Authorization: Bearer $ORQ_API_KEY' \\\n  --header 'Content-Type: application/json' \\\n  --data '{}'\n",
                        },
                        {
                            "label": "Core - Sync a server that uses template variables",
                            "lang": "curl",
                            "source": "curl --request POST \\\n  --url 'https://api.orq.ai/v2/mcp-servers/01JQ0K5R8N2ZC7X4M9T3V6HWBD:sync' \\\n  --header 'Authorization: Bearer $ORQ_API_KEY' \\\n  --header 'Content-Type: application/json' \\\n  --data '{\n    \"discovery_variables\": { \"github_token\": \"ghp_example_token\" }\n  }'\n",
                        },
                        {
                            "label": "Python - Sync the tool catalog",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\nresult = client.mcp_servers.sync(id="01JQ0K5R8N2ZC7X4M9T3V6HWBD")\n\nstate = result.mcp_server.sync_state\n# A sync that discovers nothing still reports SYNCED with tools_total 0.\nprint(state.status, state.tools_added, state.tools_removed)\n',
                        },
                        {
                            "label": "Node.js - Sync the tool catalog",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\nconst result = await client.mcpServers.sync({\n  id: '01JQ0K5R8N2ZC7X4M9T3V6HWBD',\n});\n\nconst state = result.mcpServer.syncState;\n// A sync that discovers nothing still reports SYNCED with toolsTotal 0.\nconsole.log(state.status, state.toolsAdded, state.toolsRemoved);\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.SyncMcpServerResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    async def sync_async(
        self,
        *,
        id: str,
        discovery_variables: Optional[Mapping[str, str]] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.SyncMcpServerResponse:
        r"""Sync an MCP server

        Connects to an upstream MCP server, discovers tools, and persists the tool list and sync state on the server record.

        :param id: Unique identifier of the MCP server.
        :param discovery_variables: Values for the server's `template_variables`; treated as sensitive and not persisted.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.McpServerSyncRequest(
            id=id,
            sync_mcp_server_request=models.SyncMcpServerRequest(
                discovery_variables=utils.unmarshal(
                    discovery_variables, Optional[Dict[str, str]]
                ),
            ),
        )

        req = self._build_request_async(
            method="POST",
            path="/v2/mcp-servers/{id}:sync",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.sync_mcp_server_request,
                False,
                False,
                "json",
                models.SyncMcpServerRequest,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpServerSync",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpServers",
                    "x-cli-name": "sync",
                    "x-code-samples": [
                        {
                            "label": "Core - Sync the tool catalog",
                            "lang": "curl",
                            "source": "curl --request POST \\\n  --url 'https://api.orq.ai/v2/mcp-servers/01JQ0K5R8N2ZC7X4M9T3V6HWBD:sync' \\\n  --header 'Authorization: Bearer $ORQ_API_KEY' \\\n  --header 'Content-Type: application/json' \\\n  --data '{}'\n",
                        },
                        {
                            "label": "Core - Sync a server that uses template variables",
                            "lang": "curl",
                            "source": "curl --request POST \\\n  --url 'https://api.orq.ai/v2/mcp-servers/01JQ0K5R8N2ZC7X4M9T3V6HWBD:sync' \\\n  --header 'Authorization: Bearer $ORQ_API_KEY' \\\n  --header 'Content-Type: application/json' \\\n  --data '{\n    \"discovery_variables\": { \"github_token\": \"ghp_example_token\" }\n  }'\n",
                        },
                        {
                            "label": "Python - Sync the tool catalog",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\nresult = client.mcp_servers.sync(id="01JQ0K5R8N2ZC7X4M9T3V6HWBD")\n\nstate = result.mcp_server.sync_state\n# A sync that discovers nothing still reports SYNCED with tools_total 0.\nprint(state.status, state.tools_added, state.tools_removed)\n',
                        },
                        {
                            "label": "Node.js - Sync the tool catalog",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\nconst result = await client.mcpServers.sync({\n  id: '01JQ0K5R8N2ZC7X4M9T3V6HWBD',\n});\n\nconst state = result.mcpServer.syncState;\n// A sync that discovers nothing still reports SYNCED with toolsTotal 0.\nconsole.log(state.status, state.toolsAdded, state.toolsRemoved);\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.SyncMcpServerResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)
