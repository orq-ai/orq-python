"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from .basesdk import BaseSDK
from datetime import datetime
from orq_ai_sdk import models, utils
from orq_ai_sdk._hooks import HookContext
from orq_ai_sdk.types import OptionalNullable, UNSET
from orq_ai_sdk.utils import get_security_from_env
from orq_ai_sdk.utils.unmarshal_json_response import unmarshal_json_response
from typing import List, Mapping, Optional, Union


class Reporting(BaseSDK):
    def query(
        self,
        *,
        metric: models.Metric,
        from_: datetime,
        to: datetime,
        grain: Optional[models.Grain] = None,
        group_by: Optional[List[models.GroupBy]] = None,
        filters: Optional[
            Union[List[models.Filter], List[models.FilterTypedDict]]
        ] = None,
        limit: Optional[int] = None,
        time_zone: Optional[str] = None,
        include_totals: Optional[bool] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.QueryReportResponse:
        r"""Query reporting metrics

        Returns time-series analytics for AI usage, cost, latency, evaluator results, and guardrail outcomes. Select a metric and time range, break results down by supported dimensions, apply filters, and optionally include totals for the full range.

        :param metric: Catalogue metric to query.
        :param from_: Inclusive lower bound for the report window (RFC 3339, UTC).
        :param to: Exclusive upper bound for the report window (RFC 3339, UTC).
        :param grain: Requested bucket grain. Use `auto` or omit the field to let the server choose based on the requested range.
        :param group_by: Reporting dimensions to break down by. Valid dimensions depend on the selected metric.
        :param filters: Up to 20 allowlisted predicates combined with AND.
        :param limit: Maximum bucket rows returned. Defaults to 1000 and is capped at
            5000.
        :param time_zone: IANA time zone applied to bucket boundaries, for example
            `America/New_York`. Response timestamps remain UTC. Empty means UTC.
        :param include_totals: When true, include a `totals` block aggregated across the full
            report window.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.QueryReportRequest(
            metric=metric,
            from_=from_,
            to=to,
            grain=grain,
            group_by=group_by,
            filters=utils.get_pydantic_model(filters, Optional[List[models.Filter]]),
            limit=limit,
            time_zone=time_zone,
            include_totals=include_totals,
        )

        req = self._build_request(
            method="POST",
            path="/v2/reporting",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.QueryReportRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="ReportingQuery",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.QueryReportResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)

    async def query_async(
        self,
        *,
        metric: models.Metric,
        from_: datetime,
        to: datetime,
        grain: Optional[models.Grain] = None,
        group_by: Optional[List[models.GroupBy]] = None,
        filters: Optional[
            Union[List[models.Filter], List[models.FilterTypedDict]]
        ] = None,
        limit: Optional[int] = None,
        time_zone: Optional[str] = None,
        include_totals: Optional[bool] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.QueryReportResponse:
        r"""Query reporting metrics

        Returns time-series analytics for AI usage, cost, latency, evaluator results, and guardrail outcomes. Select a metric and time range, break results down by supported dimensions, apply filters, and optionally include totals for the full range.

        :param metric: Catalogue metric to query.
        :param from_: Inclusive lower bound for the report window (RFC 3339, UTC).
        :param to: Exclusive upper bound for the report window (RFC 3339, UTC).
        :param grain: Requested bucket grain. Use `auto` or omit the field to let the server choose based on the requested range.
        :param group_by: Reporting dimensions to break down by. Valid dimensions depend on the selected metric.
        :param filters: Up to 20 allowlisted predicates combined with AND.
        :param limit: Maximum bucket rows returned. Defaults to 1000 and is capped at
            5000.
        :param time_zone: IANA time zone applied to bucket boundaries, for example
            `America/New_York`. Response timestamps remain UTC. Empty means UTC.
        :param include_totals: When true, include a `totals` block aggregated across the full
            report window.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.QueryReportRequest(
            metric=metric,
            from_=from_,
            to=to,
            grain=grain,
            group_by=group_by,
            filters=utils.get_pydantic_model(filters, Optional[List[models.Filter]]),
            limit=limit,
            time_zone=time_zone,
            include_totals=include_totals,
        )

        req = self._build_request_async(
            method="POST",
            path="/v2/reporting",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.QueryReportRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="ReportingQuery",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.QueryReportResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIError("API error occurred", http_res, http_res_text)

        raise models.APIError("Unexpected response received", http_res)
