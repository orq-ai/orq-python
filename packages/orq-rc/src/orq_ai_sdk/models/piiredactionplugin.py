"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Dict, List, Literal, Optional
from typing_extensions import NotRequired, TypedDict


ID = Literal["pii_redaction",]
r"""PII redaction plugin."""


OnFailure = Literal[
    "block",
    "passthrough",
]
r"""Behaviour when detection fails."""


class PIIRedactionPluginTypedDict(TypedDict):
    id: ID
    r"""PII redaction plugin."""
    language: NotRequired[str]
    r"""Detector language. Use \"auto\" to auto-detect; omitting the field falls back to en."""
    regions: NotRequired[List[str]]
    r"""Region codes selecting whole regions of coverage (e.g. \"nl\", \"gb\"). Every entity type those regions cover is redacted, alongside the base catalog. [\"all\"] is exclusive, and leaving both this and entities empty also runs every region, so selecting nothing is the widest request rather than the narrowest. Combines with entities: the two selections are unioned, so entities adds types on top of the region coverage."""
    entities: NotRequired[List[str]]
    r"""The entity types to redact. A named type is redacted even when it belongs to a region, so a region's types can be selected individually without naming the region. On its own this is a strict allowlist; alongside regions it adds to the region coverage. See GET /v2/pii/capabilities for valid types."""
    entity_thresholds: NotRequired[Dict[str, float]]
    r"""Per-entity confidence cutoff overrides in [0,1]. Tunes confidence only and never changes which types are redacted, so every key must also appear in entities."""
    threshold: NotRequired[float]
    r"""Baseline confidence cutoff applied to every entity type without a per-entity override."""
    on_failure: NotRequired[OnFailure]
    r"""Behaviour when detection fails."""


class PIIRedactionPlugin(BaseModel):
    id: ID
    r"""PII redaction plugin."""

    language: Optional[str] = None
    r"""Detector language. Use \"auto\" to auto-detect; omitting the field falls back to en."""

    regions: Optional[List[str]] = None
    r"""Region codes selecting whole regions of coverage (e.g. \"nl\", \"gb\"). Every entity type those regions cover is redacted, alongside the base catalog. [\"all\"] is exclusive, and leaving both this and entities empty also runs every region, so selecting nothing is the widest request rather than the narrowest. Combines with entities: the two selections are unioned, so entities adds types on top of the region coverage."""

    entities: Optional[List[str]] = None
    r"""The entity types to redact. A named type is redacted even when it belongs to a region, so a region's types can be selected individually without naming the region. On its own this is a strict allowlist; alongside regions it adds to the region coverage. See GET /v2/pii/capabilities for valid types."""

    entity_thresholds: Optional[Dict[str, float]] = None
    r"""Per-entity confidence cutoff overrides in [0,1]. Tunes confidence only and never changes which types are redacted, so every key must also appear in entities."""

    threshold: Optional[float] = None
    r"""Baseline confidence cutoff applied to every entity type without a per-entity override."""

    on_failure: Optional[OnFailure] = None
    r"""Behaviour when detection fails."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "language",
                "regions",
                "entities",
                "entity_thresholds",
                "threshold",
                "on_failure",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
