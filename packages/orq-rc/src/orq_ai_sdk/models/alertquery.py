"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .monitorfilter import MonitorFilter, MonitorFilterTypedDict
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class AlertQueryTypedDict(TypedDict):
    metric: str
    r"""Reporting API catalogue metric, e.g. `genai.cost` or
    `genai.latency.avg`. Validated against the catalogue when the alert
    is saved.
    """
    filters: NotRequired[List[MonitorFilterTypedDict]]
    r"""Reporting filters combined with AND."""


class AlertQuery(BaseModel):
    metric: str
    r"""Reporting API catalogue metric, e.g. `genai.cost` or
    `genai.latency.avg`. Validated against the catalogue when the alert
    is saved.
    """

    filters: Optional[List[MonitorFilter]] = None
    r"""Reporting filters combined with AND."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["filters"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
