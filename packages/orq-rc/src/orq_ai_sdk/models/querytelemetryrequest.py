"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .telemetrysource import TelemetrySource
from .tracecompute import TraceCompute, TraceComputeTypedDict
from .tracefilter import TraceFilter, TraceFilterTypedDict
from datetime import datetime
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import List, Literal, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


QueryTelemetryRequestMode = Literal[
    "timeseries",
    "scalar",
]
r"""Value shaping. `timeseries` buckets by grain; `scalar` returns one row per group. When omitted, grain selects the compatible shape."""


QueryTelemetryRequestSort = Literal[
    "desc",
    "asc",
]
r"""Ordering for scalar/top-list rows. Defaults to `desc`."""


class QueryTelemetryRequestTypedDict(TypedDict):
    source: NotRequired[TelemetrySource]
    from_: NotRequired[datetime]
    to: NotRequired[datetime]
    compute: NotRequired[List[TraceComputeTypedDict]]
    grain: NotRequired[str]
    r"""Empty string and \"none\" are equivalent: a single row per group instead
    of a time series.
    """
    group_by: NotRequired[List[str]]
    filters: NotRequired[List[TraceFilterTypedDict]]
    filter_operator: NotRequired[str]
    limit: NotRequired[int]
    r"""Scalar queries: maximum returned rows, default 100. Time-series queries: optional maximum number of distinct groups; exceeding it fails the query instead of truncating timelines. Time-series results are complete within safety budgets of 5000 rows, 5000 buckets, and 50000 metric values. Queries are limited to 90 days and 30 seconds of execution."""
    time_zone: NotRequired[str]
    include_totals: NotRequired[bool]
    mode: NotRequired[QueryTelemetryRequestMode]
    r"""Value shaping. `timeseries` buckets by grain; `scalar` returns one row per group. When omitted, grain selects the compatible shape."""
    sort: NotRequired[QueryTelemetryRequestSort]
    r"""Ordering for scalar/top-list rows. Defaults to `desc`."""
    interval_seconds: NotRequired[int]
    r"""Explicit bucket width in seconds. Takes precedence over `grain` when both are set."""
    selected_range_seconds: NotRequired[int]
    r"""The span originally selected, before live extended [from, to). `grain:\"auto\"` resolves bucket width from this span, independent of how far [from, to) has since grown."""
    project_id: NotRequired[str]
    r"""Pins the read to one project the caller can reach. Omit to keep the
    caller's token scope.
    """


class QueryTelemetryRequest(BaseModel):
    source: Optional[TelemetrySource] = None

    from_: Annotated[Optional[datetime], pydantic.Field(alias="from")] = None

    to: Optional[datetime] = None

    compute: Optional[List[TraceCompute]] = None

    grain: Optional[str] = None
    r"""Empty string and \"none\" are equivalent: a single row per group instead
    of a time series.
    """

    group_by: Optional[List[str]] = None

    filters: Optional[List[TraceFilter]] = None

    filter_operator: Optional[str] = None

    limit: Optional[int] = None
    r"""Scalar queries: maximum returned rows, default 100. Time-series queries: optional maximum number of distinct groups; exceeding it fails the query instead of truncating timelines. Time-series results are complete within safety budgets of 5000 rows, 5000 buckets, and 50000 metric values. Queries are limited to 90 days and 30 seconds of execution."""

    time_zone: Optional[str] = None

    include_totals: Optional[bool] = None

    mode: Optional[QueryTelemetryRequestMode] = None
    r"""Value shaping. `timeseries` buckets by grain; `scalar` returns one row per group. When omitted, grain selects the compatible shape."""

    sort: Optional[QueryTelemetryRequestSort] = None
    r"""Ordering for scalar/top-list rows. Defaults to `desc`."""

    interval_seconds: Optional[int] = None
    r"""Explicit bucket width in seconds. Takes precedence over `grain` when both are set."""

    selected_range_seconds: Optional[int] = None
    r"""The span originally selected, before live extended [from, to). `grain:\"auto\"` resolves bucket width from this span, independent of how far [from, to) has since grown."""

    project_id: Optional[str] = None
    r"""Pins the read to one project the caller can reach. Omit to keep the
    caller's token scope.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "source",
                "from",
                "to",
                "compute",
                "grain",
                "group_by",
                "filters",
                "filter_operator",
                "limit",
                "time_zone",
                "include_totals",
                "mode",
                "sort",
                "interval_seconds",
                "selected_range_seconds",
                "project_id",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    QueryTelemetryRequest.model_rebuild()
except NameError:
    pass
