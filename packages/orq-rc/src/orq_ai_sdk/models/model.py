"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .modelartificialanalysis import (
    ModelArtificialAnalysis,
    ModelArtificialAnalysisTypedDict,
)
from .modelmodalities import ModelModalities, ModelModalitiesTypedDict
from .modelparameter import ModelParameter, ModelParameterTypedDict
from .modelprovider import ModelProvider, ModelProviderTypedDict
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class ModelPricingTypedDict(TypedDict):
    r"""Resolved commercial pricing, including service-tier variants. Each
    variant carries a `when` CEL condition string evaluated against
    `input_tokens`, `output_tokens`, `total_tokens` and `tier`; all
    variants whose condition is true are merged onto the base pricing in
    order, later variants winning per field. Absent when this offering has
    no representable token-based pricing.
    """


class ModelPricing(BaseModel):
    r"""Resolved commercial pricing, including service-tier variants. Each
    variant carries a `when` CEL condition string evaluated against
    `input_tokens`, `output_tokens`, `total_tokens` and `tier`; all
    variants whose condition is true are merged onto the base pricing in
    order, later variants winning per field. Absent when this offering has
    no representable token-based pricing.
    """


class ModelTypedDict(TypedDict):
    id: str
    r"""Catalog identifier, `<provider>/<model>` (for example `openai/gpt-4o`)."""
    created: str
    r"""When orq listed this offering, Unix seconds (string-encoded int64 in
    JSON).
    """
    name: str
    r"""Display name shown in the orq model garden."""
    description: str
    r"""Short model description."""
    provider: ModelProviderTypedDict
    r"""Provider that serves this offering."""
    endpoints: List[str]
    r"""API endpoints this offering exposes (chat, batch, ...)."""
    modalities: ModelModalitiesTypedDict
    r"""Content modalities this offering consumes and produces."""
    offering_of: str
    r"""Provider-independent model reference this offering serves, `<developer>/<stem>`."""
    supported_parameters: List[ModelParameterTypedDict]
    r"""Playground and API parameters this offering exposes."""
    supported_tiers: List[str]
    r"""Paid service tier names available to this offering."""
    location: List[str]
    r"""Regions this offering routes to, lowercased."""
    status: str
    r"""Catalog lifecycle: active, preview or deprecated."""
    type: str
    r"""Task type: chat, completion, embedding, image, ..."""
    knowledge_cutoff: str
    r"""Training-data cutoff (YYYY-MM). Empty when unset."""
    release_date: str
    r"""Public release date (YYYY-MM-DD). Empty when unset."""
    deprecation: NotRequired[str]
    r"""When this offering stops being served, Unix seconds (string-encoded
    int64 in JSON): orq's own sunset date if one is set, otherwise the
    developer's announced deprecation date. Absent when neither is set.
    """
    pricing: NotRequired[ModelPricingTypedDict]
    r"""Resolved commercial pricing, including service-tier variants. Each
    variant carries a `when` CEL condition string evaluated against
    `input_tokens`, `output_tokens`, `total_tokens` and `tier`; all
    variants whose condition is true are merged onto the base pricing in
    order, later variants winning per field. Absent when this offering has
    no representable token-based pricing.
    """
    context_window: NotRequired[str]
    r"""Maximum input context size in tokens (string-encoded int64 in JSON).
    Absent when not applicable.
    """
    max_output_tokens: NotRequired[str]
    r"""Maximum output size in tokens (string-encoded int64 in JSON). Absent
    when not applicable.
    """
    artificial_intelligence: NotRequired[ModelArtificialAnalysisTypedDict]
    r"""Artificial Analysis benchmark data. Absent when unavailable for this
    offering.
    """


class Model(BaseModel):
    id: str
    r"""Catalog identifier, `<provider>/<model>` (for example `openai/gpt-4o`)."""

    created: str
    r"""When orq listed this offering, Unix seconds (string-encoded int64 in
    JSON).
    """

    name: str
    r"""Display name shown in the orq model garden."""

    description: str
    r"""Short model description."""

    provider: ModelProvider
    r"""Provider that serves this offering."""

    endpoints: List[str]
    r"""API endpoints this offering exposes (chat, batch, ...)."""

    modalities: ModelModalities
    r"""Content modalities this offering consumes and produces."""

    offering_of: str
    r"""Provider-independent model reference this offering serves, `<developer>/<stem>`."""

    supported_parameters: List[ModelParameter]
    r"""Playground and API parameters this offering exposes."""

    supported_tiers: List[str]
    r"""Paid service tier names available to this offering."""

    location: List[str]
    r"""Regions this offering routes to, lowercased."""

    status: str
    r"""Catalog lifecycle: active, preview or deprecated."""

    type: str
    r"""Task type: chat, completion, embedding, image, ..."""

    knowledge_cutoff: str
    r"""Training-data cutoff (YYYY-MM). Empty when unset."""

    release_date: str
    r"""Public release date (YYYY-MM-DD). Empty when unset."""

    deprecation: Optional[str] = None
    r"""When this offering stops being served, Unix seconds (string-encoded
    int64 in JSON): orq's own sunset date if one is set, otherwise the
    developer's announced deprecation date. Absent when neither is set.
    """

    pricing: Optional[ModelPricing] = None
    r"""Resolved commercial pricing, including service-tier variants. Each
    variant carries a `when` CEL condition string evaluated against
    `input_tokens`, `output_tokens`, `total_tokens` and `tier`; all
    variants whose condition is true are merged onto the base pricing in
    order, later variants winning per field. Absent when this offering has
    no representable token-based pricing.
    """

    context_window: Optional[str] = None
    r"""Maximum input context size in tokens (string-encoded int64 in JSON).
    Absent when not applicable.
    """

    max_output_tokens: Optional[str] = None
    r"""Maximum output size in tokens (string-encoded int64 in JSON). Absent
    when not applicable.
    """

    artificial_intelligence: Optional[ModelArtificialAnalysis] = None
    r"""Artificial Analysis benchmark data. Absent when unavailable for this
    offering.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "deprecation",
                "pricing",
                "context_window",
                "max_output_tokens",
                "artificial_intelligence",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
