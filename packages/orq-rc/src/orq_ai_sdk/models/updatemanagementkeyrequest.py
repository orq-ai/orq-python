"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .managementkeystatus import ManagementKeyStatus
from .managementpermissionmode import ManagementPermissionMode
from datetime import datetime
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Dict, Optional
from typing_extensions import NotRequired, TypedDict


class UpdateManagementKeyRequestTypedDict(TypedDict):
    name: NotRequired[str]
    r"""New name. Omit to keep current."""
    status: NotRequired[ManagementKeyStatus]
    permission_mode: NotRequired[ManagementPermissionMode]
    access: NotRequired[Dict[str, int]]
    r"""Replacement access map. Required when changing to
    `MANAGEMENT_PERMISSION_MODE_RESTRICTED`; ignored otherwise. Provide
    an empty map to clear.
    """
    expires_at: NotRequired[datetime]
    r"""New expiration. Omit to keep current. Set `clear_expires_at = true`
    to remove an existing expiration.
    """
    clear_expires_at: NotRequired[bool]
    r"""Force-clear the expiration. Mutually exclusive with `expires_at`."""


class UpdateManagementKeyRequest(BaseModel):
    name: Optional[str] = None
    r"""New name. Omit to keep current."""

    status: Optional[ManagementKeyStatus] = None

    permission_mode: Optional[ManagementPermissionMode] = None

    access: Optional[Dict[str, int]] = None
    r"""Replacement access map. Required when changing to
    `MANAGEMENT_PERMISSION_MODE_RESTRICTED`; ignored otherwise. Provide
    an empty map to clear.
    """

    expires_at: Optional[datetime] = None
    r"""New expiration. Omit to keep current. Set `clear_expires_at = true`
    to remove an existing expiration.
    """

    clear_expires_at: Optional[bool] = None
    r"""Force-clear the expiration. Mutually exclusive with `expires_at`."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "name",
                "status",
                "permission_mode",
                "access",
                "expires_at",
                "clear_expires_at",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
