"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import List, Literal, Optional
from typing_extensions import NotRequired, TypedDict


ID = Literal["pii_redaction",]
r"""Plugin discriminator. Must be `pii_redaction`."""


OnFailure = Literal[
    "block",
    "passthrough",
]
r"""Behavior when redaction is unavailable. `block` (default) fails the request; `passthrough` sends the original text."""


Entities = Literal[
    "AGE",
    "API_KEY",
    "BANK_ROUTING",
    "BIC",
    "BIOMETRIC_ID",
    "BLOOD_TYPE",
    "BSN",
    "CREDIT_CARD",
    "CRYPTO",
    "DATE_TIME",
    "DEVICE_ID",
    "EDUCATION_LEVEL",
    "EMAIL",
    "EMAIL_ADDRESS",
    "EMPLOYMENT_STATUS",
    "GENDER",
    "HEALTH_PLAN_ID",
    "HTTP_COOKIE",
    "IBAN_CODE",
    "ID",
    "ID_CARD",
    "IMEI",
    "IP_ADDRESS",
    "JOBTITLE",
    "JWT",
    "KVK_NUMBER",
    "LANGUAGE",
    "LICENSE_NUMBER",
    "LICENSE_PLATE",
    "LOCATION",
    "MAC_ADDRESS",
    "MEDICAL_LICENSE",
    "MEDICAL_RECORD",
    "NRP",
    "ORGANIZATION",
    "PASSPORT",
    "PASSWORD",
    "PERSON",
    "PHONE_NUMBER",
    "PIN",
    "POLITICAL_VIEW",
    "RACE_ETHNICITY",
    "RELIGIOUS_BELIEF",
    "SEXUALITY",
    "TAX_ID",
    "TITLE",
    "UK_NHS",
    "URL",
    "US_BANK_NUMBER",
    "US_DRIVER_LICENSE",
    "US_ITIN",
    "US_PASSPORT",
    "US_SSN",
    "UUID",
    "VEHICLE_ID",
]


class PIIRedactionPluginAutoTypedDict(TypedDict):
    id: ID
    r"""Plugin discriminator. Must be `pii_redaction`."""
    on_failure: NotRequired[OnFailure]
    r"""Behavior when redaction is unavailable. `block` (default) fails the request; `passthrough` sends the original text."""
    threshold: NotRequired[float]
    r"""Detector confidence cutoff in [0,1]."""
    entities: NotRequired[List[Entities]]
    r"""Entity types to redact when no detector language is configured. Omit to redact every detected type."""


class PIIRedactionPluginAuto(BaseModel):
    id: ID
    r"""Plugin discriminator. Must be `pii_redaction`."""

    on_failure: Optional[OnFailure] = None
    r"""Behavior when redaction is unavailable. `block` (default) fails the request; `passthrough` sends the original text."""

    threshold: Optional[float] = None
    r"""Detector confidence cutoff in [0,1]."""

    entities: Optional[List[Entities]] = None
    r"""Entity types to redact when no detector language is configured. Omit to redact every detected type."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["on_failure", "threshold", "entities"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
