"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from orq_ai_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
import pydantic
from pydantic import model_serializer
from typing import Any, Dict, List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class ContextTypedDict(TypedDict):
    trace_id: str
    span_id: str


class Context(BaseModel):
    trace_id: str

    span_id: str


class PublicSpanTypedDict(TypedDict):
    r"""A resolved span as returned by the public API. Only the identifying fields are guaranteed to be present; the attribute set depends on the span type and the instrumentation that produced it."""

    id: str
    r"""Unique identifier of the span"""
    trace_id: str
    r"""Unique identifier of the trace the span belongs to"""
    type: str
    r"""Type of the span, for example `span.chat_completion` or `trace`. New span types are added over time, so treat this as an open set of values."""
    span_id: NotRequired[str]
    parent_id: NotRequired[Nullable[str]]
    r"""Identifier of the parent span, if any"""
    session_id: NotRequired[Nullable[str]]
    r"""Identifier relating multiple traces together"""
    name: NotRequired[str]
    start_time: NotRequired[str]
    r"""ISO timestamp indicating when the span started"""
    end_time: NotRequired[str]
    r"""ISO timestamp indicating when the span ended"""
    duration: NotRequired[float]
    r"""Time elapsed between the span start and end timestamp"""
    total_tokens: NotRequired[float]
    total_cost: NotRequired[float]
    billable: NotRequired[bool]
    context: NotRequired[ContextTypedDict]
    events: NotRequired[List[Dict[str, Any]]]
    input: NotRequired[Any]
    output: NotRequired[Any]
    attributes: NotRequired[Dict[str, Any]]
    r"""OpenTelemetry and orq.ai attributes of the span, keyed by attribute name."""


class PublicSpan(BaseModel):
    r"""A resolved span as returned by the public API. Only the identifying fields are guaranteed to be present; the attribute set depends on the span type and the instrumentation that produced it."""

    id: Annotated[str, pydantic.Field(alias="_id")]
    r"""Unique identifier of the span"""

    trace_id: str
    r"""Unique identifier of the trace the span belongs to"""

    type: str
    r"""Type of the span, for example `span.chat_completion` or `trace`. New span types are added over time, so treat this as an open set of values."""

    span_id: Optional[str] = None

    parent_id: OptionalNullable[str] = UNSET
    r"""Identifier of the parent span, if any"""

    session_id: OptionalNullable[str] = UNSET
    r"""Identifier relating multiple traces together"""

    name: Optional[str] = None

    start_time: Optional[str] = None
    r"""ISO timestamp indicating when the span started"""

    end_time: Optional[str] = None
    r"""ISO timestamp indicating when the span ended"""

    duration: Optional[float] = None
    r"""Time elapsed between the span start and end timestamp"""

    total_tokens: Optional[float] = None

    total_cost: Optional[float] = None

    billable: Optional[bool] = None

    context: Optional[Context] = None

    events: Optional[List[Dict[str, Any]]] = None

    input: Optional[Any] = None

    output: Optional[Any] = None

    attributes: Optional[Dict[str, Any]] = None
    r"""OpenTelemetry and orq.ai attributes of the span, keyed by attribute name."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "span_id",
                "parent_id",
                "session_id",
                "name",
                "start_time",
                "end_time",
                "duration",
                "total_tokens",
                "total_cost",
                "billable",
                "context",
                "events",
                "input",
                "output",
                "attributes",
            ]
        )
        nullable_fields = set(["parent_id", "session_id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


try:
    PublicSpan.model_rebuild()
except NameError:
    pass
