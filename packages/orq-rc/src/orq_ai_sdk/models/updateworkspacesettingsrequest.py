"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .piiredaction import PiiRedaction, PiiRedactionTypedDict
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class UpdateWorkspaceSettingsRequestTypedDict(TypedDict):
    r"""UpdateWorkspaceSettingsRequest is a partial update: every field is optional
    and an omitted field is left unchanged.

    There is deliberately no `key` / `slug` field. The workspace slug is
    immutable through this service (it is embedded in resource URLs and API
    credentials), mirroring the dashboard's WorkspaceUpdateGuard, which rejects a
    `key` in the PATCH /workspaces/:key body. Do not add one.
    """

    display_name: NotRequired[str]
    r"""New workspace display name. Omit to keep the current name."""
    enforce_enabled_models: NotRequired[bool]
    r"""New value for the enforce-enabled-models flag. Omit to keep the current
    value.
    """
    pii_redaction: NotRequired[PiiRedactionTypedDict]
    r"""Workspace-default PII redaction update. Omit to leave it unchanged. When
    present, `enabled` is always applied; `config` omitted keeps the stored
    config, `config` present replaces it wholesale, and `config: {}` clears it.
    """


class UpdateWorkspaceSettingsRequest(BaseModel):
    r"""UpdateWorkspaceSettingsRequest is a partial update: every field is optional
    and an omitted field is left unchanged.

    There is deliberately no `key` / `slug` field. The workspace slug is
    immutable through this service (it is embedded in resource URLs and API
    credentials), mirroring the dashboard's WorkspaceUpdateGuard, which rejects a
    `key` in the PATCH /workspaces/:key body. Do not add one.
    """

    display_name: Optional[str] = None
    r"""New workspace display name. Omit to keep the current name."""

    enforce_enabled_models: Optional[bool] = None
    r"""New value for the enforce-enabled-models flag. Omit to keep the current
    value.
    """

    pii_redaction: Optional[PiiRedaction] = None
    r"""Workspace-default PII redaction update. Omit to leave it unchanged. When
    present, `enabled` is always applied; `config` omitted keeps the stored
    config, `config` present replaces it wholesale, and `config: {}` clears it.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["display_name", "enforce_enabled_models", "pii_redaction"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
