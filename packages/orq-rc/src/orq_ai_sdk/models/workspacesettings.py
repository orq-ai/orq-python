"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .piiredaction import PiiRedaction, PiiRedactionTypedDict
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class WorkspaceSettingsTypedDict(TypedDict):
    r"""WorkspaceSettings is the full settings state returned by Get and Update."""

    key: str
    r"""Read-only workspace key/slug. Returned for reference; it cannot be changed
    through this service.
    """
    display_name: str
    r"""Human-readable workspace display name."""
    enforce_enabled_models: bool
    r"""Whether only workspace-enabled models may be served."""
    pii_redaction: NotRequired[PiiRedactionTypedDict]
    r"""Workspace-default PII redaction plugin configuration. Omitted when the
    workspace has never configured it.
    """


class WorkspaceSettings(BaseModel):
    r"""WorkspaceSettings is the full settings state returned by Get and Update."""

    key: str
    r"""Read-only workspace key/slug. Returned for reference; it cannot be changed
    through this service.
    """

    display_name: str
    r"""Human-readable workspace display name."""

    enforce_enabled_models: bool
    r"""Whether only workspace-enabled models may be served."""

    pii_redaction: Optional[PiiRedaction] = None
    r"""Workspace-default PII redaction plugin configuration. Omitted when the
    workspace has never configured it.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["pii_redaction"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
