"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .sharing import Sharing, SharingTypedDict
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Literal, Optional
from typing_extensions import NotRequired, TypedDict


ExternalAccess = Literal[
    "disabled",
    "read_only",
    "read_write",
]
r"""Whether external MCP clients may reach this file system. Defaults to disabled."""


class CreateFileSystemRequestTypedDict(TypedDict):
    key: str
    r"""The unique key of the file system. The key is unique and immutable and cannot be repeated within the same workspace."""
    path: str
    r"""Entity storage path. With workspace-level API keys, the first element identifies the project. With project-level API keys, the path is relative to that project."""
    display_name: NotRequired[str]
    r"""Human readable name shown in the UI. Defaults to the key."""
    description: NotRequired[str]
    external_access: NotRequired[ExternalAccess]
    r"""Whether external MCP clients may reach this file system. Defaults to disabled."""
    sharing: NotRequired[SharingTypedDict]
    r"""Sharing controls which projects in the workspace may use this entity and
    what they may do with it.
    """


class CreateFileSystemRequest(BaseModel):
    key: str
    r"""The unique key of the file system. The key is unique and immutable and cannot be repeated within the same workspace."""

    path: str
    r"""Entity storage path. With workspace-level API keys, the first element identifies the project. With project-level API keys, the path is relative to that project."""

    display_name: Optional[str] = None
    r"""Human readable name shown in the UI. Defaults to the key."""

    description: Optional[str] = None

    external_access: Optional[ExternalAccess] = None
    r"""Whether external MCP clients may reach this file system. Defaults to disabled."""

    sharing: Optional[Sharing] = None
    r"""Sharing controls which projects in the workspace may use this entity and
    what they may do with it.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["display_name", "description", "external_access", "sharing"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
