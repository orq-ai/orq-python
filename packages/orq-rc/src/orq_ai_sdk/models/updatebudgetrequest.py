"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .budgetalert import BudgetAlert, BudgetAlertTypedDict
from .budgetlimits import BudgetLimits, BudgetLimitsTypedDict
from .budgetmatch import BudgetMatch, BudgetMatchTypedDict
from .ratelimit import RateLimit, RateLimitTypedDict
from datetime import datetime
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class UpdateBudgetRequestTypedDict(TypedDict):
    limits: NotRequired[BudgetLimitsTypedDict]
    r"""New limits. Omit to keep current."""
    rate_limit: NotRequired[RateLimitTypedDict]
    r"""New rate limit. Omit to keep current."""
    is_active: NotRequired[bool]
    r"""New active state. Omit to keep current."""
    expires_at: NotRequired[datetime]
    r"""New expiration. Omit to keep current. Set `clear_expires_at = true`
    to remove an existing expiration.
    """
    clear_expires_at: NotRequired[bool]
    r"""Force-clear the expiration. Mutually exclusive with `expires_at`."""
    match: NotRequired[BudgetMatchTypedDict]
    r"""New matching expression. Only valid for dynamic budgets (no
    structured scope) — the scope of a scoped budget is immutable, so
    its derived expression is too. Validated via CEL parse.
    """
    alerts: NotRequired[List[BudgetAlertTypedDict]]
    r"""Replaces the alert list wholesale. Omit to keep the current alerts.
    An entry with a known `id` is edited in place; one with no id is minted.
    """
    clear_alerts: NotRequired[bool]
    r"""Force-clear every alert. Mutually exclusive with a non-empty `alerts`."""


class UpdateBudgetRequest(BaseModel):
    limits: Optional[BudgetLimits] = None
    r"""New limits. Omit to keep current."""

    rate_limit: Optional[RateLimit] = None
    r"""New rate limit. Omit to keep current."""

    is_active: Optional[bool] = None
    r"""New active state. Omit to keep current."""

    expires_at: Optional[datetime] = None
    r"""New expiration. Omit to keep current. Set `clear_expires_at = true`
    to remove an existing expiration.
    """

    clear_expires_at: Optional[bool] = None
    r"""Force-clear the expiration. Mutually exclusive with `expires_at`."""

    match: Optional[BudgetMatch] = None
    r"""New matching expression. Only valid for dynamic budgets (no
    structured scope) — the scope of a scoped budget is immutable, so
    its derived expression is too. Validated via CEL parse.
    """

    alerts: Optional[List[BudgetAlert]] = None
    r"""Replaces the alert list wholesale. Omit to keep the current alerts.
    An entry with a known `id` is edited in place; one with no id is minted.
    """

    clear_alerts: Optional[bool] = None
    r"""Force-clear every alert. Mutually exclusive with a non-empty `alerts`."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "limits",
                "rate_limit",
                "is_active",
                "expires_at",
                "clear_expires_at",
                "match",
                "alerts",
                "clear_alerts",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
