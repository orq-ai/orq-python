"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .facetvalue import FacetValue, FacetValueTypedDict
from datetime import datetime
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class LogPatternTypedDict(TypedDict):
    id: str
    r"""Stable hash of the normalized template."""
    template: str
    count: str
    r"""Number of sampled records assigned to this pattern. This is exact when
    meta.truncated is false.
    """
    percentage: float
    r"""Percentage of sampled records assigned to this pattern, from 0 to 100."""
    first_seen: datetime
    last_seen: datetime
    sample_log_ids: List[str]
    r"""A bounded set of example record identifiers; raw bodies are not repeated."""
    severity_summary: List[FacetValueTypedDict]
    r"""Top severity and service values observed inside this pattern."""
    service_summary: List[FacetValueTypedDict]
    suggested_query: NotRequired[str]
    r"""Bounded literal fragment that can be sent through SearchLogs to find
    representative raw records for this normalized pattern.
    """


class LogPattern(BaseModel):
    id: str
    r"""Stable hash of the normalized template."""

    template: str

    count: str
    r"""Number of sampled records assigned to this pattern. This is exact when
    meta.truncated is false.
    """

    percentage: float
    r"""Percentage of sampled records assigned to this pattern, from 0 to 100."""

    first_seen: datetime

    last_seen: datetime

    sample_log_ids: List[str]
    r"""A bounded set of example record identifiers; raw bodies are not repeated."""

    severity_summary: List[FacetValue]
    r"""Top severity and service values observed inside this pattern."""

    service_summary: List[FacetValue]

    suggested_query: Optional[str] = None
    r"""Bounded literal fragment that can be sent through SearchLogs to find
    representative raw records for this normalized pattern.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["suggested_query"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
