"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class AuditLogSortTypedDict(TypedDict):
    r"""A sort expression for audit log queries."""

    key: str
    r"""Audit log field to sort by. Supported keys include _id, created_at, entity_type, entity_id, action, and actor_id."""
    direction: NotRequired[str]
    r"""Sort direction. Defaults to desc when omitted or invalid."""


class AuditLogSort(BaseModel):
    r"""A sort expression for audit log queries."""

    key: str
    r"""Audit log field to sort by. Supported keys include _id, created_at, entity_type, entity_id, action, and actor_id."""

    direction: Optional[str] = None
    r"""Sort direction. Defaults to desc when omitted or invalid."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["direction"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
