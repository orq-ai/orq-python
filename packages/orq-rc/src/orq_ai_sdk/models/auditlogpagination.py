"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class AuditLogPaginationTypedDict(TypedDict):
    r"""Cursor pagination settings for audit log queries."""

    limit: NotRequired[int]
    r"""Maximum number of audit logs to return. Defaults to 50 when omitted."""
    starting_after: NotRequired[str]
    r"""Cursor for the next page. Use the last audit_log_id from the previous page."""
    ending_before: NotRequired[str]
    r"""Cursor for the previous page. Use the first audit_log_id from the current page."""


class AuditLogPagination(BaseModel):
    r"""Cursor pagination settings for audit log queries."""

    limit: Optional[int] = None
    r"""Maximum number of audit logs to return. Defaults to 50 when omitted."""

    starting_after: Optional[str] = None
    r"""Cursor for the next page. Use the last audit_log_id from the previous page."""

    ending_before: Optional[str] = None
    r"""Cursor for the previous page. Use the first audit_log_id from the current page."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["limit", "starting_after", "ending_before"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
