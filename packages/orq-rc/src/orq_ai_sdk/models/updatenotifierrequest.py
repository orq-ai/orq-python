"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .notifiertype import NotifierType
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class UpdateNotifierRequestMetadataTypedDict(TypedDict):
    r"""Optional. Replacement custom JSON metadata."""


class UpdateNotifierRequestMetadata(BaseModel):
    r"""Optional. Replacement custom JSON metadata."""


class HeadersTypedDict(TypedDict):
    r"""Optional replacement generic webhook headers. Secret header values returned by GET or LIST are masked as an empty string; omit those entries or replace them with the real value when updating."""


class Headers(BaseModel):
    r"""Optional replacement generic webhook headers. Secret header values returned by GET or LIST are masked as an empty string; omit those entries or replace them with the real value when updating."""


class UpdateNotifierRequestTypedDict(TypedDict):
    r"""Partial notifier update. All request body fields are optional individually, but at least one field must be provided. When changing `type`, include the destination field required by the new type. Secret header values returned by GET or LIST are masked as an empty string; omit those entries or replace them with the real value when updating."""

    project_id: NotRequired[str]
    r"""Optional. New containing project. Workspace-scoped callers may set an empty value to make the notifier workspace-wide. Project-scoped API keys remain pinned to the API key's project."""
    display_name: NotRequired[str]
    r"""Optional. New human-readable notifier name."""
    metadata: NotRequired[UpdateNotifierRequestMetadataTypedDict]
    r"""Optional. Replacement custom JSON metadata."""
    type: NotRequired[NotifierType]
    emails: NotRequired[List[str]]
    r"""Optional replacement email recipients. Required when changing `type` to `NOTIFIER_TYPE_EMAIL`."""
    incoming_webhook_url: NotRequired[str]
    r"""Optional replacement Slack incoming webhook URL. Required when changing `type` to `NOTIFIER_TYPE_SLACK_WEBHOOK`."""
    webhook_url: NotRequired[str]
    r"""Optional replacement generic webhook URL. Required when changing `type` to `NOTIFIER_TYPE_WEBHOOK`."""
    headers: NotRequired[HeadersTypedDict]
    r"""Optional replacement generic webhook headers. Secret header values returned by GET or LIST are masked as an empty string; omit those entries or replace them with the real value when updating."""


class UpdateNotifierRequest(BaseModel):
    r"""Partial notifier update. All request body fields are optional individually, but at least one field must be provided. When changing `type`, include the destination field required by the new type. Secret header values returned by GET or LIST are masked as an empty string; omit those entries or replace them with the real value when updating."""

    project_id: Optional[str] = None
    r"""Optional. New containing project. Workspace-scoped callers may set an empty value to make the notifier workspace-wide. Project-scoped API keys remain pinned to the API key's project."""

    display_name: Optional[str] = None
    r"""Optional. New human-readable notifier name."""

    metadata: Optional[UpdateNotifierRequestMetadata] = None
    r"""Optional. Replacement custom JSON metadata."""

    type: Optional[NotifierType] = None

    emails: Optional[List[str]] = None
    r"""Optional replacement email recipients. Required when changing `type` to `NOTIFIER_TYPE_EMAIL`."""

    incoming_webhook_url: Optional[str] = None
    r"""Optional replacement Slack incoming webhook URL. Required when changing `type` to `NOTIFIER_TYPE_SLACK_WEBHOOK`."""

    webhook_url: Optional[str] = None
    r"""Optional replacement generic webhook URL. Required when changing `type` to `NOTIFIER_TYPE_WEBHOOK`."""

    headers: Optional[Headers] = None
    r"""Optional replacement generic webhook headers. Secret header values returned by GET or LIST are masked as an empty string; omit those entries or replace them with the real value when updating."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "project_id",
                "display_name",
                "metadata",
                "type",
                "emails",
                "incoming_webhook_url",
                "webhook_url",
                "headers",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
