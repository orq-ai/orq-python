"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .monitorwidget import MonitorWidget, MonitorWidgetTypedDict
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class CreateMonitorRequestTypedDict(TypedDict):
    display_name: str
    r"""Workspace-unique display name."""
    widgets: List[MonitorWidgetTypedDict]
    r"""Widgets to render on the monitor."""
    description: NotRequired[str]
    r"""Short human-readable summary of what the monitor tracks."""
    project_id: NotRequired[str]
    r"""Project that should own the monitor. Omit to create a
    workspace-wide monitor (requires admin access).
    """
    default_range: NotRequired[str]
    r"""Default relative time range applied when the monitor opens."""
    preset_key: NotRequired[str]
    r"""Preset the monitor is created from, stamped for provenance."""


class CreateMonitorRequest(BaseModel):
    display_name: str
    r"""Workspace-unique display name."""

    widgets: List[MonitorWidget]
    r"""Widgets to render on the monitor."""

    description: Optional[str] = None
    r"""Short human-readable summary of what the monitor tracks."""

    project_id: Optional[str] = None
    r"""Project that should own the monitor. Omit to create a
    workspace-wide monitor (requires admin access).
    """

    default_range: Optional[str] = None
    r"""Default relative time range applied when the monitor opens."""

    preset_key: Optional[str] = None
    r"""Preset the monitor is created from, stamped for provenance."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["description", "project_id", "default_range", "preset_key"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
