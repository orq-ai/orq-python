"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from datetime import datetime
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import List, Literal, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


ContentType = Literal[
    "application/json",
    "application/x-www-form-urlencoded",
]
r"""Content type sent with webhook deliveries."""


class WebhookTypedDict(TypedDict):
    r"""A webhook subscription and its delivery configuration. Responses include the signing secret; treat it as sensitive."""

    id: str
    r"""Unique webhook ID."""
    url: str
    r"""HTTPS endpoint that receives webhook deliveries."""
    content_type: ContentType
    r"""Content type sent with webhook deliveries."""
    display_name: str
    r"""Human-readable webhook name."""
    events: List[str]
    r"""Workspace event slugs that trigger a delivery, for example `deployment.invoked` or `llm.response`."""
    secret: str
    r"""Signing secret used to verify webhook delivery signatures. Treat this value as sensitive."""
    created_by_id: str
    r"""Account ID that created the webhook."""
    updated_by_id: str
    r"""Account ID that most recently updated the webhook."""
    created: datetime
    r"""Creation time in RFC 3339 format."""
    updated: datetime
    r"""Most recent update time in RFC 3339 format."""
    enabled: NotRequired[bool]
    r"""Whether webhook deliveries are enabled."""
    failure_count: NotRequired[int]
    r"""Server-managed number of consecutive delivery failures."""


class Webhook(BaseModel):
    r"""A webhook subscription and its delivery configuration. Responses include the signing secret; treat it as sensitive."""

    id: Annotated[str, pydantic.Field(alias="_id")]
    r"""Unique webhook ID."""

    url: str
    r"""HTTPS endpoint that receives webhook deliveries."""

    content_type: ContentType
    r"""Content type sent with webhook deliveries."""

    display_name: str
    r"""Human-readable webhook name."""

    events: List[str]
    r"""Workspace event slugs that trigger a delivery, for example `deployment.invoked` or `llm.response`."""

    secret: str
    r"""Signing secret used to verify webhook delivery signatures. Treat this value as sensitive."""

    created_by_id: str
    r"""Account ID that created the webhook."""

    updated_by_id: str
    r"""Account ID that most recently updated the webhook."""

    created: datetime
    r"""Creation time in RFC 3339 format."""

    updated: datetime
    r"""Most recent update time in RFC 3339 format."""

    enabled: Optional[bool] = None
    r"""Whether webhook deliveries are enabled."""

    failure_count: Optional[int] = None
    r"""Server-managed number of consecutive delivery failures."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["enabled", "failure_count"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    Webhook.model_rebuild()
except NameError:
    pass
