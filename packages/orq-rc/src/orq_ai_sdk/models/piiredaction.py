"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .piiredactionconfig import PiiRedactionConfig, PiiRedactionConfigTypedDict
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class PiiRedactionTypedDict(TypedDict):
    r"""PiiRedaction is the workspace-default configuration for the pii_redaction
    plugin: an enable flag plus the optional plugin config the gateway applies as
    a floor for requests that send no plugins of their own.
    """

    enabled: bool
    r"""Whether the workspace-default PII redaction plugin is enabled."""
    config: NotRequired[PiiRedactionConfigTypedDict]
    r"""Plugin configuration applied when enabled. Optional."""


class PiiRedaction(BaseModel):
    r"""PiiRedaction is the workspace-default configuration for the pii_redaction
    plugin: an enable flag plus the optional plugin config the gateway applies as
    a floor for requests that send no plugins of their own.
    """

    enabled: bool
    r"""Whether the workspace-default PII redaction plugin is enabled."""

    config: Optional[PiiRedactionConfig] = None
    r"""Plugin configuration applied when enabled. Optional."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["config"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
