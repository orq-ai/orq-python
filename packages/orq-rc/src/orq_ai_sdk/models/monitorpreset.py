"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .monitorwidget import MonitorWidget, MonitorWidgetTypedDict
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import List, Literal, Optional
from typing_extensions import NotRequired, TypedDict


MonitorPresetKind = Literal[
    "dashboard",
    "widget",
]
r"""Preset kind."""


class MonitorPresetTypedDict(TypedDict):
    preset_key: str
    r"""Stable identifier for the preset, e.g. `genai-overview` or
    `widget-cost-by-model`.
    """
    display_name: str
    r"""Human-readable preset name shown in galleries."""
    description: str
    r"""What the preset shows and when to use it."""
    kind: MonitorPresetKind
    r"""Preset kind."""
    widgets: List[MonitorWidgetTypedDict]
    r"""Widgets the preset materializes into a create request. Widget
    presets carry exactly one entry.
    """
    default_range: NotRequired[str]
    r"""Default relative time range suggested by the preset."""


class MonitorPreset(BaseModel):
    preset_key: str
    r"""Stable identifier for the preset, e.g. `genai-overview` or
    `widget-cost-by-model`.
    """

    display_name: str
    r"""Human-readable preset name shown in galleries."""

    description: str
    r"""What the preset shows and when to use it."""

    kind: MonitorPresetKind
    r"""Preset kind."""

    widgets: List[MonitorWidget]
    r"""Widgets the preset materializes into a create request. Widget
    presets carry exactly one entry.
    """

    default_range: Optional[str] = None
    r"""Default relative time range suggested by the preset."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["default_range"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
