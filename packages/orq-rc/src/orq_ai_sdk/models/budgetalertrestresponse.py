"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .budgetalertdimension import BudgetAlertDimension
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class BudgetAlertRestResponseTypedDict(TypedDict):
    r"""Notifies every listed notifier once current-period consumption on
    `dimension` reaches `threshold_percent` of the matching limit. Each alert
    fires at most once per period, re-arming on rollover or a limit change.
    The dimension must have a limit set, or the write is rejected.
    """

    threshold_percent: int
    r"""Percentage of the dimension's limit at which the alert fires, 1–100."""
    notifier_ids: List[str]
    r"""Must be workspace-scoped; project-scoped notifiers are rejected."""
    id: NotRequired[str]
    r"""Assigned by ORQ. Supply an existing id to edit that alert in place."""
    dimension: NotRequired[BudgetAlertDimension]


class BudgetAlertRestResponse(BaseModel):
    r"""Notifies every listed notifier once current-period consumption on
    `dimension` reaches `threshold_percent` of the matching limit. Each alert
    fires at most once per period, re-arming on rollover or a limit change.
    The dimension must have a limit set, or the write is rejected.
    """

    threshold_percent: Annotated[int, pydantic.Field(alias="thresholdPercent")]
    r"""Percentage of the dimension's limit at which the alert fires, 1–100."""

    notifier_ids: Annotated[List[str], pydantic.Field(alias="notifierIds")]
    r"""Must be workspace-scoped; project-scoped notifiers are rejected."""

    id: Optional[str] = None
    r"""Assigned by ORQ. Supply an existing id to edit that alert in place."""

    dimension: Optional[BudgetAlertDimension] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["id", "dimension"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    BudgetAlertRestResponse.model_rebuild()
except NameError:
    pass
