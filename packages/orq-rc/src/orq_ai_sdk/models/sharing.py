"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .sharingallprojects import SharingAllProjects, SharingAllProjectsTypedDict
from .sharingselectedprojects import (
    SharingSelectedProjects,
    SharingSelectedProjectsTypedDict,
)
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class SharingTypedDict(TypedDict):
    r"""Sharing controls which projects in the workspace may use this entity and
    what they may do with it.
    """

    all_projects: NotRequired[SharingAllProjectsTypedDict]
    r"""Visible to every project in the workspace."""
    selected: NotRequired[SharingSelectedProjectsTypedDict]
    r"""Visible to the listed projects only."""
    allow_version_pin: NotRequired[bool]
    r"""Consuming projects may pin a specific version instead of tracking
    latest. Enforcement lives with the consuming reference.
    """
    allow_fork: NotRequired[bool]
    r"""Projects may duplicate this entity into a detached project-owned
    copy.
    """
    auto_grant_new_projects: NotRequired[bool]
    r"""New projects created after this sharing config is applied should
    receive access automatically. Enforcement lives with project
    creation and entity-specific adoption code.
    """


class Sharing(BaseModel):
    r"""Sharing controls which projects in the workspace may use this entity and
    what they may do with it.
    """

    all_projects: Optional[SharingAllProjects] = None
    r"""Visible to every project in the workspace."""

    selected: Optional[SharingSelectedProjects] = None
    r"""Visible to the listed projects only."""

    allow_version_pin: Optional[bool] = None
    r"""Consuming projects may pin a specific version instead of tracking
    latest. Enforcement lives with the consuming reference.
    """

    allow_fork: Optional[bool] = None
    r"""Projects may duplicate this entity into a detached project-owned
    copy.
    """

    auto_grant_new_projects: Optional[bool] = None
    r"""New projects created after this sharing config is applied should
    receive access automatically. Enforcement lives with project
    creation and entity-specific adoption code.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "all_projects",
                "selected",
                "allow_version_pin",
                "allow_fork",
                "auto_grant_new_projects",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
