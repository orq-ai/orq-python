"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Literal, Optional
from typing_extensions import NotRequired, TypedDict


Comparator = Literal[
    "gt",
    "gte",
    "lt",
    "lte",
    "eq",
]
r"""Comparison applied as `value <comparator> threshold`."""


Window = Literal[
    "5m",
    "15m",
    "30m",
    "1h",
    "4h",
    "1d",
]
r"""Look-back window aggregated on each evaluation tick."""


Interval = Literal[
    "30s",
    "5m",
    "15m",
    "1h",
    "1d",
]
r"""How often the alert is evaluated. `30s` is a testing cadence."""


Delay = Literal[
    "30s",
    "60s",
    "120s",
    "300s",
    "600s",
]
r"""Ingestion delay subtracted from the evaluation window. Defaults to `30s`."""


class AlertConditionTypedDict(TypedDict):
    comparator: Comparator
    r"""Comparison applied as `value <comparator> threshold`."""
    threshold: float
    r"""Critical-tier threshold. Breaching it opens (or escalates) a
    trigger at critical severity.
    """
    window: Window
    r"""Look-back window aggregated on each evaluation tick."""
    interval: Interval
    r"""How often the alert is evaluated. `30s` is a testing cadence."""
    degraded_threshold: NotRequired[float]
    r"""Optional degraded-tier threshold. Values past it but short of
    `threshold` open a trigger at degraded severity. Must sit on the
    recovery side of `threshold` for the chosen comparator and is not
    supported with `eq`.
    """
    resolve_threshold: NotRequired[float]
    r"""Optional custom resolve bound (hysteresis). An open trigger
    resolves only once the value recovers past this bound; defaults to
    the lowest configured tier. Must sit at or past that tier on the
    recovery side. Not supported with `eq`.
    """
    delay: NotRequired[Delay]
    r"""Ingestion delay subtracted from the evaluation window. Defaults to `30s`."""


class AlertCondition(BaseModel):
    comparator: Comparator
    r"""Comparison applied as `value <comparator> threshold`."""

    threshold: float
    r"""Critical-tier threshold. Breaching it opens (or escalates) a
    trigger at critical severity.
    """

    window: Window
    r"""Look-back window aggregated on each evaluation tick."""

    interval: Interval
    r"""How often the alert is evaluated. `30s` is a testing cadence."""

    degraded_threshold: Optional[float] = None
    r"""Optional degraded-tier threshold. Values past it but short of
    `threshold` open a trigger at degraded severity. Must sit on the
    recovery side of `threshold` for the chosen comparator and is not
    supported with `eq`.
    """

    resolve_threshold: Optional[float] = None
    r"""Optional custom resolve bound (hysteresis). An open trigger
    resolves only once the value recovers past this bound; defaults to
    the lowest configured tier. Must sit at or past that tier on the
    recovery side. Not supported with `eq`.
    """

    delay: Optional[Delay] = None
    r"""Ingestion delay subtracted from the evaluation window. Defaults to `30s`."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["degraded_threshold", "resolve_threshold", "delay"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
