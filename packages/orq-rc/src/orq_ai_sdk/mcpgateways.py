"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from .basesdk import BaseSDK
from orq_ai_sdk import models, utils
from orq_ai_sdk._hooks import HookContext
from orq_ai_sdk.types import OptionalNullable, UNSET
from orq_ai_sdk.utils import get_security_from_env
from orq_ai_sdk.utils.unmarshal_json_response import unmarshal_json_response
from typing import Iterable, List, Mapping, Optional, Union


class McpGateways(BaseSDK):
    def list(
        self,
        *,
        limit: Optional[int] = None,
        starting_after: Optional[str] = None,
        ending_before: Optional[str] = None,
        search: Optional[str] = None,
        status: Optional[models.McpGatewayStatus] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.ListMcpGatewaysResponse:
        r"""List MCP gateways

        Returns a paginated list of MCP gateways in the workspace.

        :param limit: Page size between 1 and 200. Defaults to 25.
        :param starting_after: Cursor for the page after the given item id. Mutually exclusive with `ending_before`.
        :param ending_before: Cursor for the page before the given item id. Mutually exclusive with `starting_after`.
        :param search: Case-insensitive match against the gateway key and display name.
        :param status: Returns only gateways in this status.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.McpGatewayListRequest(
            limit=limit,
            starting_after=starting_after,
            ending_before=ending_before,
            search=search,
            status=status,
        )

        req = self._build_request(
            method="GET",
            path="/v2/mcp-gateways",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpGatewayList",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpGateways",
                    "x-cli-name": "list",
                    "x-code-samples": [
                        {
                            "label": "Core - List MCP gateways",
                            "lang": "curl",
                            "source": "curl --get 'https://api.orq.ai/v2/mcp-gateways' \\\n  --header 'Authorization: Bearer $ORQ_API_KEY' \\\n  --data-urlencode 'limit=25' \\\n  --data-urlencode 'status=MCP_GATEWAY_STATUS_ACTIVE'\n",
                        },
                        {
                            "label": "Python - List MCP gateways",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\npage = client.mcp_gateways.list(\n    limit=25,\n    status="MCP_GATEWAY_STATUS_ACTIVE",\n)\n\nfor gateway in page.data:\n    print(gateway.key, gateway.public_url, gateway.exposed_tools_count)\n',
                        },
                        {
                            "label": "Node.js - List MCP gateways",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\nconst page = await client.mcpGateways.list({\n  limit: 25,\n  status: 'MCP_GATEWAY_STATUS_ACTIVE',\n});\n\nfor (const gateway of page.data) {\n  console.log(gateway.key, gateway.publicUrl, gateway.exposedToolsCount);\n}\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.ListMcpGatewaysResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    async def list_async(
        self,
        *,
        limit: Optional[int] = None,
        starting_after: Optional[str] = None,
        ending_before: Optional[str] = None,
        search: Optional[str] = None,
        status: Optional[models.McpGatewayStatus] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.ListMcpGatewaysResponse:
        r"""List MCP gateways

        Returns a paginated list of MCP gateways in the workspace.

        :param limit: Page size between 1 and 200. Defaults to 25.
        :param starting_after: Cursor for the page after the given item id. Mutually exclusive with `ending_before`.
        :param ending_before: Cursor for the page before the given item id. Mutually exclusive with `starting_after`.
        :param search: Case-insensitive match against the gateway key and display name.
        :param status: Returns only gateways in this status.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.McpGatewayListRequest(
            limit=limit,
            starting_after=starting_after,
            ending_before=ending_before,
            search=search,
            status=status,
        )

        req = self._build_request_async(
            method="GET",
            path="/v2/mcp-gateways",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpGatewayList",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpGateways",
                    "x-cli-name": "list",
                    "x-code-samples": [
                        {
                            "label": "Core - List MCP gateways",
                            "lang": "curl",
                            "source": "curl --get 'https://api.orq.ai/v2/mcp-gateways' \\\n  --header 'Authorization: Bearer $ORQ_API_KEY' \\\n  --data-urlencode 'limit=25' \\\n  --data-urlencode 'status=MCP_GATEWAY_STATUS_ACTIVE'\n",
                        },
                        {
                            "label": "Python - List MCP gateways",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\npage = client.mcp_gateways.list(\n    limit=25,\n    status="MCP_GATEWAY_STATUS_ACTIVE",\n)\n\nfor gateway in page.data:\n    print(gateway.key, gateway.public_url, gateway.exposed_tools_count)\n',
                        },
                        {
                            "label": "Node.js - List MCP gateways",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\nconst page = await client.mcpGateways.list({\n  limit: 25,\n  status: 'MCP_GATEWAY_STATUS_ACTIVE',\n});\n\nfor (const gateway of page.data) {\n  console.log(gateway.key, gateway.publicUrl, gateway.exposedToolsCount);\n}\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.ListMcpGatewaysResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    def create(
        self,
        *,
        key: str,
        display_name: str,
        description: Optional[str] = None,
        server_links: Optional[
            Union[
                Iterable[models.McpGatewayServerLink],
                Iterable[models.McpGatewayServerLinkTypedDict],
            ]
        ] = None,
        tool_naming: Optional[models.McpToolNaming] = None,
        mode: Optional[models.McpGatewayMode] = None,
        sharing: Optional[Union[models.Sharing, models.SharingTypedDict]] = None,
        plugins: Optional[
            Union[
                Iterable[models.McpGatewayPlugin],
                Iterable[models.McpGatewayPluginTypedDict],
            ]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.CreateMcpGatewayResponse:
        r"""Create an MCP gateway

        Creates a client-facing MCP gateway that links one or more synced upstream servers and exposes a unified MCP endpoint.

        :param key: Lowercase slug of letters, digits, hyphens and underscores, max 64 characters, unique per workspace; backs the gateway's public MCP URL.
        :param display_name: Human readable name shown in the workspace.
        :param description: Free-form note about what this gateway is for.
        :param server_links: Upstream servers this gateway aggregates.
        :param tool_naming:
        :param mode:
        :param sharing: Which projects in the workspace may use this gateway. Defaults to every project.
        :param plugins: Plugins run on every tool call this gateway serves.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.CreateMcpGatewayRequest(
            key=key,
            display_name=display_name,
            description=description,
            server_links=utils.get_pydantic_model(
                server_links, Optional[List[models.McpGatewayServerLink]]
            ),
            tool_naming=tool_naming,
            mode=mode,
            sharing=utils.get_pydantic_model(sharing, Optional[models.Sharing]),
            plugins=utils.get_pydantic_model(
                plugins, Optional[List[models.McpGatewayPlugin]]
            ),
        )

        req = self._build_request(
            method="POST",
            path="/v2/mcp-gateways",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.CreateMcpGatewayRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpGatewayCreate",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpGateways",
                    "x-cli-name": "create",
                    "x-code-samples": [
                        {
                            "label": "Core - Create MCP gateway",
                            "lang": "curl",
                            "source": 'curl --request POST \\\n  --url \'https://api.orq.ai/v2/mcp-gateways\' \\\n  --header \'Authorization: Bearer $ORQ_API_KEY\' \\\n  --header \'Content-Type: application/json\' \\\n  --data \'{\n    "key": "support-desk",\n    "display_name": "Support Desk",\n    "description": "Tools the support agent is allowed to call.",\n    "server_links": [\n      {\n        "mcp_server_id": "01JQ0K5R8N2ZC7X4M9T3V6HWBD",\n        "alias": "github",\n        "enabled": true,\n        "tool_exposure": { "mode": "MCP_TOOL_EXPOSURE_MODE_ALL" }\n      }\n    ],\n    "tool_naming": "MCP_TOOL_NAMING_PREFIX_ON_COLLISION",\n    "mode": "MCP_GATEWAY_MODE_DIRECT"\n  }\'\n',
                        },
                        {
                            "label": "Python - Create MCP gateway",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\n# Each link needs an alias and a tool_exposure; both are required.\nresult = client.mcp_gateways.create(\n    key="support-desk",\n    display_name="Support Desk",\n    description="Tools the support agent is allowed to call.",\n    server_links=[\n        {\n            "mcp_server_id": "01JQ0K5R8N2ZC7X4M9T3V6HWBD",\n            "alias": "github",\n            "enabled": True,\n            "tool_exposure": {"mode": "MCP_TOOL_EXPOSURE_MODE_ALL"},\n        },\n    ],\n    tool_naming="MCP_TOOL_NAMING_PREFIX_ON_COLLISION",\n    mode="MCP_GATEWAY_MODE_DIRECT",\n)\n\nprint(result.mcp_gateway.public_url)\n',
                        },
                        {
                            "label": "Node.js - Create MCP gateway",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\n// Each link needs an alias and a toolExposure; both are required.\nconst result = await client.mcpGateways.create({\n  key: 'support-desk',\n  displayName: 'Support Desk',\n  description: 'Tools the support agent is allowed to call.',\n  serverLinks: [\n    {\n      mcpServerId: '01JQ0K5R8N2ZC7X4M9T3V6HWBD',\n      alias: 'github',\n      enabled: true,\n      toolExposure: { mode: 'MCP_TOOL_EXPOSURE_MODE_ALL' },\n    },\n  ],\n  toolNaming: 'MCP_TOOL_NAMING_PREFIX_ON_COLLISION',\n  mode: 'MCP_GATEWAY_MODE_DIRECT',\n});\n\nconsole.log(result.mcpGateway.publicUrl);\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.CreateMcpGatewayResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    async def create_async(
        self,
        *,
        key: str,
        display_name: str,
        description: Optional[str] = None,
        server_links: Optional[
            Union[
                Iterable[models.McpGatewayServerLink],
                Iterable[models.McpGatewayServerLinkTypedDict],
            ]
        ] = None,
        tool_naming: Optional[models.McpToolNaming] = None,
        mode: Optional[models.McpGatewayMode] = None,
        sharing: Optional[Union[models.Sharing, models.SharingTypedDict]] = None,
        plugins: Optional[
            Union[
                Iterable[models.McpGatewayPlugin],
                Iterable[models.McpGatewayPluginTypedDict],
            ]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.CreateMcpGatewayResponse:
        r"""Create an MCP gateway

        Creates a client-facing MCP gateway that links one or more synced upstream servers and exposes a unified MCP endpoint.

        :param key: Lowercase slug of letters, digits, hyphens and underscores, max 64 characters, unique per workspace; backs the gateway's public MCP URL.
        :param display_name: Human readable name shown in the workspace.
        :param description: Free-form note about what this gateway is for.
        :param server_links: Upstream servers this gateway aggregates.
        :param tool_naming:
        :param mode:
        :param sharing: Which projects in the workspace may use this gateway. Defaults to every project.
        :param plugins: Plugins run on every tool call this gateway serves.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.CreateMcpGatewayRequest(
            key=key,
            display_name=display_name,
            description=description,
            server_links=utils.get_pydantic_model(
                server_links, Optional[List[models.McpGatewayServerLink]]
            ),
            tool_naming=tool_naming,
            mode=mode,
            sharing=utils.get_pydantic_model(sharing, Optional[models.Sharing]),
            plugins=utils.get_pydantic_model(
                plugins, Optional[List[models.McpGatewayPlugin]]
            ),
        )

        req = self._build_request_async(
            method="POST",
            path="/v2/mcp-gateways",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.CreateMcpGatewayRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpGatewayCreate",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpGateways",
                    "x-cli-name": "create",
                    "x-code-samples": [
                        {
                            "label": "Core - Create MCP gateway",
                            "lang": "curl",
                            "source": 'curl --request POST \\\n  --url \'https://api.orq.ai/v2/mcp-gateways\' \\\n  --header \'Authorization: Bearer $ORQ_API_KEY\' \\\n  --header \'Content-Type: application/json\' \\\n  --data \'{\n    "key": "support-desk",\n    "display_name": "Support Desk",\n    "description": "Tools the support agent is allowed to call.",\n    "server_links": [\n      {\n        "mcp_server_id": "01JQ0K5R8N2ZC7X4M9T3V6HWBD",\n        "alias": "github",\n        "enabled": true,\n        "tool_exposure": { "mode": "MCP_TOOL_EXPOSURE_MODE_ALL" }\n      }\n    ],\n    "tool_naming": "MCP_TOOL_NAMING_PREFIX_ON_COLLISION",\n    "mode": "MCP_GATEWAY_MODE_DIRECT"\n  }\'\n',
                        },
                        {
                            "label": "Python - Create MCP gateway",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\n# Each link needs an alias and a tool_exposure; both are required.\nresult = client.mcp_gateways.create(\n    key="support-desk",\n    display_name="Support Desk",\n    description="Tools the support agent is allowed to call.",\n    server_links=[\n        {\n            "mcp_server_id": "01JQ0K5R8N2ZC7X4M9T3V6HWBD",\n            "alias": "github",\n            "enabled": True,\n            "tool_exposure": {"mode": "MCP_TOOL_EXPOSURE_MODE_ALL"},\n        },\n    ],\n    tool_naming="MCP_TOOL_NAMING_PREFIX_ON_COLLISION",\n    mode="MCP_GATEWAY_MODE_DIRECT",\n)\n\nprint(result.mcp_gateway.public_url)\n',
                        },
                        {
                            "label": "Node.js - Create MCP gateway",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\n// Each link needs an alias and a toolExposure; both are required.\nconst result = await client.mcpGateways.create({\n  key: 'support-desk',\n  displayName: 'Support Desk',\n  description: 'Tools the support agent is allowed to call.',\n  serverLinks: [\n    {\n      mcpServerId: '01JQ0K5R8N2ZC7X4M9T3V6HWBD',\n      alias: 'github',\n      enabled: true,\n      toolExposure: { mode: 'MCP_TOOL_EXPOSURE_MODE_ALL' },\n    },\n  ],\n  toolNaming: 'MCP_TOOL_NAMING_PREFIX_ON_COLLISION',\n  mode: 'MCP_GATEWAY_MODE_DIRECT',\n});\n\nconsole.log(result.mcpGateway.publicUrl);\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.CreateMcpGatewayResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    def list_tools(
        self,
        *,
        gateway_id: str,
        limit: Optional[int] = None,
        starting_after: Optional[str] = None,
        ending_before: Optional[str] = None,
        mcp_server_id: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.ListMcpGatewayToolsResponse:
        r"""List exposed tools for a gateway

        Returns the namespaced tool view for a gateway.

        :param gateway_id: Unique identifier of the MCP gateway.
        :param limit: Page size between 1 and 200. Defaults to 25.
        :param starting_after: Cursor for the page after the given `exposed_name`. Mutually exclusive with `ending_before`.
        :param ending_before: Cursor for the page before the given `exposed_name`. Mutually exclusive with `starting_after`.
        :param mcp_server_id: Returns only tools contributed by this upstream server.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.McpGatewayListToolsRequest(
            gateway_id=gateway_id,
            limit=limit,
            starting_after=starting_after,
            ending_before=ending_before,
            mcp_server_id=mcp_server_id,
        )

        req = self._build_request(
            method="GET",
            path="/v2/mcp-gateways/{gateway_id}/tools",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpGatewayListTools",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpGateways",
                    "x-cli-name": "listTools",
                    "x-code-samples": [
                        {
                            "label": "Core - List the tools a gateway exposes",
                            "lang": "curl",
                            "source": "curl --get 'https://api.orq.ai/v2/mcp-gateways/01JQ0M8W4T5YB2Q7N1F6K3PZRC/tools' \\\n  --header 'Authorization: Bearer $ORQ_API_KEY' \\\n  --data-urlencode 'limit=100'\n",
                        },
                        {
                            "label": "Core - List the tools contributed by one server",
                            "lang": "curl",
                            "source": "curl --get 'https://api.orq.ai/v2/mcp-gateways/01JQ0M8W4T5YB2Q7N1F6K3PZRC/tools' \\\n  --header 'Authorization: Bearer $ORQ_API_KEY' \\\n  --data-urlencode 'mcp_server_id=01JQ0K5R8N2ZC7X4M9T3V6HWBD'\n",
                        },
                        {
                            "label": "Python - List the tools a gateway exposes",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\npage = client.mcp_gateways.list_tools(\n    gateway_id="01JQ0M8W4T5YB2Q7N1F6K3PZRC",\n    limit=100,\n)\n\n# exposed_name is what MCP clients call; upstream_tool_name is the original.\nfor tool in page.data:\n    print(tool.exposed_name, "->", tool.upstream_tool_name)\n',
                        },
                        {
                            "label": "Node.js - List the tools a gateway exposes",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\nconst page = await client.mcpGateways.listTools({\n  gatewayId: '01JQ0M8W4T5YB2Q7N1F6K3PZRC',\n  limit: 100,\n});\n\n// exposedName is what MCP clients call; upstreamToolName is the original.\nfor (const tool of page.data) {\n  console.log(tool.exposedName, '->', tool.upstreamToolName);\n}\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.ListMcpGatewayToolsResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    async def list_tools_async(
        self,
        *,
        gateway_id: str,
        limit: Optional[int] = None,
        starting_after: Optional[str] = None,
        ending_before: Optional[str] = None,
        mcp_server_id: Optional[str] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.ListMcpGatewayToolsResponse:
        r"""List exposed tools for a gateway

        Returns the namespaced tool view for a gateway.

        :param gateway_id: Unique identifier of the MCP gateway.
        :param limit: Page size between 1 and 200. Defaults to 25.
        :param starting_after: Cursor for the page after the given `exposed_name`. Mutually exclusive with `ending_before`.
        :param ending_before: Cursor for the page before the given `exposed_name`. Mutually exclusive with `starting_after`.
        :param mcp_server_id: Returns only tools contributed by this upstream server.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.McpGatewayListToolsRequest(
            gateway_id=gateway_id,
            limit=limit,
            starting_after=starting_after,
            ending_before=ending_before,
            mcp_server_id=mcp_server_id,
        )

        req = self._build_request_async(
            method="GET",
            path="/v2/mcp-gateways/{gateway_id}/tools",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpGatewayListTools",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpGateways",
                    "x-cli-name": "listTools",
                    "x-code-samples": [
                        {
                            "label": "Core - List the tools a gateway exposes",
                            "lang": "curl",
                            "source": "curl --get 'https://api.orq.ai/v2/mcp-gateways/01JQ0M8W4T5YB2Q7N1F6K3PZRC/tools' \\\n  --header 'Authorization: Bearer $ORQ_API_KEY' \\\n  --data-urlencode 'limit=100'\n",
                        },
                        {
                            "label": "Core - List the tools contributed by one server",
                            "lang": "curl",
                            "source": "curl --get 'https://api.orq.ai/v2/mcp-gateways/01JQ0M8W4T5YB2Q7N1F6K3PZRC/tools' \\\n  --header 'Authorization: Bearer $ORQ_API_KEY' \\\n  --data-urlencode 'mcp_server_id=01JQ0K5R8N2ZC7X4M9T3V6HWBD'\n",
                        },
                        {
                            "label": "Python - List the tools a gateway exposes",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\npage = client.mcp_gateways.list_tools(\n    gateway_id="01JQ0M8W4T5YB2Q7N1F6K3PZRC",\n    limit=100,\n)\n\n# exposed_name is what MCP clients call; upstream_tool_name is the original.\nfor tool in page.data:\n    print(tool.exposed_name, "->", tool.upstream_tool_name)\n',
                        },
                        {
                            "label": "Node.js - List the tools a gateway exposes",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\nconst page = await client.mcpGateways.listTools({\n  gatewayId: '01JQ0M8W4T5YB2Q7N1F6K3PZRC',\n  limit: 100,\n});\n\n// exposedName is what MCP clients call; upstreamToolName is the original.\nfor (const tool of page.data) {\n  console.log(tool.exposedName, '->', tool.upstreamToolName);\n}\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.ListMcpGatewayToolsResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    def retrieve(
        self,
        *,
        id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.GetMcpGatewayResponse:
        r"""Retrieve an MCP gateway

        Retrieves the details of an existing MCP gateway by its unique ID.

        :param id: Unique identifier of the MCP gateway.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.McpGatewayGetRequest(
            id=id,
        )

        req = self._build_request(
            method="GET",
            path="/v2/mcp-gateways/{id}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpGatewayGet",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpGateways",
                    "x-cli-name": "retrieve",
                    "x-code-samples": [
                        {
                            "label": "Core - Retrieve an MCP gateway",
                            "lang": "curl",
                            "source": "curl --get 'https://api.orq.ai/v2/mcp-gateways/01JQ0M8W4T5YB2Q7N1F6K3PZRC' \\\n  --header 'Authorization: Bearer $ORQ_API_KEY'\n",
                        },
                        {
                            "label": "Python - Retrieve an MCP gateway",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\nresult = client.mcp_gateways.retrieve(id="01JQ0M8W4T5YB2Q7N1F6K3PZRC")\n\n# public_url is the endpoint MCP clients connect to.\nprint(result.mcp_gateway.public_url, result.mcp_gateway.status)\n',
                        },
                        {
                            "label": "Node.js - Retrieve an MCP gateway",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\nconst result = await client.mcpGateways.retrieve({\n  id: '01JQ0M8W4T5YB2Q7N1F6K3PZRC',\n});\n\n// publicUrl is the endpoint MCP clients connect to.\nconsole.log(result.mcpGateway.publicUrl, result.mcpGateway.status);\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.GetMcpGatewayResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    async def retrieve_async(
        self,
        *,
        id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.GetMcpGatewayResponse:
        r"""Retrieve an MCP gateway

        Retrieves the details of an existing MCP gateway by its unique ID.

        :param id: Unique identifier of the MCP gateway.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.McpGatewayGetRequest(
            id=id,
        )

        req = self._build_request_async(
            method="GET",
            path="/v2/mcp-gateways/{id}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpGatewayGet",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpGateways",
                    "x-cli-name": "retrieve",
                    "x-code-samples": [
                        {
                            "label": "Core - Retrieve an MCP gateway",
                            "lang": "curl",
                            "source": "curl --get 'https://api.orq.ai/v2/mcp-gateways/01JQ0M8W4T5YB2Q7N1F6K3PZRC' \\\n  --header 'Authorization: Bearer $ORQ_API_KEY'\n",
                        },
                        {
                            "label": "Python - Retrieve an MCP gateway",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\nresult = client.mcp_gateways.retrieve(id="01JQ0M8W4T5YB2Q7N1F6K3PZRC")\n\n# public_url is the endpoint MCP clients connect to.\nprint(result.mcp_gateway.public_url, result.mcp_gateway.status)\n',
                        },
                        {
                            "label": "Node.js - Retrieve an MCP gateway",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\nconst result = await client.mcpGateways.retrieve({\n  id: '01JQ0M8W4T5YB2Q7N1F6K3PZRC',\n});\n\n// publicUrl is the endpoint MCP clients connect to.\nconsole.log(result.mcpGateway.publicUrl, result.mcpGateway.status);\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.GetMcpGatewayResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    def delete(
        self,
        *,
        id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.DeleteMcpGatewayResponse:
        r"""Delete an MCP gateway

        Deletes an MCP gateway from the workspace. The response body is empty when the delete succeeds.

        :param id: Unique identifier of the MCP gateway.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.McpGatewayDeleteRequest(
            id=id,
        )

        req = self._build_request(
            method="DELETE",
            path="/v2/mcp-gateways/{id}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpGatewayDelete",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpGateways",
                    "x-cli-name": "delete",
                    "x-code-samples": [
                        {
                            "label": "Core - Delete an MCP gateway",
                            "lang": "curl",
                            "source": "curl --request DELETE \\\n  --url 'https://api.orq.ai/v2/mcp-gateways/01JQ0M8W4T5YB2Q7N1F6K3PZRC' \\\n  --header 'Authorization: Bearer $ORQ_API_KEY'\n",
                        },
                        {
                            "label": "Python - Delete an MCP gateway",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\n# Connected MCP clients start receiving "gateway not found".\nclient.mcp_gateways.delete(id="01JQ0M8W4T5YB2Q7N1F6K3PZRC")\n',
                        },
                        {
                            "label": "Node.js - Delete an MCP gateway",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\n// Connected MCP clients start receiving \"gateway not found\".\nawait client.mcpGateways.delete({ id: '01JQ0M8W4T5YB2Q7N1F6K3PZRC' });\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.DeleteMcpGatewayResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    async def delete_async(
        self,
        *,
        id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.DeleteMcpGatewayResponse:
        r"""Delete an MCP gateway

        Deletes an MCP gateway from the workspace. The response body is empty when the delete succeeds.

        :param id: Unique identifier of the MCP gateway.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.McpGatewayDeleteRequest(
            id=id,
        )

        req = self._build_request_async(
            method="DELETE",
            path="/v2/mcp-gateways/{id}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpGatewayDelete",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpGateways",
                    "x-cli-name": "delete",
                    "x-code-samples": [
                        {
                            "label": "Core - Delete an MCP gateway",
                            "lang": "curl",
                            "source": "curl --request DELETE \\\n  --url 'https://api.orq.ai/v2/mcp-gateways/01JQ0M8W4T5YB2Q7N1F6K3PZRC' \\\n  --header 'Authorization: Bearer $ORQ_API_KEY'\n",
                        },
                        {
                            "label": "Python - Delete an MCP gateway",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\n# Connected MCP clients start receiving "gateway not found".\nclient.mcp_gateways.delete(id="01JQ0M8W4T5YB2Q7N1F6K3PZRC")\n',
                        },
                        {
                            "label": "Node.js - Delete an MCP gateway",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\n// Connected MCP clients start receiving \"gateway not found\".\nawait client.mcpGateways.delete({ id: '01JQ0M8W4T5YB2Q7N1F6K3PZRC' });\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.DeleteMcpGatewayResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    def update(
        self,
        *,
        id: str,
        key: Optional[str] = None,
        display_name: Optional[str] = None,
        description: Optional[str] = None,
        server_links: Optional[
            Union[
                Iterable[models.McpGatewayServerLink],
                Iterable[models.McpGatewayServerLinkTypedDict],
            ]
        ] = None,
        tool_naming: Optional[models.McpToolNaming] = None,
        status: Optional[models.McpGatewayStatus] = None,
        mode: Optional[models.McpGatewayMode] = None,
        sharing: Optional[Union[models.Sharing, models.SharingTypedDict]] = None,
        clear_server_links: Optional[bool] = None,
        plugins: Optional[
            Union[
                Iterable[models.McpGatewayPlugin],
                Iterable[models.McpGatewayPluginTypedDict],
            ]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.UpdateMcpGatewayResponse:
        r"""Update an MCP gateway

        Updates mutable fields of an existing MCP gateway. Omitted optional fields keep their current values.

        :param id: Unique identifier of the MCP gateway.
        :param key: Rejected with INVALID_ARGUMENT: the key backs the gateway's public MCP URL
            and is immutable after creation. Retained so callers get an error rather
            than a silently ignored field.
        :param display_name: Human readable name shown in the workspace.
        :param description: Free-form note about what this gateway is for.
        :param server_links: Replaces the current links. An empty array is treated as no change; use `clear_server_links` to remove them all.
        :param tool_naming:
        :param status:
        :param mode:
        :param sharing: Which projects in the workspace may use this gateway. Defaults to every project.
        :param clear_server_links: Set true to remove every link; cannot be combined with `server_links`.
        :param plugins: Plugins run on every tool call this gateway serves.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.McpGatewayUpdateRequest(
            id=id,
            update_mcp_gateway_request=models.UpdateMcpGatewayRequest(
                key=key,
                display_name=display_name,
                description=description,
                server_links=utils.get_pydantic_model(
                    server_links, Optional[List[models.McpGatewayServerLink]]
                ),
                tool_naming=tool_naming,
                status=status,
                mode=mode,
                sharing=utils.get_pydantic_model(sharing, Optional[models.Sharing]),
                clear_server_links=clear_server_links,
                plugins=utils.get_pydantic_model(
                    plugins, Optional[List[models.McpGatewayPlugin]]
                ),
            ),
        )

        req = self._build_request(
            method="PATCH",
            path="/v2/mcp-gateways/{id}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.update_mcp_gateway_request,
                False,
                False,
                "json",
                models.UpdateMcpGatewayRequest,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpGatewayUpdate",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpGateways",
                    "x-cli-name": "update",
                    "x-code-samples": [
                        {
                            "label": "Core - Narrow the tools a gateway exposes",
                            "lang": "curl",
                            "source": 'curl --request PATCH \\\n  --url \'https://api.orq.ai/v2/mcp-gateways/01JQ0M8W4T5YB2Q7N1F6K3PZRC\' \\\n  --header \'Authorization: Bearer $ORQ_API_KEY\' \\\n  --header \'Content-Type: application/json\' \\\n  --data \'{\n    "server_links": [\n      {\n        "mcp_server_id": "01JQ0K5R8N2ZC7X4M9T3V6HWBD",\n        "alias": "github",\n        "enabled": true,\n        "tool_exposure": {\n          "mode": "MCP_TOOL_EXPOSURE_MODE_SELECTED",\n          "tool_ids": [\n            "01JQ0KA2M7VD9E4R6T8Y1U3XSB",\n            "01JQ0KB5P9WF2G7H4J6K8L1NQD"\n          ]\n        }\n      }\n    ]\n  }\'\n',
                        },
                        {
                            "label": "Core - Disable a gateway",
                            "lang": "curl",
                            "source": "curl --request PATCH \\\n  --url 'https://api.orq.ai/v2/mcp-gateways/01JQ0M8W4T5YB2Q7N1F6K3PZRC' \\\n  --header 'Authorization: Bearer $ORQ_API_KEY' \\\n  --header 'Content-Type: application/json' \\\n  --data '{ \"status\": \"MCP_GATEWAY_STATUS_DISABLED\" }'\n",
                        },
                        {
                            "label": "Python - Narrow the tools a gateway exposes",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\n# tool_ids come from McpTool.id. A tool renamed upstream gets a new id\n# on the next sync, which drops it from this allowlist.\nresult = client.mcp_gateways.update(\n    id="01JQ0M8W4T5YB2Q7N1F6K3PZRC",\n    server_links=[\n        {\n            "mcp_server_id": "01JQ0K5R8N2ZC7X4M9T3V6HWBD",\n            "alias": "github",\n            "enabled": True,\n            "tool_exposure": {\n                "mode": "MCP_TOOL_EXPOSURE_MODE_SELECTED",\n                "tool_ids": [\n                    "01JQ0KA2M7VD9E4R6T8Y1U3XSB",\n                    "01JQ0KB5P9WF2G7H4J6K8L1NQD",\n                ],\n            },\n        },\n    ],\n)\n\nprint(result.mcp_gateway.exposed_tools_count)\n',
                        },
                        {
                            "label": "Node.js - Narrow the tools a gateway exposes",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\n// toolIds come from McpTool.id. A tool renamed upstream gets a new id\n// on the next sync, which drops it from this allowlist.\nconst result = await client.mcpGateways.update({\n  id: '01JQ0M8W4T5YB2Q7N1F6K3PZRC',\n  serverLinks: [\n    {\n      mcpServerId: '01JQ0K5R8N2ZC7X4M9T3V6HWBD',\n      alias: 'github',\n      enabled: true,\n      toolExposure: {\n        mode: 'MCP_TOOL_EXPOSURE_MODE_SELECTED',\n        toolIds: [\n          '01JQ0KA2M7VD9E4R6T8Y1U3XSB',\n          '01JQ0KB5P9WF2G7H4J6K8L1NQD',\n        ],\n      },\n    },\n  ],\n});\n\nconsole.log(result.mcpGateway.exposedToolsCount);\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.UpdateMcpGatewayResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    async def update_async(
        self,
        *,
        id: str,
        key: Optional[str] = None,
        display_name: Optional[str] = None,
        description: Optional[str] = None,
        server_links: Optional[
            Union[
                Iterable[models.McpGatewayServerLink],
                Iterable[models.McpGatewayServerLinkTypedDict],
            ]
        ] = None,
        tool_naming: Optional[models.McpToolNaming] = None,
        status: Optional[models.McpGatewayStatus] = None,
        mode: Optional[models.McpGatewayMode] = None,
        sharing: Optional[Union[models.Sharing, models.SharingTypedDict]] = None,
        clear_server_links: Optional[bool] = None,
        plugins: Optional[
            Union[
                Iterable[models.McpGatewayPlugin],
                Iterable[models.McpGatewayPluginTypedDict],
            ]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.UpdateMcpGatewayResponse:
        r"""Update an MCP gateway

        Updates mutable fields of an existing MCP gateway. Omitted optional fields keep their current values.

        :param id: Unique identifier of the MCP gateway.
        :param key: Rejected with INVALID_ARGUMENT: the key backs the gateway's public MCP URL
            and is immutable after creation. Retained so callers get an error rather
            than a silently ignored field.
        :param display_name: Human readable name shown in the workspace.
        :param description: Free-form note about what this gateway is for.
        :param server_links: Replaces the current links. An empty array is treated as no change; use `clear_server_links` to remove them all.
        :param tool_naming:
        :param status:
        :param mode:
        :param sharing: Which projects in the workspace may use this gateway. Defaults to every project.
        :param clear_server_links: Set true to remove every link; cannot be combined with `server_links`.
        :param plugins: Plugins run on every tool call this gateway serves.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.McpGatewayUpdateRequest(
            id=id,
            update_mcp_gateway_request=models.UpdateMcpGatewayRequest(
                key=key,
                display_name=display_name,
                description=description,
                server_links=utils.get_pydantic_model(
                    server_links, Optional[List[models.McpGatewayServerLink]]
                ),
                tool_naming=tool_naming,
                status=status,
                mode=mode,
                sharing=utils.get_pydantic_model(sharing, Optional[models.Sharing]),
                clear_server_links=clear_server_links,
                plugins=utils.get_pydantic_model(
                    plugins, Optional[List[models.McpGatewayPlugin]]
                ),
            ),
        )

        req = self._build_request_async(
            method="PATCH",
            path="/v2/mcp-gateways/{id}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.update_mcp_gateway_request,
                False,
                False,
                "json",
                models.UpdateMcpGatewayRequest,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="McpGatewayUpdate",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["MCP Gateway"],
                extensions={
                    "x-cli-group": "mcpGateways",
                    "x-cli-name": "update",
                    "x-code-samples": [
                        {
                            "label": "Core - Narrow the tools a gateway exposes",
                            "lang": "curl",
                            "source": 'curl --request PATCH \\\n  --url \'https://api.orq.ai/v2/mcp-gateways/01JQ0M8W4T5YB2Q7N1F6K3PZRC\' \\\n  --header \'Authorization: Bearer $ORQ_API_KEY\' \\\n  --header \'Content-Type: application/json\' \\\n  --data \'{\n    "server_links": [\n      {\n        "mcp_server_id": "01JQ0K5R8N2ZC7X4M9T3V6HWBD",\n        "alias": "github",\n        "enabled": true,\n        "tool_exposure": {\n          "mode": "MCP_TOOL_EXPOSURE_MODE_SELECTED",\n          "tool_ids": [\n            "01JQ0KA2M7VD9E4R6T8Y1U3XSB",\n            "01JQ0KB5P9WF2G7H4J6K8L1NQD"\n          ]\n        }\n      }\n    ]\n  }\'\n',
                        },
                        {
                            "label": "Core - Disable a gateway",
                            "lang": "curl",
                            "source": "curl --request PATCH \\\n  --url 'https://api.orq.ai/v2/mcp-gateways/01JQ0M8W4T5YB2Q7N1F6K3PZRC' \\\n  --header 'Authorization: Bearer $ORQ_API_KEY' \\\n  --header 'Content-Type: application/json' \\\n  --data '{ \"status\": \"MCP_GATEWAY_STATUS_DISABLED\" }'\n",
                        },
                        {
                            "label": "Python - Narrow the tools a gateway exposes",
                            "lang": "python",
                            "source": 'import os\nfrom orq_ai_sdk import Orq\n\nclient = Orq(api_key=os.environ["ORQ_API_KEY"])\n\n# tool_ids come from McpTool.id. A tool renamed upstream gets a new id\n# on the next sync, which drops it from this allowlist.\nresult = client.mcp_gateways.update(\n    id="01JQ0M8W4T5YB2Q7N1F6K3PZRC",\n    server_links=[\n        {\n            "mcp_server_id": "01JQ0K5R8N2ZC7X4M9T3V6HWBD",\n            "alias": "github",\n            "enabled": True,\n            "tool_exposure": {\n                "mode": "MCP_TOOL_EXPOSURE_MODE_SELECTED",\n                "tool_ids": [\n                    "01JQ0KA2M7VD9E4R6T8Y1U3XSB",\n                    "01JQ0KB5P9WF2G7H4J6K8L1NQD",\n                ],\n            },\n        },\n    ],\n)\n\nprint(result.mcp_gateway.exposed_tools_count)\n',
                        },
                        {
                            "label": "Node.js - Narrow the tools a gateway exposes",
                            "lang": "typescript",
                            "source": "import { Orq } from '@orq-ai/node';\n\nconst client = new Orq({\n  apiKey: process.env.ORQ_API_KEY,\n});\n\n// toolIds come from McpTool.id. A tool renamed upstream gets a new id\n// on the next sync, which drops it from this allowlist.\nconst result = await client.mcpGateways.update({\n  id: '01JQ0M8W4T5YB2Q7N1F6K3PZRC',\n  serverLinks: [\n    {\n      mcpServerId: '01JQ0K5R8N2ZC7X4M9T3V6HWBD',\n      alias: 'github',\n      enabled: true,\n      toolExposure: {\n        mode: 'MCP_TOOL_EXPOSURE_MODE_SELECTED',\n        toolIds: [\n          '01JQ0KA2M7VD9E4R6T8Y1U3XSB',\n          '01JQ0KB5P9WF2G7H4J6K8L1NQD',\n        ],\n      },\n    },\n  ],\n});\n\nconsole.log(result.mcpGateway.exposedToolsCount);\n",
                        },
                    ],
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.UpdateMcpGatewayResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)
