"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class StructuredToolCallTypedDict(TypedDict):
    r"""StructuredToolCall mirrors one tool invocation and its result. Name is the
    only field the grader requires; a call without one is dropped.
    """

    name: NotRequired[str]
    arguments: NotRequired[str]
    output: NotRequired[str]


class StructuredToolCall(BaseModel):
    r"""StructuredToolCall mirrors one tool invocation and its result. Name is the
    only field the grader requires; a call without one is dropped.
    """

    name: Optional[str] = None

    arguments: Optional[str] = None

    output: Optional[str] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["name", "arguments", "output"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
