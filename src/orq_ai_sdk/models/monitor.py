"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .monitorwidget import MonitorWidget, MonitorWidgetTypedDict
from datetime import datetime
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import List, Literal, Optional
from typing_extensions import NotRequired, TypedDict


DefaultRange = Literal[
    "15m",
    "1h",
    "4h",
    "1d",
    "3d",
    "7d",
    "14d",
    "30d",
    "90d",
]
r"""Default relative time range applied when the monitor opens."""


class MonitorTypedDict(TypedDict):
    monitor_id: str
    r"""Unique monitor identifier assigned by ORQ, for example
    `monitor_01H...`. Use this value in retrieve, update, and delete
    requests.
    """
    display_name: str
    r"""Workspace-unique display name."""
    description: str
    r"""Short human-readable summary of what the monitor tracks."""
    project_id: str
    r"""Project that owns the monitor. Empty for workspace-wide monitors (admin-managed). Immutable after creation."""
    widgets: List[MonitorWidgetTypedDict]
    r"""Widgets rendered on the monitor, each carrying its own query and
    layout position.
    """
    preset_key: str
    r"""Preset this monitor was created from. Empty when built from scratch."""
    created_at: datetime
    r"""Time when the monitor was created."""
    updated_at: datetime
    r"""Time when the monitor was last updated."""
    created_by_id: str
    r"""User ID that created the monitor."""
    updated_by_id: str
    r"""User ID that last updated the monitor."""
    default_range: NotRequired[DefaultRange]
    r"""Default relative time range applied when the monitor opens."""


class Monitor(BaseModel):
    monitor_id: str
    r"""Unique monitor identifier assigned by ORQ, for example
    `monitor_01H...`. Use this value in retrieve, update, and delete
    requests.
    """

    display_name: str
    r"""Workspace-unique display name."""

    description: str
    r"""Short human-readable summary of what the monitor tracks."""

    project_id: str
    r"""Project that owns the monitor. Empty for workspace-wide monitors (admin-managed). Immutable after creation."""

    widgets: List[MonitorWidget]
    r"""Widgets rendered on the monitor, each carrying its own query and
    layout position.
    """

    preset_key: str
    r"""Preset this monitor was created from. Empty when built from scratch."""

    created_at: datetime
    r"""Time when the monitor was created."""

    updated_at: datetime
    r"""Time when the monitor was last updated."""

    created_by_id: str
    r"""User ID that created the monitor."""

    updated_by_id: str
    r"""User ID that last updated the monitor."""

    default_range: Optional[DefaultRange] = None
    r"""Default relative time range applied when the monitor opens."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["default_range"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
