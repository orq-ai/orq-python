"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from datetime import datetime
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Literal, Optional
from typing_extensions import NotRequired, TypedDict


Severity = Literal[
    "critical",
    "degraded",
]
r"""Tier the value landed in when breached. Empty when not breached."""


class AlertRunTypedDict(TypedDict):
    at: datetime
    r"""Time of the evaluation tick."""
    value: float
    r"""Observed metric value. Zero when `has_data` is false."""
    breached: bool
    r"""Whether the value breached the alert condition."""
    has_data: bool
    r"""Whether the evaluation window contained any data."""
    severity: NotRequired[Severity]
    r"""Tier the value landed in when breached. Empty when not breached."""


class AlertRun(BaseModel):
    at: datetime
    r"""Time of the evaluation tick."""

    value: float
    r"""Observed metric value. Zero when `has_data` is false."""

    breached: bool
    r"""Whether the value breached the alert condition."""

    has_data: bool
    r"""Whether the evaluation window contained any data."""

    severity: Optional[Severity] = None
    r"""Tier the value landed in when breached. Empty when not breached."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["severity"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
