"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .syncstatus import SyncStatus
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class McpSyncStateTypedDict(TypedDict):
    status: NotRequired[SyncStatus]
    tools_total: NotRequired[int]
    r"""Tools in the catalog after the last sync."""
    tools_added: NotRequired[int]
    r"""Tools the last sync discovered for the first time."""
    tools_removed: NotRequired[int]
    r"""Tools the last sync no longer found upstream and marked MISSING."""
    last_synced_at: NotRequired[str]
    r"""ISO 8601 timestamp of the last sync attempt."""
    errors: NotRequired[List[str]]
    r"""Failures reported by the last sync."""


class McpSyncState(BaseModel):
    status: Optional[SyncStatus] = None

    tools_total: Optional[int] = None
    r"""Tools in the catalog after the last sync."""

    tools_added: Optional[int] = None
    r"""Tools the last sync discovered for the first time."""

    tools_removed: Optional[int] = None
    r"""Tools the last sync no longer found upstream and marked MISSING."""

    last_synced_at: Optional[str] = None
    r"""ISO 8601 timestamp of the last sync attempt."""

    errors: Optional[List[str]] = None
    r"""Failures reported by the last sync."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "status",
                "tools_total",
                "tools_added",
                "tools_removed",
                "last_synced_at",
                "errors",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
