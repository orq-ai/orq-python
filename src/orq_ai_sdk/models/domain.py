"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .domaingroup import DomainGroup
from .scopemode import ScopeMode
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class DomainTypedDict(TypedDict):
    r"""Domain describes a permission domain that can be granted to an API
    key. The capability catalog is the registry consulted by
    ResolvePermissions() at authorize() time. Verbs are derived by
    convention from `id` + `group` + `readable` / `writable` flags.
    """

    id: NotRequired[str]
    r"""Stable domain identifier (e.g. \"agent\", \"chat_completions\"). Used
    as the key in ApiKey.access and as the verb prefix in resolved
    permissions (e.g. agent.list, agent.view, agent.create).
    """
    display_name: NotRequired[str]
    r"""Human-readable label for UI."""
    group: NotRequired[DomainGroup]
    allowed_scopes: NotRequired[List[ScopeMode]]
    r"""Project scopes this domain may be granted under. A workspace-admin
    domain like `member` is typically SCOPE_MODE_ALL only.
    """
    readable: NotRequired[bool]
    r"""Whether this domain can be granted read access. When false the UI
    disables the Read segment (e.g. write-only gateway endpoints).
    """
    writable: NotRequired[bool]
    r"""Whether this domain can be granted write access. When false the UI
    disables the Write segment (e.g. read-only `model` listing).
    """


class Domain(BaseModel):
    r"""Domain describes a permission domain that can be granted to an API
    key. The capability catalog is the registry consulted by
    ResolvePermissions() at authorize() time. Verbs are derived by
    convention from `id` + `group` + `readable` / `writable` flags.
    """

    id: Optional[str] = None
    r"""Stable domain identifier (e.g. \"agent\", \"chat_completions\"). Used
    as the key in ApiKey.access and as the verb prefix in resolved
    permissions (e.g. agent.list, agent.view, agent.create).
    """

    display_name: Optional[str] = None
    r"""Human-readable label for UI."""

    group: Optional[DomainGroup] = None

    allowed_scopes: Optional[List[ScopeMode]] = None
    r"""Project scopes this domain may be granted under. A workspace-admin
    domain like `member` is typically SCOPE_MODE_ALL only.
    """

    readable: Optional[bool] = None
    r"""Whether this domain can be granted read access. When false the UI
    disables the Read segment (e.g. write-only gateway endpoints).
    """

    writable: Optional[bool] = None
    r"""Whether this domain can be granted write access. When false the UI
    disables the Write segment (e.g. read-only `model` listing).
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["id", "display_name", "group", "allowed_scopes", "readable", "writable"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
