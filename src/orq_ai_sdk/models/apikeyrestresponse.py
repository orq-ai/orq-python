"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from datetime import datetime
from orq_ai_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
import pydantic
from pydantic import ConfigDict, model_serializer
from typing import Any, Dict, List, Optional
from typing_extensions import NotRequired, TypedDict


class APIKeyRestResponseTypedDict(TypedDict):
    id: str
    name: str
    workspace_id: str
    token: str
    r"""Raw on create/retrieve and masked in list responses."""
    active: NotRequired[bool]
    is_legacy: NotRequired[bool]
    source: NotRequired[str]
    budget: NotRequired[Dict[str, Any]]
    expiration: NotRequired[datetime]
    projects: NotRequired[List[str]]
    created_by_id: NotRequired[Nullable[str]]
    updated_by_id: NotRequired[Nullable[str]]
    created: NotRequired[datetime]
    updated: NotRequired[datetime]
    consumption: NotRequired[float]
    token_consumption: NotRequired[float]
    requests_per_minute_consumption: NotRequired[float]
    owner: NotRequired[Dict[str, Any]]
    project_scope: NotRequired[Dict[str, Any]]
    permission_mode: NotRequired[str]
    access: NotRequired[Dict[str, str]]
    token_prefix: NotRequired[str]
    status: NotRequired[str]
    constraints: NotRequired[Dict[str, Any]]
    mcp_access: NotRequired[Dict[str, Any]]
    legacy_token_family: NotRequired[str]
    legacy_key_id: NotRequired[str]
    last_used_at: NotRequired[datetime]


class APIKeyRestResponse(BaseModel):
    model_config = ConfigDict(
        populate_by_name=True, arbitrary_types_allowed=True, extra="allow"
    )
    __pydantic_extra__: Dict[str, Any] = pydantic.Field(init=False)

    id: str

    name: str

    workspace_id: str

    token: str
    r"""Raw on create/retrieve and masked in list responses."""

    active: Optional[bool] = None

    is_legacy: Optional[bool] = None

    source: Optional[str] = None

    budget: Optional[Dict[str, Any]] = None

    expiration: Optional[datetime] = None

    projects: Optional[List[str]] = None

    created_by_id: OptionalNullable[str] = UNSET

    updated_by_id: OptionalNullable[str] = UNSET

    created: Optional[datetime] = None

    updated: Optional[datetime] = None

    consumption: Optional[float] = None

    token_consumption: Optional[float] = None

    requests_per_minute_consumption: Optional[float] = None

    owner: Optional[Dict[str, Any]] = None

    project_scope: Optional[Dict[str, Any]] = None

    permission_mode: Optional[str] = None

    access: Optional[Dict[str, str]] = None

    token_prefix: Optional[str] = None

    status: Optional[str] = None

    constraints: Optional[Dict[str, Any]] = None

    mcp_access: Optional[Dict[str, Any]] = None

    legacy_token_family: Optional[str] = None

    legacy_key_id: Optional[str] = None

    last_used_at: Optional[datetime] = None

    @property
    def additional_properties(self):
        return self.__pydantic_extra__

    @additional_properties.setter
    def additional_properties(self, value):
        self.__pydantic_extra__ = value  # pyright: ignore[reportIncompatibleVariableOverride]

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "active",
                "is_legacy",
                "source",
                "budget",
                "expiration",
                "projects",
                "created_by_id",
                "updated_by_id",
                "created",
                "updated",
                "consumption",
                "token_consumption",
                "requests_per_minute_consumption",
                "owner",
                "project_scope",
                "permission_mode",
                "access",
                "token_prefix",
                "status",
                "constraints",
                "mcp_access",
                "legacy_token_family",
                "legacy_key_id",
                "last_used_at",
            ]
        )
        nullable_fields = set(["created_by_id", "updated_by_id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            serialized.pop(k, serialized.pop(n, None))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val
        for k, v in serialized.items():
            m[k] = v

        return m
