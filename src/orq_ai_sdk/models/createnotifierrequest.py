"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .notifiertype import NotifierType
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import List, Optional, Union
from typing_extensions import NotRequired, TypeAliasType, TypedDict


class CreateNotifierRequest3MetadataTypedDict(TypedDict):
    r"""Optional. Custom JSON metadata stored with the notifier."""


class CreateNotifierRequest3Metadata(BaseModel):
    r"""Optional. Custom JSON metadata stored with the notifier."""


class CreateNotifierRequest3HeadersTypedDict(TypedDict):
    r"""Optional generic webhook headers. Each value may be a string or `{ \"secret\": true, \"value\": \"...\" }`."""


class CreateNotifierRequest3Headers(BaseModel):
    r"""Optional generic webhook headers. Each value may be a string or `{ \"secret\": true, \"value\": \"...\" }`."""


class GenericWebhookNotifierCreateRequestTypedDict(TypedDict):
    r"""`webhook_url` is required when `type` is `NOTIFIER_TYPE_WEBHOOK`; `headers` is optional."""

    type: NotifierType
    webhook_url: str
    r"""Required when `type` is `NOTIFIER_TYPE_WEBHOOK`. Omit for other notifier types."""
    display_name: str
    r"""Required. Human-readable notifier name."""
    project_id: NotRequired[str]
    r"""Optional. Workspace-scoped callers may set this to create a project-scoped notifier, or omit it for a workspace-wide notifier. Project-scoped API keys create in the API key's project."""
    metadata: NotRequired[CreateNotifierRequest3MetadataTypedDict]
    r"""Optional. Custom JSON metadata stored with the notifier."""
    emails: NotRequired[List[str]]
    r"""Required when `type` is `NOTIFIER_TYPE_EMAIL`. Omit for other notifier types."""
    incoming_webhook_url: NotRequired[str]
    r"""Required when `type` is `NOTIFIER_TYPE_SLACK_WEBHOOK`. Omit for other notifier types."""
    headers: NotRequired[CreateNotifierRequest3HeadersTypedDict]
    r"""Optional generic webhook headers. Each value may be a string or `{ \"secret\": true, \"value\": \"...\" }`."""


class GenericWebhookNotifierCreateRequest(BaseModel):
    r"""`webhook_url` is required when `type` is `NOTIFIER_TYPE_WEBHOOK`; `headers` is optional."""

    type: NotifierType

    webhook_url: str
    r"""Required when `type` is `NOTIFIER_TYPE_WEBHOOK`. Omit for other notifier types."""

    display_name: str
    r"""Required. Human-readable notifier name."""

    project_id: Optional[str] = None
    r"""Optional. Workspace-scoped callers may set this to create a project-scoped notifier, or omit it for a workspace-wide notifier. Project-scoped API keys create in the API key's project."""

    metadata: Optional[CreateNotifierRequest3Metadata] = None
    r"""Optional. Custom JSON metadata stored with the notifier."""

    emails: Optional[List[str]] = None
    r"""Required when `type` is `NOTIFIER_TYPE_EMAIL`. Omit for other notifier types."""

    incoming_webhook_url: Optional[str] = None
    r"""Required when `type` is `NOTIFIER_TYPE_SLACK_WEBHOOK`. Omit for other notifier types."""

    headers: Optional[CreateNotifierRequest3Headers] = None
    r"""Optional generic webhook headers. Each value may be a string or `{ \"secret\": true, \"value\": \"...\" }`."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["project_id", "metadata", "emails", "incoming_webhook_url", "headers"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class CreateNotifierRequest2MetadataTypedDict(TypedDict):
    r"""Optional. Custom JSON metadata stored with the notifier."""


class CreateNotifierRequest2Metadata(BaseModel):
    r"""Optional. Custom JSON metadata stored with the notifier."""


class CreateNotifierRequest2HeadersTypedDict(TypedDict):
    r"""Optional generic webhook headers. Each value may be a string or `{ \"secret\": true, \"value\": \"...\" }`."""


class CreateNotifierRequest2Headers(BaseModel):
    r"""Optional generic webhook headers. Each value may be a string or `{ \"secret\": true, \"value\": \"...\" }`."""


class SlackWebhookNotifierCreateRequestTypedDict(TypedDict):
    r"""`incoming_webhook_url` is required when `type` is `NOTIFIER_TYPE_SLACK_WEBHOOK`."""

    type: NotifierType
    incoming_webhook_url: str
    r"""Required when `type` is `NOTIFIER_TYPE_SLACK_WEBHOOK`. Omit for other notifier types."""
    display_name: str
    r"""Required. Human-readable notifier name."""
    project_id: NotRequired[str]
    r"""Optional. Workspace-scoped callers may set this to create a project-scoped notifier, or omit it for a workspace-wide notifier. Project-scoped API keys create in the API key's project."""
    metadata: NotRequired[CreateNotifierRequest2MetadataTypedDict]
    r"""Optional. Custom JSON metadata stored with the notifier."""
    emails: NotRequired[List[str]]
    r"""Required when `type` is `NOTIFIER_TYPE_EMAIL`. Omit for other notifier types."""
    webhook_url: NotRequired[str]
    r"""Required when `type` is `NOTIFIER_TYPE_WEBHOOK`. Omit for other notifier types."""
    headers: NotRequired[CreateNotifierRequest2HeadersTypedDict]
    r"""Optional generic webhook headers. Each value may be a string or `{ \"secret\": true, \"value\": \"...\" }`."""


class SlackWebhookNotifierCreateRequest(BaseModel):
    r"""`incoming_webhook_url` is required when `type` is `NOTIFIER_TYPE_SLACK_WEBHOOK`."""

    type: NotifierType

    incoming_webhook_url: str
    r"""Required when `type` is `NOTIFIER_TYPE_SLACK_WEBHOOK`. Omit for other notifier types."""

    display_name: str
    r"""Required. Human-readable notifier name."""

    project_id: Optional[str] = None
    r"""Optional. Workspace-scoped callers may set this to create a project-scoped notifier, or omit it for a workspace-wide notifier. Project-scoped API keys create in the API key's project."""

    metadata: Optional[CreateNotifierRequest2Metadata] = None
    r"""Optional. Custom JSON metadata stored with the notifier."""

    emails: Optional[List[str]] = None
    r"""Required when `type` is `NOTIFIER_TYPE_EMAIL`. Omit for other notifier types."""

    webhook_url: Optional[str] = None
    r"""Required when `type` is `NOTIFIER_TYPE_WEBHOOK`. Omit for other notifier types."""

    headers: Optional[CreateNotifierRequest2Headers] = None
    r"""Optional generic webhook headers. Each value may be a string or `{ \"secret\": true, \"value\": \"...\" }`."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["project_id", "metadata", "emails", "webhook_url", "headers"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class CreateNotifierRequestMetadataTypedDict(TypedDict):
    r"""Optional. Custom JSON metadata stored with the notifier."""


class CreateNotifierRequestMetadata(BaseModel):
    r"""Optional. Custom JSON metadata stored with the notifier."""


class CreateNotifierRequestHeadersTypedDict(TypedDict):
    r"""Optional generic webhook headers. Each value may be a string or `{ \"secret\": true, \"value\": \"...\" }`."""


class CreateNotifierRequestHeaders(BaseModel):
    r"""Optional generic webhook headers. Each value may be a string or `{ \"secret\": true, \"value\": \"...\" }`."""


class EmailNotifierCreateRequestTypedDict(TypedDict):
    r"""`emails` is required when `type` is `NOTIFIER_TYPE_EMAIL`."""

    type: NotifierType
    emails: List[str]
    r"""Required when `type` is `NOTIFIER_TYPE_EMAIL`. Omit for other notifier types."""
    display_name: str
    r"""Required. Human-readable notifier name."""
    project_id: NotRequired[str]
    r"""Optional. Workspace-scoped callers may set this to create a project-scoped notifier, or omit it for a workspace-wide notifier. Project-scoped API keys create in the API key's project."""
    metadata: NotRequired[CreateNotifierRequestMetadataTypedDict]
    r"""Optional. Custom JSON metadata stored with the notifier."""
    incoming_webhook_url: NotRequired[str]
    r"""Required when `type` is `NOTIFIER_TYPE_SLACK_WEBHOOK`. Omit for other notifier types."""
    webhook_url: NotRequired[str]
    r"""Required when `type` is `NOTIFIER_TYPE_WEBHOOK`. Omit for other notifier types."""
    headers: NotRequired[CreateNotifierRequestHeadersTypedDict]
    r"""Optional generic webhook headers. Each value may be a string or `{ \"secret\": true, \"value\": \"...\" }`."""


class EmailNotifierCreateRequest(BaseModel):
    r"""`emails` is required when `type` is `NOTIFIER_TYPE_EMAIL`."""

    type: NotifierType

    emails: List[str]
    r"""Required when `type` is `NOTIFIER_TYPE_EMAIL`. Omit for other notifier types."""

    display_name: str
    r"""Required. Human-readable notifier name."""

    project_id: Optional[str] = None
    r"""Optional. Workspace-scoped callers may set this to create a project-scoped notifier, or omit it for a workspace-wide notifier. Project-scoped API keys create in the API key's project."""

    metadata: Optional[CreateNotifierRequestMetadata] = None
    r"""Optional. Custom JSON metadata stored with the notifier."""

    incoming_webhook_url: Optional[str] = None
    r"""Required when `type` is `NOTIFIER_TYPE_SLACK_WEBHOOK`. Omit for other notifier types."""

    webhook_url: Optional[str] = None
    r"""Required when `type` is `NOTIFIER_TYPE_WEBHOOK`. Omit for other notifier types."""

    headers: Optional[CreateNotifierRequestHeaders] = None
    r"""Optional generic webhook headers. Each value may be a string or `{ \"secret\": true, \"value\": \"...\" }`."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["project_id", "metadata", "incoming_webhook_url", "webhook_url", "headers"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


CreateNotifierRequestTypedDict = TypeAliasType(
    "CreateNotifierRequestTypedDict",
    Union[
        EmailNotifierCreateRequestTypedDict,
        SlackWebhookNotifierCreateRequestTypedDict,
        GenericWebhookNotifierCreateRequestTypedDict,
    ],
)
r"""Create notifier request. `project_id`, `display_name`, and `type` are always required. The destination field required by `type` is captured in `oneOf`."""


CreateNotifierRequest = TypeAliasType(
    "CreateNotifierRequest",
    Union[
        EmailNotifierCreateRequest,
        SlackWebhookNotifierCreateRequest,
        GenericWebhookNotifierCreateRequest,
    ],
)
r"""Create notifier request. `project_id`, `display_name`, and `type` are always required. The destination field required by `type` is captured in `oneOf`."""
