"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from orq_ai_sdk.utils import FieldMetadata, QueryParamMetadata
from pydantic import model_serializer
from typing import Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class WebhookListRequestTypedDict(TypedDict):
    page: NotRequired[int]
    r"""One-based page number. Defaults to 1 and must be between 1 and 1,000,000."""
    limit: NotRequired[int]
    r"""Number of webhooks to return per page. Defaults to 20 and must be between
    1 and 200.
    """
    search: NotRequired[str]
    r"""Optional case-insensitive substring matched against display name, content
    type, and URL. Maximum length is 200 characters.
    """
    event: NotRequired[str]
    r"""Optional event slug that must be present in the webhook's event
    subscriptions, for example `llm.response`. Maximum length is 200
    characters.
    """
    sort: NotRequired[str]
    r"""Field used to order results. Allowed values are `created`, `updated`, and
    `display_name`. Defaults to `created`.
    """
    direction: NotRequired[str]
    r"""Sort direction. Allowed values are `asc` and `desc`. Defaults to `desc`."""
    content_type: NotRequired[str]
    r"""Optional comma-separated content types to match, for example
    `application/json,application/x-www-form-urlencoded`. A webhook matches
    when its content type equals any supplied value.
    """
    enabled: NotRequired[bool]
    r"""Optional delivery status filter. When omitted, enabled and disabled
    webhooks are returned.
    """


class WebhookListRequest(BaseModel):
    page: Annotated[
        Optional[int],
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = None
    r"""One-based page number. Defaults to 1 and must be between 1 and 1,000,000."""

    limit: Annotated[
        Optional[int],
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = None
    r"""Number of webhooks to return per page. Defaults to 20 and must be between
    1 and 200.
    """

    search: Annotated[
        Optional[str],
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = None
    r"""Optional case-insensitive substring matched against display name, content
    type, and URL. Maximum length is 200 characters.
    """

    event: Annotated[
        Optional[str],
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = None
    r"""Optional event slug that must be present in the webhook's event
    subscriptions, for example `llm.response`. Maximum length is 200
    characters.
    """

    sort: Annotated[
        Optional[str],
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = None
    r"""Field used to order results. Allowed values are `created`, `updated`, and
    `display_name`. Defaults to `created`.
    """

    direction: Annotated[
        Optional[str],
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = None
    r"""Sort direction. Allowed values are `asc` and `desc`. Defaults to `desc`."""

    content_type: Annotated[
        Optional[str],
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = None
    r"""Optional comma-separated content types to match, for example
    `application/json,application/x-www-form-urlencoded`. A webhook matches
    when its content type equals any supplied value.
    """

    enabled: Annotated[
        Optional[bool],
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = None
    r"""Optional delivery status filter. When omitted, enabled and disabled
    webhooks are returned.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "page",
                "limit",
                "search",
                "event",
                "sort",
                "direction",
                "content_type",
                "enabled",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
