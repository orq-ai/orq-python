"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .knowledgemetadata import KnowledgeMetadata, KnowledgeMetadataTypedDict
from .knowledgesettings import KnowledgeSettings, KnowledgeSettingsTypedDict
from orq_ai_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
import pydantic
from pydantic import model_serializer
from typing import Literal, Optional, Union
from typing_extensions import Annotated, NotRequired, TypeAliasType, TypedDict


Knowledge2Type = Literal["external",]


class Knowledge2RerankConfigTypedDict(TypedDict):
    r"""The rerank configuration for the knowledge base. In case the model is provided it will be used to enhance the search precision."""

    rerank_model: str
    r"""The rerank model to use for the knowledge base."""
    top_k: NotRequired[int]
    r"""The number of results to return by the reranking model"""
    rerank_threshold: NotRequired[float]
    r"""The threshold value used to filter the rerank results, only documents with a relevance score greater than the threshold will be returned"""


class Knowledge2RerankConfig(BaseModel):
    r"""The rerank configuration for the knowledge base. In case the model is provided it will be used to enhance the search precision."""

    rerank_model: str
    r"""The rerank model to use for the knowledge base."""

    top_k: Optional[int] = 5
    r"""The number of results to return by the reranking model"""

    rerank_threshold: Optional[float] = 0.5
    r"""The threshold value used to filter the rerank results, only documents with a relevance score greater than the threshold will be returned"""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["top_k", "rerank_threshold"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class Knowledge2AgenticRagConfigTypedDict(TypedDict):
    r"""The Agentic RAG configuration for the knowledge base. If `null` is provided, Agentic RAG will be disabled."""

    model: str
    r"""The model to use for the Agentic RAG"""


class Knowledge2AgenticRagConfig(BaseModel):
    r"""The Agentic RAG configuration for the knowledge base. If `null` is provided, Agentic RAG will be disabled."""

    model: str
    r"""The model to use for the Agentic RAG"""


class KnowledgeRetrievalSettingsTypedDict(TypedDict):
    r"""The retrieval settings for the knowledge base."""

    top_k: NotRequired[int]
    r"""The number of results to return from the search."""
    threshold: NotRequired[float]
    r"""The threshold value used to filter the search results, only documents with a relevance score greater than the threshold will be returned"""
    rerank_config: NotRequired[Nullable[Knowledge2RerankConfigTypedDict]]
    r"""The rerank configuration for the knowledge base. In case the model is provided it will be used to enhance the search precision."""
    agentic_rag_config: NotRequired[Nullable[Knowledge2AgenticRagConfigTypedDict]]
    r"""The Agentic RAG configuration for the knowledge base. If `null` is provided, Agentic RAG will be disabled."""


class KnowledgeRetrievalSettings(BaseModel):
    r"""The retrieval settings for the knowledge base."""

    top_k: Optional[int] = 5
    r"""The number of results to return from the search."""

    threshold: Optional[float] = 0
    r"""The threshold value used to filter the search results, only documents with a relevance score greater than the threshold will be returned"""

    rerank_config: OptionalNullable[Knowledge2RerankConfig] = UNSET
    r"""The rerank configuration for the knowledge base. In case the model is provided it will be used to enhance the search precision."""

    agentic_rag_config: OptionalNullable[Knowledge2AgenticRagConfig] = UNSET
    r"""The Agentic RAG configuration for the knowledge base. If `null` is provided, Agentic RAG will be disabled."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["top_k", "threshold", "rerank_config", "agentic_rag_config"]
        )
        nullable_fields = set(["rerank_config", "agentic_rag_config"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class ExternalConfigTypedDict(TypedDict):
    name: str
    r"""The name of the external knowledge base."""
    api_url: str
    r"""The API URL of the external knowledge base."""


class ExternalConfig(BaseModel):
    name: str
    r"""The name of the external knowledge base."""

    api_url: str
    r"""The API URL of the external knowledge base."""


class Knowledge2TypedDict(TypedDict):
    id: str
    r"""The unique identifier of the knowledge base."""
    created: str
    r"""The creation date of the knowledge base."""
    key: str
    r"""The unique key of the knowledge base."""
    domain_id: str
    r"""The project/domain ID of the knowledge base."""
    updated: str
    r"""The last update date of the knowledge base."""
    external_config: ExternalConfigTypedDict
    description: NotRequired[Nullable[str]]
    r"""The description of the knowledge base."""
    path: NotRequired[str]
    r"""Entity storage path.

    With workspace-level API keys, use the format `project/folder/subfolder/...`. The first element identifies the project, followed by nested folders (auto-created as needed). Example: `Default/agents`.

    With project-level API keys, the project is predetermined by the API key, so the path is relative to that project. Example: `agents`. For backward compatibility, a leading project name is ignored when it matches the scoped project.
    """
    created_by_id: NotRequired[Nullable[str]]
    updated_by_id: NotRequired[Nullable[str]]
    type: NotRequired[Knowledge2Type]
    retrieval_settings: NotRequired[KnowledgeRetrievalSettingsTypedDict]
    r"""The retrieval settings for the knowledge base."""
    settings: NotRequired[KnowledgeSettingsTypedDict]
    r"""Additive compatibility fields formerly available only from private internal endpoints."""
    metadata: NotRequired[KnowledgeMetadataTypedDict]


class Knowledge2(BaseModel):
    id: Annotated[str, pydantic.Field(alias="_id")]
    r"""The unique identifier of the knowledge base."""

    created: str
    r"""The creation date of the knowledge base."""

    key: str
    r"""The unique key of the knowledge base."""

    domain_id: str
    r"""The project/domain ID of the knowledge base."""

    updated: str
    r"""The last update date of the knowledge base."""

    external_config: ExternalConfig

    description: OptionalNullable[str] = UNSET
    r"""The description of the knowledge base."""

    path: Optional[str] = None
    r"""Entity storage path.

    With workspace-level API keys, use the format `project/folder/subfolder/...`. The first element identifies the project, followed by nested folders (auto-created as needed). Example: `Default/agents`.

    With project-level API keys, the project is predetermined by the API key, so the path is relative to that project. Example: `agents`. For backward compatibility, a leading project name is ignored when it matches the scoped project.
    """

    created_by_id: OptionalNullable[str] = UNSET

    updated_by_id: OptionalNullable[str] = UNSET

    type: Optional[Knowledge2Type] = "external"

    retrieval_settings: Optional[KnowledgeRetrievalSettings] = None
    r"""The retrieval settings for the knowledge base."""

    settings: Optional[KnowledgeSettings] = None
    r"""Additive compatibility fields formerly available only from private internal endpoints."""

    metadata: Optional[KnowledgeMetadata] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "description",
                "path",
                "created_by_id",
                "updated_by_id",
                "type",
                "retrieval_settings",
                "settings",
                "metadata",
            ]
        )
        nullable_fields = set(["description", "created_by_id", "updated_by_id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


KnowledgeType = Literal["internal",]


RetrievalType = Literal[
    "vector_search",
    "keyword_search",
    "hybrid_search",
]
r"""The retrieval type to use for the knowledge base. If not provided, Hybrid Search will be used as a default query strategy."""


class KnowledgeRerankConfigTypedDict(TypedDict):
    r"""The rerank configuration for the knowledge base. In case the model is provided it will be used to enhance the search precision."""

    rerank_model: str
    r"""The rerank model to use for the knowledge base."""
    top_k: NotRequired[int]
    r"""The number of results to return by the reranking model"""
    rerank_threshold: NotRequired[float]
    r"""The threshold value used to filter the rerank results, only documents with a relevance score greater than the threshold will be returned"""


class KnowledgeRerankConfig(BaseModel):
    r"""The rerank configuration for the knowledge base. In case the model is provided it will be used to enhance the search precision."""

    rerank_model: str
    r"""The rerank model to use for the knowledge base."""

    top_k: Optional[int] = 5
    r"""The number of results to return by the reranking model"""

    rerank_threshold: Optional[float] = 0.5
    r"""The threshold value used to filter the rerank results, only documents with a relevance score greater than the threshold will be returned"""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["top_k", "rerank_threshold"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class KnowledgeAgenticRagConfigTypedDict(TypedDict):
    r"""The Agentic RAG configuration for the knowledge base. If `null` is provided, Agentic RAG will be disabled."""

    model: str
    r"""The model to use for the Agentic RAG"""


class KnowledgeAgenticRagConfig(BaseModel):
    r"""The Agentic RAG configuration for the knowledge base. If `null` is provided, Agentic RAG will be disabled."""

    model: str
    r"""The model to use for the Agentic RAG"""


class RetrievalSettingsTypedDict(TypedDict):
    r"""The retrieval settings for the knowledge base. If not provider, Hybrid Search will be used as a default query strategy."""

    retrieval_type: NotRequired[RetrievalType]
    r"""The retrieval type to use for the knowledge base. If not provided, Hybrid Search will be used as a default query strategy."""
    top_k: NotRequired[int]
    r"""The number of results to return from the search."""
    threshold: NotRequired[float]
    r"""The threshold value used to filter the search results, only documents with a relevance score greater than the threshold will be returned"""
    rerank_config: NotRequired[Nullable[KnowledgeRerankConfigTypedDict]]
    r"""The rerank configuration for the knowledge base. In case the model is provided it will be used to enhance the search precision."""
    agentic_rag_config: NotRequired[Nullable[KnowledgeAgenticRagConfigTypedDict]]
    r"""The Agentic RAG configuration for the knowledge base. If `null` is provided, Agentic RAG will be disabled."""


class RetrievalSettings(BaseModel):
    r"""The retrieval settings for the knowledge base. If not provider, Hybrid Search will be used as a default query strategy."""

    retrieval_type: Optional[RetrievalType] = "hybrid_search"
    r"""The retrieval type to use for the knowledge base. If not provided, Hybrid Search will be used as a default query strategy."""

    top_k: Optional[int] = 5
    r"""The number of results to return from the search."""

    threshold: Optional[float] = 0
    r"""The threshold value used to filter the search results, only documents with a relevance score greater than the threshold will be returned"""

    rerank_config: OptionalNullable[KnowledgeRerankConfig] = UNSET
    r"""The rerank configuration for the knowledge base. In case the model is provided it will be used to enhance the search precision."""

    agentic_rag_config: OptionalNullable[KnowledgeAgenticRagConfig] = UNSET
    r"""The Agentic RAG configuration for the knowledge base. If `null` is provided, Agentic RAG will be disabled."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "retrieval_type",
                "top_k",
                "threshold",
                "rerank_config",
                "agentic_rag_config",
            ]
        )
        nullable_fields = set(["rerank_config", "agentic_rag_config"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class Knowledge1TypedDict(TypedDict):
    id: str
    r"""The unique identifier of the knowledge base."""
    created: str
    r"""The creation date of the knowledge base."""
    key: str
    r"""The unique key of the knowledge base."""
    domain_id: str
    r"""The project/domain ID of the knowledge base."""
    updated: str
    r"""The last update date of the knowledge base."""
    model: str
    r"""The embeddings model used for the knowledge base."""
    description: NotRequired[Nullable[str]]
    r"""The description of the knowledge base."""
    path: NotRequired[str]
    r"""Entity storage path.

    With workspace-level API keys, use the format `project/folder/subfolder/...`. The first element identifies the project, followed by nested folders (auto-created as needed). Example: `Default/agents`.

    With project-level API keys, the project is predetermined by the API key, so the path is relative to that project. Example: `agents`. For backward compatibility, a leading project name is ignored when it matches the scoped project.
    """
    created_by_id: NotRequired[Nullable[str]]
    updated_by_id: NotRequired[Nullable[str]]
    type: NotRequired[KnowledgeType]
    retrieval_settings: NotRequired[RetrievalSettingsTypedDict]
    r"""The retrieval settings for the knowledge base. If not provider, Hybrid Search will be used as a default query strategy."""
    settings: NotRequired[KnowledgeSettingsTypedDict]
    r"""Additive compatibility fields formerly available only from private internal endpoints."""
    metadata: NotRequired[KnowledgeMetadataTypedDict]


class Knowledge1(BaseModel):
    id: Annotated[str, pydantic.Field(alias="_id")]
    r"""The unique identifier of the knowledge base."""

    created: str
    r"""The creation date of the knowledge base."""

    key: str
    r"""The unique key of the knowledge base."""

    domain_id: str
    r"""The project/domain ID of the knowledge base."""

    updated: str
    r"""The last update date of the knowledge base."""

    model: str
    r"""The embeddings model used for the knowledge base."""

    description: OptionalNullable[str] = UNSET
    r"""The description of the knowledge base."""

    path: Optional[str] = None
    r"""Entity storage path.

    With workspace-level API keys, use the format `project/folder/subfolder/...`. The first element identifies the project, followed by nested folders (auto-created as needed). Example: `Default/agents`.

    With project-level API keys, the project is predetermined by the API key, so the path is relative to that project. Example: `agents`. For backward compatibility, a leading project name is ignored when it matches the scoped project.
    """

    created_by_id: OptionalNullable[str] = UNSET

    updated_by_id: OptionalNullable[str] = UNSET

    type: Optional[KnowledgeType] = "internal"

    retrieval_settings: Optional[RetrievalSettings] = None
    r"""The retrieval settings for the knowledge base. If not provider, Hybrid Search will be used as a default query strategy."""

    settings: Optional[KnowledgeSettings] = None
    r"""Additive compatibility fields formerly available only from private internal endpoints."""

    metadata: Optional[KnowledgeMetadata] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "description",
                "path",
                "created_by_id",
                "updated_by_id",
                "type",
                "retrieval_settings",
                "settings",
                "metadata",
            ]
        )
        nullable_fields = set(["description", "created_by_id", "updated_by_id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


KnowledgeTypedDict = TypeAliasType(
    "KnowledgeTypedDict", Union[Knowledge1TypedDict, Knowledge2TypedDict]
)


Knowledge = TypeAliasType("Knowledge", Union[Knowledge1, Knowledge2])


try:
    Knowledge2.model_rebuild()
except NameError:
    pass
try:
    Knowledge1.model_rebuild()
except NameError:
    pass
