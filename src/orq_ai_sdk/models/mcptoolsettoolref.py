"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class McpToolsetToolRefTypedDict(TypedDict):
    r"""McpToolsetToolRef points at a single tool by its origin server and the
    tool's bare (upstream) name.
    """

    mcp_server_id: NotRequired[str]
    tool_name: NotRequired[str]


class McpToolsetToolRef(BaseModel):
    r"""McpToolsetToolRef points at a single tool by its origin server and the
    tool's bare (upstream) name.
    """

    mcp_server_id: Optional[str] = None

    tool_name: Optional[str] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["mcp_server_id", "tool_name"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
