"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .apikeystatus import APIKeyStatus
from .ownertype import OwnerType
from .permissionmode import PermissionMode
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from orq_ai_sdk.utils import FieldMetadata, QueryParamMetadata
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


class APIKeyListRequestTypedDict(TypedDict):
    limit: NotRequired[int]
    r"""Page size, 1–200. Unset uses the server default (25)."""
    starting_after: NotRequired[str]
    r"""Cursor for forward pagination. Set to the `api_key_id` of the last
    item from the previous page.
    """
    ending_before: NotRequired[str]
    r"""Cursor for backward pagination. Set to the `api_key_id` of the
    first item from the previous page.
    """
    project_id: NotRequired[str]
    r"""Optional filter: only return keys belonging to this project. When
    omitted, returns workspace-scoped and any single-project keys.
    """
    status: NotRequired[APIKeyStatus]
    r"""Optional filter: only return keys with this status."""
    search: NotRequired[str]
    r"""Optional case-insensitive substring match against the api-key
    name. Empty means no name filter.
    """
    owner_type: NotRequired[List[OwnerType]]
    r"""Optional filter: only return keys whose `owner.kind` matches
    one of the requested types. Combines the user / service-account
    oneof cases into a single repeated enum so the wire stays flat
    and multi-select filters travel as a single field. Empty means
    no owner-type filter.
    """
    permission_mode: NotRequired[List[PermissionMode]]
    r"""Optional filter: only return keys whose permission mode is one
    of the listed presets. Empty means no permission-mode filter.
    """


class APIKeyListRequest(BaseModel):
    limit: Annotated[
        Optional[int],
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = None
    r"""Page size, 1–200. Unset uses the server default (25)."""

    starting_after: Annotated[
        Optional[str],
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = None
    r"""Cursor for forward pagination. Set to the `api_key_id` of the last
    item from the previous page.
    """

    ending_before: Annotated[
        Optional[str],
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = None
    r"""Cursor for backward pagination. Set to the `api_key_id` of the
    first item from the previous page.
    """

    project_id: Annotated[
        Optional[str],
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = None
    r"""Optional filter: only return keys belonging to this project. When
    omitted, returns workspace-scoped and any single-project keys.
    """

    status: Annotated[
        Optional[APIKeyStatus],
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = None
    r"""Optional filter: only return keys with this status."""

    search: Annotated[
        Optional[str],
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = None
    r"""Optional case-insensitive substring match against the api-key
    name. Empty means no name filter.
    """

    owner_type: Annotated[
        Optional[List[OwnerType]],
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = None
    r"""Optional filter: only return keys whose `owner.kind` matches
    one of the requested types. Combines the user / service-account
    oneof cases into a single repeated enum so the wire stays flat
    and multi-select filters travel as a single field. Empty means
    no owner-type filter.
    """

    permission_mode: Annotated[
        Optional[List[PermissionMode]],
        FieldMetadata(query=QueryParamMetadata(style="form", explode=True)),
    ] = None
    r"""Optional filter: only return keys whose permission mode is one
    of the listed presets. Empty means no permission-mode filter.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "limit",
                "starting_after",
                "ending_before",
                "project_id",
                "status",
                "search",
                "owner_type",
                "permission_mode",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
