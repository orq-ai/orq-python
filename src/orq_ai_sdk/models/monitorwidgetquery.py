"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .monitorfilter import MonitorFilter, MonitorFilterTypedDict
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import List, Literal, Optional
from typing_extensions import NotRequired, TypedDict


Grain = Literal[
    "auto",
    "minute",
    "hour",
    "day",
]
r"""Requested bucket grain for timeseries widgets."""


class MonitorWidgetQueryTypedDict(TypedDict):
    metric: str
    r"""Reporting API catalogue metric, e.g. `genai.cost` or
    `genai.latency.p95`. Validated against the catalogue when the
    monitor is saved.
    """
    group_by: NotRequired[List[str]]
    r"""Reporting dimensions to break down by. Valid dimensions depend on
    the metric; validated when the monitor is saved.
    """
    filters: NotRequired[List[MonitorFilterTypedDict]]
    r"""Reporting filters combined with AND."""
    grain: NotRequired[Grain]
    r"""Requested bucket grain for timeseries widgets."""
    limit: NotRequired[int]
    r"""Row cap. For toplist widgets this is the top-N group count."""


class MonitorWidgetQuery(BaseModel):
    metric: str
    r"""Reporting API catalogue metric, e.g. `genai.cost` or
    `genai.latency.p95`. Validated against the catalogue when the
    monitor is saved.
    """

    group_by: Optional[List[str]] = None
    r"""Reporting dimensions to break down by. Valid dimensions depend on
    the metric; validated when the monitor is saved.
    """

    filters: Optional[List[MonitorFilter]] = None
    r"""Reporting filters combined with AND."""

    grain: Optional[Grain] = None
    r"""Requested bucket grain for timeseries widgets."""

    limit: Optional[int] = None
    r"""Row cap. For toplist widgets this is the top-N group count."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["group_by", "filters", "grain", "limit"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
