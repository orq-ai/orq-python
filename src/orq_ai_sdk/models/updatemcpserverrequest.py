"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .mcpauthconfig import McpAuthConfig, McpAuthConfigTypedDict
from .mcpconnection import McpConnection, McpConnectionTypedDict
from .mcptoolexposure import McpToolExposure, McpToolExposureTypedDict
from .sharing import Sharing, SharingTypedDict
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class UpdateMcpServerRequestTypedDict(TypedDict):
    key: NotRequired[str]
    r"""Rejected with INVALID_ARGUMENT: the key prefixes this server's tools in
    every gateway that exposes it, so it is immutable after creation. Retained
    so callers get an error rather than a silently ignored field.
    """
    display_name: NotRequired[str]
    r"""Human readable name shown in the workspace."""
    description: NotRequired[str]
    r"""Free-form note about what this server is for."""
    connection: NotRequired[McpConnectionTypedDict]
    r"""How the gateway dials the upstream server."""
    auth: NotRequired[McpAuthConfigTypedDict]
    r"""Credentials the gateway sends upstream; send `type: NONE` explicitly for public servers."""
    default_tool_exposure: NotRequired[McpToolExposureTypedDict]
    r"""Fallback exposure used by any gateway link that does not set its own."""
    sharing: NotRequired[SharingTypedDict]
    r"""Which projects in the workspace may use this server. Defaults to every project."""


class UpdateMcpServerRequest(BaseModel):
    key: Optional[str] = None
    r"""Rejected with INVALID_ARGUMENT: the key prefixes this server's tools in
    every gateway that exposes it, so it is immutable after creation. Retained
    so callers get an error rather than a silently ignored field.
    """

    display_name: Optional[str] = None
    r"""Human readable name shown in the workspace."""

    description: Optional[str] = None
    r"""Free-form note about what this server is for."""

    connection: Optional[McpConnection] = None
    r"""How the gateway dials the upstream server."""

    auth: Optional[McpAuthConfig] = None
    r"""Credentials the gateway sends upstream; send `type: NONE` explicitly for public servers."""

    default_tool_exposure: Optional[McpToolExposure] = None
    r"""Fallback exposure used by any gateway link that does not set its own."""

    sharing: Optional[Sharing] = None
    r"""Which projects in the workspace may use this server. Defaults to every project."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "key",
                "display_name",
                "description",
                "connection",
                "auth",
                "default_tool_exposure",
                "sharing",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
