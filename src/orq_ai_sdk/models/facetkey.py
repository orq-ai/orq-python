"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .facetvalue import FacetValue, FacetValueTypedDict
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class FacetKeyTypedDict(TypedDict):
    r"""FacetKey is one facetable key inside a group, with its observed volume and
    top values for the requested range. field is the name usable in filters /
    sort (e.g. \"attribute.http.method\" or \"service_name\"); it is empty when the
    key is not filterable (the scope group).
    """

    key: NotRequired[str]
    field: NotRequired[str]
    log_count: NotRequired[str]
    distinct_value_count: NotRequired[str]
    top_values: NotRequired[List[FacetValueTypedDict]]
    filterable: NotRequired[bool]


class FacetKey(BaseModel):
    r"""FacetKey is one facetable key inside a group, with its observed volume and
    top values for the requested range. field is the name usable in filters /
    sort (e.g. \"attribute.http.method\" or \"service_name\"); it is empty when the
    key is not filterable (the scope group).
    """

    key: Optional[str] = None

    field: Optional[str] = None

    log_count: Optional[str] = None

    distinct_value_count: Optional[str] = None

    top_values: Optional[List[FacetValue]] = None

    filterable: Optional[bool] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "key",
                "field",
                "log_count",
                "distinct_value_count",
                "top_values",
                "filterable",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
