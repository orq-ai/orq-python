"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .allprojects import AllProjects, AllProjectsTypedDict
from .singleproject import SingleProject, SingleProjectTypedDict
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class ProjectScopeTypedDict(TypedDict):
    r"""Project authorization scope. Single-project or all-projects.
    Multi-project use cases are served by minting per-project keys or by
    using an all-projects key with `restricted` mode.
    """

    all: NotRequired[AllProjectsTypedDict]
    single: NotRequired[SingleProjectTypedDict]


class ProjectScope(BaseModel):
    r"""Project authorization scope. Single-project or all-projects.
    Multi-project use cases are served by minting per-project keys or by
    using an all-projects key with `restricted` mode.
    """

    all: Optional[AllProjects] = None

    single: Optional[SingleProject] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["all", "single"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
