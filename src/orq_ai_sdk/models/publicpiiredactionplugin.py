"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from orq_ai_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing import List, Literal, Optional
from typing_extensions import NotRequired, TypedDict


PublicPIIRedactionPluginID = Literal["pii_redaction",]
r"""Plugin discriminator. Must be pii_redaction."""


PublicPIIRedactionPluginLanguage = Literal[
    "en",
    "nl",
]
r"""Detector language. Defaults to en."""


PublicPIIRedactionPluginOnFailure = Literal[
    "block",
    "passthrough",
]
r"""Behavior when redaction is unavailable. block (default) fails the request; passthrough sends the original text."""


class PublicPIIRedactionPluginTypedDict(TypedDict):
    id: PublicPIIRedactionPluginID
    r"""Plugin discriminator. Must be pii_redaction."""
    entities: NotRequired[Nullable[List[str]]]
    r"""Entity types to redact (e.g. EMAIL_ADDRESS, BSN). Omit to redact every type detected for the language."""
    language: NotRequired[PublicPIIRedactionPluginLanguage]
    r"""Detector language. Defaults to en."""
    on_failure: NotRequired[PublicPIIRedactionPluginOnFailure]
    r"""Behavior when redaction is unavailable. block (default) fails the request; passthrough sends the original text."""
    persist_redacted_to_traces: NotRequired[bool]
    r"""Store the redacted (placeholder) form in traces rather than the restored original. Defaults to true."""
    threshold: NotRequired[float]
    r"""Detector confidence cutoff in [0,1]."""


class PublicPIIRedactionPlugin(BaseModel):
    id: PublicPIIRedactionPluginID
    r"""Plugin discriminator. Must be pii_redaction."""

    entities: OptionalNullable[List[str]] = UNSET
    r"""Entity types to redact (e.g. EMAIL_ADDRESS, BSN). Omit to redact every type detected for the language."""

    language: Optional[PublicPIIRedactionPluginLanguage] = None
    r"""Detector language. Defaults to en."""

    on_failure: Optional[PublicPIIRedactionPluginOnFailure] = None
    r"""Behavior when redaction is unavailable. block (default) fails the request; passthrough sends the original text."""

    persist_redacted_to_traces: Optional[bool] = None
    r"""Store the redacted (placeholder) form in traces rather than the restored original. Defaults to true."""

    threshold: Optional[float] = None
    r"""Detector confidence cutoff in [0,1]."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "entities",
                "language",
                "on_failure",
                "persist_redacted_to_traces",
                "threshold",
            ]
        )
        nullable_fields = set(["entities"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
