"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .domainverificationstatus import DomainVerificationStatus
from datetime import datetime
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class DomainVerificationTypedDict(TypedDict):
    id: str
    domain: str
    status: DomainVerificationStatus
    txt_name: str
    txt_value: str
    created_at: datetime
    expires_at: NotRequired[datetime]
    verified_at: NotRequired[datetime]
    created_by_id: NotRequired[str]


class DomainVerification(BaseModel):
    id: str

    domain: str

    status: DomainVerificationStatus

    txt_name: str

    txt_value: str

    created_at: datetime

    expires_at: Optional[datetime] = None

    verified_at: Optional[datetime] = None

    created_by_id: Optional[str] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["expires_at", "verified_at", "created_by_id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
