"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .evaluationresult import EvaluationResult, EvaluationResultTypedDict
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class InvokeEvaluatorResponseTypedDict(TypedDict):
    r"""Response wrappers keep each RPC's response type distinct, so a future field
    can be added to one without touching the other.
    """

    result: NotRequired[EvaluationResultTypedDict]
    r"""The verdict. Shaped to match WorkflowRunMinifiedEvalSchema, the body the
    TypeScript route returned, so existing consumers read the same JSON.
    """


class InvokeEvaluatorResponse(BaseModel):
    r"""Response wrappers keep each RPC's response type distinct, so a future field
    can be added to one without touching the other.
    """

    result: Optional[EvaluationResult] = None
    r"""The verdict. Shaped to match WorkflowRunMinifiedEvalSchema, the body the
    TypeScript route returned, so existing consumers read the same JSON.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["result"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
