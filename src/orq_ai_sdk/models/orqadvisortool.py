"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Literal, Optional
from typing_extensions import NotRequired, TypedDict


ReasoningEffort = Literal[
    "none",
    "minimal",
    "low",
    "medium",
    "high",
    "xhigh",
    "max",
]
r"""Reasoning effort for supported models. Omit to use the provider default."""


OrqAdvisorToolType = Literal["orq:advisor",]
r"""Advisor tool discriminator."""


class OrqAdvisorToolTypedDict(TypedDict):
    r"""Lets the primary model consult a configured secondary model for advice."""

    model: str
    r"""Secondary model in provider/model format."""
    type: OrqAdvisorToolType
    r"""Advisor tool discriminator."""
    max_tokens: NotRequired[int]
    r"""Maximum secondary-model output tokens. 0 uses the provider default; the selected model may impose a lower maximum."""
    max_transcript_tokens: NotRequired[int]
    r"""Maximum estimated conversation-transcript tokens sent to the secondary model. 0 includes the full transcript."""
    max_uses: NotRequired[int]
    r"""Maximum invocations per request. 0 means unlimited."""
    reasoning_effort: NotRequired[ReasoningEffort]
    r"""Reasoning effort for supported models. Omit to use the provider default."""
    temperature: NotRequired[float]
    r"""Sampling temperature. The selected model may impose a lower maximum."""


class OrqAdvisorTool(BaseModel):
    r"""Lets the primary model consult a configured secondary model for advice."""

    model: str
    r"""Secondary model in provider/model format."""

    type: OrqAdvisorToolType
    r"""Advisor tool discriminator."""

    max_tokens: Optional[int] = None
    r"""Maximum secondary-model output tokens. 0 uses the provider default; the selected model may impose a lower maximum."""

    max_transcript_tokens: Optional[int] = None
    r"""Maximum estimated conversation-transcript tokens sent to the secondary model. 0 includes the full transcript."""

    max_uses: Optional[int] = None
    r"""Maximum invocations per request. 0 means unlimited."""

    reasoning_effort: Optional[ReasoningEffort] = None
    r"""Reasoning effort for supported models. Omit to use the provider default."""

    temperature: Optional[float] = None
    r"""Sampling temperature. The selected model may impose a lower maximum."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "max_tokens",
                "max_transcript_tokens",
                "max_uses",
                "reasoning_effort",
                "temperature",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
