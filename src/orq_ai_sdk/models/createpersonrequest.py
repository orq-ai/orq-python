"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import List, Optional
from typing_extensions import NotRequired, TypedDict


class CreatePersonRequestTypedDict(TypedDict):
    emails: List[str]
    r"""Email addresses to invite. At least one email is required."""
    roles: NotRequired[List[str]]
    r"""Roles to assign. Defaults to [\"member\"] when empty."""
    groups: NotRequired[List[str]]
    r"""Group IDs to assign."""


class CreatePersonRequest(BaseModel):
    emails: List[str]
    r"""Email addresses to invite. At least one email is required."""

    roles: Optional[List[str]] = None
    r"""Roles to assign. Defaults to [\"member\"] when empty."""

    groups: Optional[List[str]] = None
    r"""Group IDs to assign."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["roles", "groups"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
