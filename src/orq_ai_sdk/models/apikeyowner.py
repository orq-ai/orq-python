"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .serviceaccountowner import ServiceAccountOwner, ServiceAccountOwnerTypedDict
from .userowner import UserOwner, UserOwnerTypedDict
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class APIKeyOwnerTypedDict(TypedDict):
    r"""Owner attribution drives lifecycle.

    `service_account` keys are workspace-owned and outlive any individual
    user. `user` keys are bound to `user_id`: when the user is removed,
    disabled, or loses project access, the key is revoked / its scope
    shrinks per the cascade rules in ADR 0001.
    """

    user: NotRequired[UserOwnerTypedDict]
    service_account: NotRequired[ServiceAccountOwnerTypedDict]


class APIKeyOwner(BaseModel):
    r"""Owner attribution drives lifecycle.

    `service_account` keys are workspace-owned and outlive any individual
    user. `user` keys are bound to `user_id`: when the user is removed,
    disabled, or loses project access, the key is revoked / its scope
    shrinks per the cascade rules in ADR 0001.
    """

    user: Optional[UserOwner] = None

    service_account: Optional[ServiceAccountOwner] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["user", "service_account"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
