"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .searchtype import SearchType
from orq_ai_sdk.types import (
    BaseModel,
    Nullable,
    OptionalNullable,
    UNSET,
    UNSET_SENTINEL,
)
from pydantic import model_serializer
from typing import Literal, Optional
from typing_extensions import NotRequired, TypedDict


ModelType = Literal["rerank",]


class ModelParametersTypedDict(TypedDict):
    threshold: NotRequired[float]


class ModelParameters(BaseModel):
    threshold: Optional[float] = None

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["threshold"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


class SearchRetrievalConfigRerankConfigTypedDict(TypedDict):
    r"""Override reranking for this retrieval configuration. Omit to inherit stored settings or set to null to disable reranking."""

    enabled: NotRequired[bool]
    provider: NotRequired[str]
    top_k: NotRequired[int]
    model: NotRequired[str]
    model_db_id: NotRequired[str]
    model_type: NotRequired[ModelType]
    model_parameters: NotRequired[ModelParametersTypedDict]
    integration_id: NotRequired[Nullable[str]]


class SearchRetrievalConfigRerankConfig(BaseModel):
    r"""Override reranking for this retrieval configuration. Omit to inherit stored settings or set to null to disable reranking."""

    enabled: Optional[bool] = None

    provider: Optional[str] = None

    top_k: Optional[int] = None

    model: Optional[str] = None

    model_db_id: Optional[str] = None

    model_type: Optional[ModelType] = None

    model_parameters: Optional[ModelParameters] = None

    integration_id: OptionalNullable[str] = UNSET

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "enabled",
                "provider",
                "top_k",
                "model",
                "model_db_id",
                "model_type",
                "model_parameters",
                "integration_id",
            ]
        )
        nullable_fields = set(["integration_id"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m


class SearchRetrievalConfigTypedDict(TypedDict):
    r"""SearchRetrievalConfig mirrors RetrievalConfigInDatabaseSchema. Type remains
    optional because external knowledge bases use the same base configuration
    without a retrieval strategy discriminator.
    """

    type: NotRequired[SearchType]
    top_k: NotRequired[int]
    threshold: NotRequired[float]
    rerank_config: NotRequired[Nullable[SearchRetrievalConfigRerankConfigTypedDict]]
    r"""Override reranking for this retrieval configuration. Omit to inherit stored settings or set to null to disable reranking."""


class SearchRetrievalConfig(BaseModel):
    r"""SearchRetrievalConfig mirrors RetrievalConfigInDatabaseSchema. Type remains
    optional because external knowledge bases use the same base configuration
    without a retrieval strategy discriminator.
    """

    type: Optional[SearchType] = None

    top_k: Optional[int] = None

    threshold: Optional[float] = None

    rerank_config: OptionalNullable[SearchRetrievalConfigRerankConfig] = UNSET
    r"""Override reranking for this retrieval configuration. Omit to inherit stored settings or set to null to disable reranking."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(["type", "top_k", "threshold", "rerank_config"])
        nullable_fields = set(["rerank_config"])
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))
            is_nullable_and_explicitly_set = (
                k in nullable_fields
                and (self.__pydantic_fields_set__.intersection({n}))  # pylint: disable=no-member
            )

            if val != UNSET_SENTINEL:
                if (
                    val is not None
                    or k not in optional_fields
                    or is_nullable_and_explicitly_set
                ):
                    m[k] = val

        return m
