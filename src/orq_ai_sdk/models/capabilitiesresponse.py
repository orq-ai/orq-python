"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .entitylist import EntityList, EntityListTypedDict
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Dict, List, Optional
from typing_extensions import NotRequired, TypedDict


class CapabilitiesResponseTypedDict(TypedDict):
    languages: NotRequired[List[str]]
    r"""Supported detector languages, e.g. \"en\", \"nl\". There is no \"auto\" value."""
    base_entities: NotRequired[List[str]]
    r"""Universal entity types available regardless of region, e.g. PASSPORT,
    LICENSE_PLATE, UUID, JWT, BIC.
    """
    all_entities: NotRequired[List[str]]
    r"""Every entity type the service can detect across all regions and the
    base catalog.
    """
    regions: NotRequired[List[str]]
    r"""Region codes the service recognizes, e.g. \"nl\", \"gb\", \"all\"."""
    region_entities: NotRequired[Dict[str, EntityListTypedDict]]
    r"""Maps each region code to the entity types it gates. Use this to find the
    type to name in `entities` when you need a per-entity threshold on a type
    a region gates.
    """
    entity_thresholds: NotRequired[Dict[str, float]]
    r"""Sparse per-entity default thresholds; entities absent here fall back to
    default_threshold.
    """
    default_threshold: NotRequired[float]
    r"""Confidence cutoff applied when neither `threshold` nor an entity-specific
    override is supplied.
    """
    max_text_chars: NotRequired[int]
    r"""Maximum number of characters accepted per request."""


class CapabilitiesResponse(BaseModel):
    languages: Optional[List[str]] = None
    r"""Supported detector languages, e.g. \"en\", \"nl\". There is no \"auto\" value."""

    base_entities: Optional[List[str]] = None
    r"""Universal entity types available regardless of region, e.g. PASSPORT,
    LICENSE_PLATE, UUID, JWT, BIC.
    """

    all_entities: Optional[List[str]] = None
    r"""Every entity type the service can detect across all regions and the
    base catalog.
    """

    regions: Optional[List[str]] = None
    r"""Region codes the service recognizes, e.g. \"nl\", \"gb\", \"all\"."""

    region_entities: Optional[Dict[str, EntityList]] = None
    r"""Maps each region code to the entity types it gates. Use this to find the
    type to name in `entities` when you need a per-entity threshold on a type
    a region gates.
    """

    entity_thresholds: Optional[Dict[str, float]] = None
    r"""Sparse per-entity default thresholds; entities absent here fall back to
    default_threshold.
    """

    default_threshold: Optional[float] = None
    r"""Confidence cutoff applied when neither `threshold` nor an entity-specific
    override is supplied.
    """

    max_text_chars: Optional[int] = None
    r"""Maximum number of characters accepted per request."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "languages",
                "base_entities",
                "all_entities",
                "regions",
                "region_entities",
                "entity_thresholds",
                "default_threshold",
                "max_text_chars",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
