"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Dict, Optional
from typing_extensions import NotRequired, TypedDict


class MetadataTypedDict(TypedDict):
    r"""Request metadata forwarded for dynamic-budget matching
    (`metadata.team == \"ml\"`). Free-form JSON object from the request
    body's `metadata` field.
    """


class Metadata(BaseModel):
    r"""Request metadata forwarded for dynamic-budget matching
    (`metadata.team == \"ml\"`). Free-form JSON object from the request
    body's `metadata` field.
    """


class CheckBudgetsRequestTypedDict(TypedDict):
    api_key_id: NotRequired[str]
    r"""API key that issued the request (if any)."""
    project_id: NotRequired[str]
    r"""Project the request targets (if any)."""
    identity_external_id: NotRequired[str]
    r"""Identity external id for contact-scoped budgets (if any)."""
    provider: NotRequired[str]
    r"""Provider enum value for provider-scoped budgets (if any)."""
    model_id: NotRequired[str]
    r"""Full model reference for model-scoped budgets (if any), exactly as
    the caller sent it: \"provider/model\" or \"workspaceKey@provider/model\".
    """
    metadata: NotRequired[MetadataTypedDict]
    r"""Request metadata forwarded for dynamic-budget matching
    (`metadata.team == \"ml\"`). Free-form JSON object from the request
    body's `metadata` field.
    """
    headers: NotRequired[Dict[str, str]]
    r"""Request headers (lowercase keys) forwarded for dynamic-budget
    matching (`headers[\"x-env\"] == \"prod\"`).
    """


class CheckBudgetsRequest(BaseModel):
    api_key_id: Optional[str] = None
    r"""API key that issued the request (if any)."""

    project_id: Optional[str] = None
    r"""Project the request targets (if any)."""

    identity_external_id: Optional[str] = None
    r"""Identity external id for contact-scoped budgets (if any)."""

    provider: Optional[str] = None
    r"""Provider enum value for provider-scoped budgets (if any)."""

    model_id: Optional[str] = None
    r"""Full model reference for model-scoped budgets (if any), exactly as
    the caller sent it: \"provider/model\" or \"workspaceKey@provider/model\".
    """

    metadata: Optional[Metadata] = None
    r"""Request metadata forwarded for dynamic-budget matching
    (`metadata.team == \"ml\"`). Free-form JSON object from the request
    body's `metadata` field.
    """

    headers: Optional[Dict[str, str]] = None
    r"""Request headers (lowercase keys) forwarded for dynamic-budget
    matching (`headers[\"x-env\"] == \"prod\"`).
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "api_key_id",
                "project_id",
                "identity_external_id",
                "provider",
                "model_id",
                "metadata",
                "headers",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
