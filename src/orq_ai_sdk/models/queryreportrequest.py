"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .filter_ import Filter, FilterTypedDict
from datetime import datetime
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
import pydantic
from pydantic import model_serializer
from typing import List, Literal, Optional
from typing_extensions import Annotated, NotRequired, TypedDict


Metric = Literal[
    "genai.requests",
    "genai.tokens",
    "genai.cost",
    "genai.errors",
    "genai.latency.p50",
    "genai.latency.p95",
    "genai.latency.p99",
    "genai.evaluator.runs",
    "genai.evaluator.pass_rate",
    "genai.evaluator.score.avg",
    "genai.guardrail.runs",
    "genai.guardrail.block_rate",
    "genai.guardrail.triggered",
    "genai.usage",
]
r"""Catalogue metric to query."""


Grain = Literal[
    "auto",
    "minute",
    "hour",
    "day",
]
r"""Requested bucket grain. Use `auto` or omit the field to let the server choose based on the requested range."""


GroupBy = Literal[
    "project",
    "identity",
    "provider",
    "model",
    "product",
    "api_key",
    "status_code",
    "http_status_code",
    "credential_type",
    "dimension",
    "dimension_type",
    "tag",
    "agent",
    "tool",
    "deployment",
    "evaluator",
    "dataset",
    "prompt",
    "policy",
    "conversation",
    "thread",
    "memory_store",
    "knowledge",
    "sheet",
    "guardrail_origin",
    "evaluator_name",
    "evaluator_type",
    "evaluator_version",
    "result_type",
    "evaluation_stage",
    "guardrail_stage",
    "evaluator_stage",
    "guardrail_action",
    "result_label",
]


class QueryReportRequestTypedDict(TypedDict):
    metric: Metric
    r"""Catalogue metric to query."""
    from_: datetime
    r"""Inclusive lower bound for the report window (RFC 3339, UTC)."""
    to: datetime
    r"""Exclusive upper bound for the report window (RFC 3339, UTC)."""
    grain: NotRequired[Grain]
    r"""Requested bucket grain. Use `auto` or omit the field to let the server choose based on the requested range."""
    group_by: NotRequired[List[GroupBy]]
    r"""Reporting dimensions to break down by. Valid dimensions depend on the selected metric."""
    filters: NotRequired[List[FilterTypedDict]]
    r"""Up to 20 allowlisted predicates combined with AND."""
    limit: NotRequired[int]
    r"""Maximum bucket rows returned. Defaults to 1000 and is capped at
    5000.
    """
    time_zone: NotRequired[str]
    r"""IANA time zone applied to bucket boundaries, for example
    `America/New_York`. Response timestamps remain UTC. Empty means UTC.
    """
    include_totals: NotRequired[bool]
    r"""When true, include a `totals` block aggregated across the full
    report window.
    """


class QueryReportRequest(BaseModel):
    metric: Metric
    r"""Catalogue metric to query."""

    from_: Annotated[datetime, pydantic.Field(alias="from")]
    r"""Inclusive lower bound for the report window (RFC 3339, UTC)."""

    to: datetime
    r"""Exclusive upper bound for the report window (RFC 3339, UTC)."""

    grain: Optional[Grain] = None
    r"""Requested bucket grain. Use `auto` or omit the field to let the server choose based on the requested range."""

    group_by: Optional[List[GroupBy]] = None
    r"""Reporting dimensions to break down by. Valid dimensions depend on the selected metric."""

    filters: Optional[List[Filter]] = None
    r"""Up to 20 allowlisted predicates combined with AND."""

    limit: Optional[int] = None
    r"""Maximum bucket rows returned. Defaults to 1000 and is capped at
    5000.
    """

    time_zone: Optional[str] = None
    r"""IANA time zone applied to bucket boundaries, for example
    `America/New_York`. Response timestamps remain UTC. Empty means UTC.
    """

    include_totals: Optional[bool] = None
    r"""When true, include a `totals` block aggregated across the full
    report window.
    """

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            ["grain", "group_by", "filters", "limit", "time_zone", "include_totals"]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m


try:
    QueryReportRequest.model_rebuild()
except NameError:
    pass
