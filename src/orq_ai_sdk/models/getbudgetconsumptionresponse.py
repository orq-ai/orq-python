"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from __future__ import annotations
from .budgetperiod import BudgetPeriod
from orq_ai_sdk.types import BaseModel, UNSET_SENTINEL
from pydantic import model_serializer
from typing import Optional
from typing_extensions import NotRequired, TypedDict


class GetBudgetConsumptionResponseTypedDict(TypedDict):
    budget_id: NotRequired[str]
    r"""Budget id whose counters are reported."""
    period: NotRequired[BudgetPeriod]
    period_date: NotRequired[str]
    r"""Period bucket label (e.g. \"2025-05-27\" for DAILY, \"2025-05\" for
    MONTHLY). Empty when no counters exist for the current period.
    """
    cost: NotRequired[float]
    r"""Cost accumulated in the current period, in USD."""
    tokens: NotRequired[float]
    r"""Tokens accumulated in the current period. Carried as a double (not
    int64) so it serializes as a JSON number rather than a quoted
    string, matching BudgetUsage.tokens and limits.token_limit.
    """
    requests_in_window: NotRequired[int]
    r"""Requests counted in the current 60-second rolling window."""


class GetBudgetConsumptionResponse(BaseModel):
    budget_id: Optional[str] = None
    r"""Budget id whose counters are reported."""

    period: Optional[BudgetPeriod] = None

    period_date: Optional[str] = None
    r"""Period bucket label (e.g. \"2025-05-27\" for DAILY, \"2025-05\" for
    MONTHLY). Empty when no counters exist for the current period.
    """

    cost: Optional[float] = None
    r"""Cost accumulated in the current period, in USD."""

    tokens: Optional[float] = None
    r"""Tokens accumulated in the current period. Carried as a double (not
    int64) so it serializes as a JSON number rather than a quoted
    string, matching BudgetUsage.tokens and limits.token_limit.
    """

    requests_in_window: Optional[int] = None
    r"""Requests counted in the current 60-second rolling window."""

    @model_serializer(mode="wrap")
    def serialize_model(self, handler):
        optional_fields = set(
            [
                "budget_id",
                "period",
                "period_date",
                "cost",
                "tokens",
                "requests_in_window",
            ]
        )
        serialized = handler(self)
        m = {}

        for n, f in type(self).model_fields.items():
            k = f.alias or n
            val = serialized.get(k, serialized.get(n))

            if val != UNSET_SENTINEL:
                if val is not None or k not in optional_fields:
                    m[k] = val

        return m
