"""Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT."""

from .basesdk import BaseSDK
from datetime import datetime
from orq_ai_sdk import models, utils
from orq_ai_sdk._hooks import HookContext
from orq_ai_sdk.types import OptionalNullable, UNSET
from orq_ai_sdk.utils import get_security_from_env
from orq_ai_sdk.utils.unmarshal_json_response import unmarshal_json_response
from typing import Iterable, List, Mapping, Optional, Union


class Budgets(BaseSDK):
    def list(
        self,
        *,
        limit: Optional[int] = None,
        starting_after: Optional[str] = None,
        ending_before: Optional[str] = None,
        scope_kind: Optional[Iterable[models.BudgetScopeKind]] = None,
        scope_target_id: Optional[str] = None,
        is_active: Optional[bool] = None,
        period: Optional[Iterable[models.BudgetPeriod]] = None,
        query: Optional[str] = None,
        sort_by: Optional[models.BudgetSortField] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.ListBudgetsResponse:
        r"""List budgets

        Returns budgets visible to the current workspace, ordered by creation time with the newest first. Supports filtering by scope kind, scope target id, period, and active state, plus an optional free-text query that searches across denormalized target names via Typesense.

        :param limit: Page size, 1–200. Unset uses the server default (25).
        :param starting_after: Cursor for forward pagination. Set to the `budget_id` of the last
            item from the previous page.
        :param ending_before: Cursor for backward pagination. Set to the `budget_id` of the
            first item from the previous page.
        :param scope_kind: Optional filter: only return budgets whose scope kind matches one
            of the listed values. Empty means no scope-kind filter.
        :param scope_target_id: Optional filter: only return budgets whose scope target id matches.
        :param is_active: Optional filter: only return budgets with this active state.
        :param period: Optional filter: only return budgets whose limits.period matches
            one of the listed values. Empty means no period filter.
        :param query: Optional free-text query. Server translates this into a Typesense
            search over the denormalized `scope_target_name` and id fields on
            the per-workspace `{workspace_id}_budgets` collection.
        :param sort_by: Field used to order the list. Unset orders by most-recently-updated.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.BudgetListRequest(
            limit=limit,
            starting_after=starting_after,
            ending_before=ending_before,
            scope_kind=utils.unmarshal(
                scope_kind, Optional[List[models.BudgetScopeKind]]
            ),
            scope_target_id=scope_target_id,
            is_active=is_active,
            period=utils.unmarshal(period, Optional[List[models.BudgetPeriod]]),
            query=query,
            sort_by=sort_by,
        )

        req = self._build_request(
            method="GET",
            path="/v2/budgets",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="BudgetList",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Budgets"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.ListBudgetsResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    async def list_async(
        self,
        *,
        limit: Optional[int] = None,
        starting_after: Optional[str] = None,
        ending_before: Optional[str] = None,
        scope_kind: Optional[Iterable[models.BudgetScopeKind]] = None,
        scope_target_id: Optional[str] = None,
        is_active: Optional[bool] = None,
        period: Optional[Iterable[models.BudgetPeriod]] = None,
        query: Optional[str] = None,
        sort_by: Optional[models.BudgetSortField] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.ListBudgetsResponse:
        r"""List budgets

        Returns budgets visible to the current workspace, ordered by creation time with the newest first. Supports filtering by scope kind, scope target id, period, and active state, plus an optional free-text query that searches across denormalized target names via Typesense.

        :param limit: Page size, 1–200. Unset uses the server default (25).
        :param starting_after: Cursor for forward pagination. Set to the `budget_id` of the last
            item from the previous page.
        :param ending_before: Cursor for backward pagination. Set to the `budget_id` of the
            first item from the previous page.
        :param scope_kind: Optional filter: only return budgets whose scope kind matches one
            of the listed values. Empty means no scope-kind filter.
        :param scope_target_id: Optional filter: only return budgets whose scope target id matches.
        :param is_active: Optional filter: only return budgets with this active state.
        :param period: Optional filter: only return budgets whose limits.period matches
            one of the listed values. Empty means no period filter.
        :param query: Optional free-text query. Server translates this into a Typesense
            search over the denormalized `scope_target_name` and id fields on
            the per-workspace `{workspace_id}_budgets` collection.
        :param sort_by: Field used to order the list. Unset orders by most-recently-updated.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.BudgetListRequest(
            limit=limit,
            starting_after=starting_after,
            ending_before=ending_before,
            scope_kind=utils.unmarshal(
                scope_kind, Optional[List[models.BudgetScopeKind]]
            ),
            scope_target_id=scope_target_id,
            is_active=is_active,
            period=utils.unmarshal(period, Optional[List[models.BudgetPeriod]]),
            query=query,
            sort_by=sort_by,
        )

        req = self._build_request_async(
            method="GET",
            path="/v2/budgets",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="BudgetList",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Budgets"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.ListBudgetsResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    def create(
        self,
        *,
        scope: Optional[Union[models.BudgetScope, models.BudgetScopeTypedDict]] = None,
        match: Optional[Union[models.BudgetMatch, models.BudgetMatchTypedDict]] = None,
        limits: Optional[
            Union[models.BudgetLimits, models.BudgetLimitsTypedDict]
        ] = None,
        rate_limit: Optional[Union[models.RateLimit, models.RateLimitTypedDict]] = None,
        is_active: Optional[bool] = None,
        expires_at: Optional[datetime] = None,
        alerts: Optional[
            Union[Iterable[models.BudgetAlert], Iterable[models.BudgetAlertTypedDict]]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.CreateBudgetResponse:
        r"""Create a new budget

        Creates a new budget in the workspace. Exactly one scope variant must be set (workspace / project / identity / api_key / provider / model). At least one of `limits.amount`, `limits.token_limit`, or `rate_limit.requests_per_minute` MUST be provided. Uniqueness is enforced across (workspace_id, scope_kind, scope_target_id).

        :param scope: Structured scope. Mutually exclusive with `match`: provide a scope
            for the six canonical kinds (the server derives the matching CEL),
            or provide `match` for a dynamic budget. Exactly one of the two
            must be set; the handler enforces that invariant.
        :param match: Raw CEL matching expression for dynamic budgets (e.g.
            `metadata.team == \"ml\" && provider == \"openai\"`). Validated via
            CEL parse at write time. Mutually exclusive with `scope`.
        :param limits: At least one of amount / token_limit / rate_limit.requests_per_minute
            must be provided on the budget; the handler enforces that invariant.
        :param rate_limit: Optional rate limit.
        :param is_active: Whether the budget should be active immediately. Defaults to true
            when omitted (handler enforces).
        :param expires_at: Optional expiration. When set in combination with is_active=true,
            the value MUST be in the future; the handler rejects past values.
        :param alerts: Optional threshold notifications. Ids are assigned by the server, so
            `alerts[].id` must be omitted here; supplying one is rejected.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.CreateBudgetRequest(
            scope=utils.get_pydantic_model(scope, Optional[models.BudgetScope]),
            match=utils.get_pydantic_model(match, Optional[models.BudgetMatch]),
            limits=utils.get_pydantic_model(limits, Optional[models.BudgetLimits]),
            rate_limit=utils.get_pydantic_model(rate_limit, Optional[models.RateLimit]),
            is_active=is_active,
            expires_at=expires_at,
            alerts=utils.get_pydantic_model(alerts, Optional[List[models.BudgetAlert]]),
        )

        req = self._build_request(
            method="POST",
            path="/v2/budgets",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.CreateBudgetRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="BudgetCreate",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Budgets"],
                extensions={
                    "x-code-samples": [
                        {
                            "label": "Node.js",
                            "lang": "typescript",
                            "source": 'await orq.budgets.create({ scope: { kind: "workspace" }, limits: { period: "MONTHLY", amount: 1000 } });\n',
                        }
                    ]
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.CreateBudgetResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    async def create_async(
        self,
        *,
        scope: Optional[Union[models.BudgetScope, models.BudgetScopeTypedDict]] = None,
        match: Optional[Union[models.BudgetMatch, models.BudgetMatchTypedDict]] = None,
        limits: Optional[
            Union[models.BudgetLimits, models.BudgetLimitsTypedDict]
        ] = None,
        rate_limit: Optional[Union[models.RateLimit, models.RateLimitTypedDict]] = None,
        is_active: Optional[bool] = None,
        expires_at: Optional[datetime] = None,
        alerts: Optional[
            Union[Iterable[models.BudgetAlert], Iterable[models.BudgetAlertTypedDict]]
        ] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.CreateBudgetResponse:
        r"""Create a new budget

        Creates a new budget in the workspace. Exactly one scope variant must be set (workspace / project / identity / api_key / provider / model). At least one of `limits.amount`, `limits.token_limit`, or `rate_limit.requests_per_minute` MUST be provided. Uniqueness is enforced across (workspace_id, scope_kind, scope_target_id).

        :param scope: Structured scope. Mutually exclusive with `match`: provide a scope
            for the six canonical kinds (the server derives the matching CEL),
            or provide `match` for a dynamic budget. Exactly one of the two
            must be set; the handler enforces that invariant.
        :param match: Raw CEL matching expression for dynamic budgets (e.g.
            `metadata.team == \"ml\" && provider == \"openai\"`). Validated via
            CEL parse at write time. Mutually exclusive with `scope`.
        :param limits: At least one of amount / token_limit / rate_limit.requests_per_minute
            must be provided on the budget; the handler enforces that invariant.
        :param rate_limit: Optional rate limit.
        :param is_active: Whether the budget should be active immediately. Defaults to true
            when omitted (handler enforces).
        :param expires_at: Optional expiration. When set in combination with is_active=true,
            the value MUST be in the future; the handler rejects past values.
        :param alerts: Optional threshold notifications. Ids are assigned by the server, so
            `alerts[].id` must be omitted here; supplying one is rejected.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.CreateBudgetRequest(
            scope=utils.get_pydantic_model(scope, Optional[models.BudgetScope]),
            match=utils.get_pydantic_model(match, Optional[models.BudgetMatch]),
            limits=utils.get_pydantic_model(limits, Optional[models.BudgetLimits]),
            rate_limit=utils.get_pydantic_model(rate_limit, Optional[models.RateLimit]),
            is_active=is_active,
            expires_at=expires_at,
            alerts=utils.get_pydantic_model(alerts, Optional[List[models.BudgetAlert]]),
        )

        req = self._build_request_async(
            method="POST",
            path="/v2/budgets",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=False,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request, False, False, "json", models.CreateBudgetRequest
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="BudgetCreate",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Budgets"],
                extensions={
                    "x-code-samples": [
                        {
                            "label": "Node.js",
                            "lang": "typescript",
                            "source": 'await orq.budgets.create({ scope: { kind: "workspace" }, limits: { period: "MONTHLY", amount: 1000 } });\n',
                        }
                    ]
                },
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.CreateBudgetResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    def get(
        self,
        *,
        budget_id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.GetBudgetResponse:
        r"""Retrieve a budget

        Retrieves the metadata for an existing budget by its unique identifier. Returns `NotFound` when the budget does not exist in the caller's workspace.

        :param budget_id: Budget id to retrieve.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.BudgetGetRequest(
            budget_id=budget_id,
        )

        req = self._build_request(
            method="GET",
            path="/v2/budgets/{budget_id}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="BudgetGet",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Budgets"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.GetBudgetResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    async def get_async(
        self,
        *,
        budget_id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.GetBudgetResponse:
        r"""Retrieve a budget

        Retrieves the metadata for an existing budget by its unique identifier. Returns `NotFound` when the budget does not exist in the caller's workspace.

        :param budget_id: Budget id to retrieve.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.BudgetGetRequest(
            budget_id=budget_id,
        )

        req = self._build_request_async(
            method="GET",
            path="/v2/budgets/{budget_id}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="BudgetGet",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Budgets"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.GetBudgetResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    def delete(
        self,
        *,
        budget_id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.DeleteBudgetResponse:
        r"""Delete a budget

        Permanently deletes a budget. Consumption counters in Redis for this budget are cleared immediately. The response body is empty on success.

        :param budget_id: Budget id to delete.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.BudgetDeleteRequest(
            budget_id=budget_id,
        )

        req = self._build_request(
            method="DELETE",
            path="/v2/budgets/{budget_id}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="BudgetDelete",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Budgets"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.DeleteBudgetResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    async def delete_async(
        self,
        *,
        budget_id: str,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.DeleteBudgetResponse:
        r"""Delete a budget

        Permanently deletes a budget. Consumption counters in Redis for this budget are cleared immediately. The response body is empty on success.

        :param budget_id: Budget id to delete.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.BudgetDeleteRequest(
            budget_id=budget_id,
        )

        req = self._build_request_async(
            method="DELETE",
            path="/v2/budgets/{budget_id}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=False,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="BudgetDelete",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Budgets"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.DeleteBudgetResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    def update(
        self,
        *,
        budget_id: str,
        limits: Optional[
            Union[models.BudgetLimits, models.BudgetLimitsTypedDict]
        ] = None,
        rate_limit: Optional[Union[models.RateLimit, models.RateLimitTypedDict]] = None,
        is_active: Optional[bool] = None,
        expires_at: Optional[datetime] = None,
        clear_expires_at: Optional[bool] = None,
        match: Optional[Union[models.BudgetMatch, models.BudgetMatchTypedDict]] = None,
        alerts: Optional[
            Union[Iterable[models.BudgetAlert], Iterable[models.BudgetAlertTypedDict]]
        ] = None,
        clear_alerts: Optional[bool] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.UpdateBudgetResponse:
        r"""Update a budget

        Updates mutable fields of a budget: limits, rate limit, activation, and expiration. The scope is immutable — to change a budget's target, delete and recreate it. Omitted fields keep their current values.

        :param budget_id: Budget id to update.
        :param limits: New limits. Omit to keep current.
        :param rate_limit: New rate limit. Omit to keep current.
        :param is_active: New active state. Omit to keep current.
        :param expires_at: New expiration. Omit to keep current. Set `clear_expires_at = true`
            to remove an existing expiration.
        :param clear_expires_at: Force-clear the expiration. Mutually exclusive with `expires_at`.
        :param match: New matching expression. Only valid for dynamic budgets (no
            structured scope) — the scope of a scoped budget is immutable, so
            its derived expression is too. Validated via CEL parse.
        :param alerts: Replaces the alert list wholesale. Omit to keep the current alerts.
            An entry with a known `id` is edited in place; one with no id is minted.
        :param clear_alerts: Force-clear every alert. Mutually exclusive with a non-empty `alerts`.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.BudgetUpdateRequest(
            budget_id=budget_id,
            update_budget_request=models.UpdateBudgetRequest(
                limits=utils.get_pydantic_model(limits, Optional[models.BudgetLimits]),
                rate_limit=utils.get_pydantic_model(
                    rate_limit, Optional[models.RateLimit]
                ),
                is_active=is_active,
                expires_at=expires_at,
                clear_expires_at=clear_expires_at,
                match=utils.get_pydantic_model(match, Optional[models.BudgetMatch]),
                alerts=utils.get_pydantic_model(
                    alerts, Optional[List[models.BudgetAlert]]
                ),
                clear_alerts=clear_alerts,
            ),
        )

        req = self._build_request(
            method="PATCH",
            path="/v2/budgets/{budget_id}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.update_budget_request,
                False,
                False,
                "json",
                models.UpdateBudgetRequest,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="BudgetUpdate",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Budgets"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.UpdateBudgetResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    async def update_async(
        self,
        *,
        budget_id: str,
        limits: Optional[
            Union[models.BudgetLimits, models.BudgetLimitsTypedDict]
        ] = None,
        rate_limit: Optional[Union[models.RateLimit, models.RateLimitTypedDict]] = None,
        is_active: Optional[bool] = None,
        expires_at: Optional[datetime] = None,
        clear_expires_at: Optional[bool] = None,
        match: Optional[Union[models.BudgetMatch, models.BudgetMatchTypedDict]] = None,
        alerts: Optional[
            Union[Iterable[models.BudgetAlert], Iterable[models.BudgetAlertTypedDict]]
        ] = None,
        clear_alerts: Optional[bool] = None,
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.UpdateBudgetResponse:
        r"""Update a budget

        Updates mutable fields of a budget: limits, rate limit, activation, and expiration. The scope is immutable — to change a budget's target, delete and recreate it. Omitted fields keep their current values.

        :param budget_id: Budget id to update.
        :param limits: New limits. Omit to keep current.
        :param rate_limit: New rate limit. Omit to keep current.
        :param is_active: New active state. Omit to keep current.
        :param expires_at: New expiration. Omit to keep current. Set `clear_expires_at = true`
            to remove an existing expiration.
        :param clear_expires_at: Force-clear the expiration. Mutually exclusive with `expires_at`.
        :param match: New matching expression. Only valid for dynamic budgets (no
            structured scope) — the scope of a scoped budget is immutable, so
            its derived expression is too. Validated via CEL parse.
        :param alerts: Replaces the alert list wholesale. Omit to keep the current alerts.
            An entry with a known `id` is edited in place; one with no id is minted.
        :param clear_alerts: Force-clear every alert. Mutually exclusive with a non-empty `alerts`.
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.BudgetUpdateRequest(
            budget_id=budget_id,
            update_budget_request=models.UpdateBudgetRequest(
                limits=utils.get_pydantic_model(limits, Optional[models.BudgetLimits]),
                rate_limit=utils.get_pydantic_model(
                    rate_limit, Optional[models.RateLimit]
                ),
                is_active=is_active,
                expires_at=expires_at,
                clear_expires_at=clear_expires_at,
                match=utils.get_pydantic_model(match, Optional[models.BudgetMatch]),
                alerts=utils.get_pydantic_model(
                    alerts, Optional[List[models.BudgetAlert]]
                ),
                clear_alerts=clear_alerts,
            ),
        )

        req = self._build_request_async(
            method="PATCH",
            path="/v2/budgets/{budget_id}",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.update_budget_request,
                False,
                False,
                "json",
                models.UpdateBudgetRequest,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="BudgetUpdate",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Budgets"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(models.UpdateBudgetResponse, http_res)
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    def reset_consumption(
        self,
        *,
        budget_id: str,
        reset_budget_consumption_request: Union[
            models.ResetBudgetConsumptionRequest,
            models.ResetBudgetConsumptionRequestTypedDict,
        ],
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.ResetBudgetConsumptionResponse:
        r"""Reset budget consumption

        Clears the current-period cost, token, and request counters for the budget. The budget record itself is preserved; only the Redis counters are reset.

        :param budget_id: Budget id whose current-period counters should be cleared.
        :param reset_budget_consumption_request:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.BudgetResetConsumptionRequest(
            budget_id=budget_id,
            reset_budget_consumption_request=utils.get_pydantic_model(
                reset_budget_consumption_request, models.ResetBudgetConsumptionRequest
            ),
        )

        req = self._build_request(
            method="POST",
            path="/v2/budgets/{budget_id}/reset-consumption",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.reset_budget_consumption_request,
                False,
                False,
                "json",
                models.ResetBudgetConsumptionRequest,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = self.do_request(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="BudgetResetConsumption",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Budgets"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.ResetBudgetConsumptionResponse, http_res
            )
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = utils.stream_to_text(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)

    async def reset_consumption_async(
        self,
        *,
        budget_id: str,
        reset_budget_consumption_request: Union[
            models.ResetBudgetConsumptionRequest,
            models.ResetBudgetConsumptionRequestTypedDict,
        ],
        retries: OptionalNullable[utils.RetryConfig] = UNSET,
        server_url: Optional[str] = None,
        timeout_ms: Optional[int] = None,
        http_headers: Optional[Mapping[str, str]] = None,
    ) -> models.ResetBudgetConsumptionResponse:
        r"""Reset budget consumption

        Clears the current-period cost, token, and request counters for the budget. The budget record itself is preserved; only the Redis counters are reset.

        :param budget_id: Budget id whose current-period counters should be cleared.
        :param reset_budget_consumption_request:
        :param retries: Override the default retry configuration for this method
        :param server_url: Override the default server URL for this method
        :param timeout_ms: Override the default request timeout configuration for this method in milliseconds
        :param http_headers: Additional headers to set or replace on requests.
        """
        base_url = None
        url_variables = None
        if timeout_ms is None:
            timeout_ms = self.sdk_configuration.timeout_ms

        if timeout_ms is None:
            timeout_ms = 600000

        if server_url is not None:
            base_url = server_url
        else:
            base_url = self._get_url(base_url, url_variables)

        request = models.BudgetResetConsumptionRequest(
            budget_id=budget_id,
            reset_budget_consumption_request=utils.get_pydantic_model(
                reset_budget_consumption_request, models.ResetBudgetConsumptionRequest
            ),
        )

        req = self._build_request_async(
            method="POST",
            path="/v2/budgets/{budget_id}/reset-consumption",
            base_url=base_url,
            url_variables=url_variables,
            request=request,
            request_body_required=True,
            request_has_path_params=True,
            request_has_query_params=True,
            user_agent_header="user-agent",
            accept_header_value="application/json",
            http_headers=http_headers,
            security=self.sdk_configuration.security,
            get_serialized_body=lambda: utils.serialize_request_body(
                request.reset_budget_consumption_request,
                False,
                False,
                "json",
                models.ResetBudgetConsumptionRequest,
            ),
            allow_empty_value=None,
            timeout_ms=timeout_ms,
        )

        if retries == UNSET:
            if self.sdk_configuration.retry_config is not UNSET:
                retries = self.sdk_configuration.retry_config

        retry_config = None
        if isinstance(retries, utils.RetryConfig):
            retry_config = (retries, ["429", "500", "502", "503", "504"])

        http_res = await self.do_request_async(
            hook_ctx=HookContext(
                config=self.sdk_configuration,
                base_url=base_url or "",
                operation_id="BudgetResetConsumption",
                oauth2_scopes=None,
                security_source=get_security_from_env(
                    self.sdk_configuration.security, models.Security
                ),
                tags=["Budgets"],
                extensions=None,
            ),
            request=req,
            is_error_status_code=lambda c: utils.match_status_codes(["4XX", "5XX"], c),
            retry_config=retry_config,
        )

        if utils.match_response(http_res, "200", "application/json"):
            return unmarshal_json_response(
                models.ResetBudgetConsumptionResponse, http_res
            )
        if utils.match_response(http_res, "4XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)
        if utils.match_response(http_res, "5XX", "*"):
            http_res_text = await utils.stream_to_text_async(http_res)
            raise models.APIDefaultError("API error occurred", http_res, http_res_text)

        raise models.APIDefaultError("Unexpected response received", http_res)
